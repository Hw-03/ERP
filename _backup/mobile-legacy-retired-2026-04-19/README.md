# Retired Mobile Legacy UI

Date: 2026-04-19

These files were previously used by the old mobile-only legacy screen derived from the earlier HTML reference.
They were removed from the active route so the app no longer switches between the old mobile page and the current main page based on viewport.

Active change summary:
- `frontend/legacy/page.tsx` now renders only the current main shell.
- The old mobile legacy components were moved into this backup folder.
