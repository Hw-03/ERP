# 허동현
받은 피드백: 3건 (해결 2 / 미해결 0 / 보류 1)

---

## 2026-05-28
### 입출고 내역 — 재작업 행 우측 패널 자식 부품명 표시
#입출고내역 #재작업 #불편 #해결

재작업 행을 클릭 했을 때 오른쪽에 열리는 정보가 '튜브 고정 보드 에노드 (나사) [ADX6000]'로 이상하게 표시됨.

→ 해결 (2026-05-28)
- 원인: 헤더 행은 `DISASSEMBLE` 부모 로그를 기준으로 표기하는데, 우측 패널은 `bundles[0].title`을 그대로 써서 자식 부품 번들이 0번에 올 경우 자식명이 표시됨.
- 수정: `frontend/app/legacy/_components/_history_sections/DesktopHistoryRightPanel.tsx` — 재작업 batch는 `DISASSEMBLE` 로그를 찾아 "{부모 품목명} 재작업"으로 통일.

---

### 입출고(불량) — 재작업 BOM 펼침 버튼 안 보임
#입출고불량 #재작업 #BOM #불편 #해결

재작업 시에 하위 품목을 펼치게 하는 버튼이 눈에 잘 보이지 않음.

→ 해결 (2026-05-28)
- 원인: 접힌 상태 chevron 색이 회색(`muted2`) + 24px 크기 + 투명 배경 → 시인성 낮음.
- 수정: `frontend/app/legacy/_components/_defect_hub/DisassembleTree.tsx` — 28px + blue 14% 옅은 배경 + blue 30% 테두리 + chevron 색 항상 blue.

---

### 주간보고 — 재작업이 출고로 안 잡힘
#주간보고 #재작업 #기타 #보류

재작업 시에 재고가 줄어드는건데, 주간보고 화면에서 출고로 적용되지 않는데, 이게 맞는지 의문.

→ 보류 — 정책 결정 대기
- 현재 동작: `backend/app/routers/inventory/weekly_report.py` 의 `_OUT_TYPES = {SHIP, BACKFLUSH}` 만 출고로 집계. 재작업의 `DEFECT_SCRAP`·`DISASSEMBLE`은 빠져 있어 재작업으로 ?F 완제품 재고가 줄어도 출고에 안 잡힘.
- 두 가지 방향 검토 중:
  - A. 백엔드 `_OUT_TYPES`에 `DEFECT_SCRAP` 추가 → 출고에 합산 (한 줄 변경).
  - B. 주간보고에 "불량/폐기" 별도 카드 신설 → 주간보고 화면 동결(2026-05-24) 해제 필요.
- 사용자 결정 대기.
