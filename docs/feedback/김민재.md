# 김민재
받은 피드백: 1건 (해결 1 / 미해결 0 / 보류 0)

---

## 2026-05-28
### 입출고 — SOLO 모델 필터 + "하네스" 검색 시 일부 품목 누락
#입출고 #대시보드 #필터 #검색 #SOLO #불편 #기타 #해결

SOLO 모델 구분 필터를 걸고 "하네스"라고 입력했을 때 'SOLO OP to CTR 40핀 FFC CABLE 100mm [하네스 3번]'이 표시되지 않음.

김현우 확인: 대시보드에서도 "하네스 3번"이라고만 검색하면 나오는데, 필터랑 동시에 하면 안 나옴.

→ 해결 (2026-05-29) — 근본 해결 (C+B 플랜 적용).
- 원인: `item_models` 테이블 411개 빈 채 방치 → 모델 필터가 마스터 의존 → 마스터 누락 부품 탈락. 부품 코드 앞글자(`8-` = SOLO)가 모델을 보장하는 회사 규약이 코드에 안 박혀 있어 발생.
- 수정:
  - `backend/app/utils/item_code.py` — 새 헬퍼 `item_code_to_model_slots()` 추가 (prefix → slot 변환).
  - `backend/app/routers/items.py` — 응답 `model_slots` 를 prefix 기반으로 계산.
  - `backend/app/routers/inventory/transactions.py` — `_model_filter()` SQL 을 `substr/instr` 기반 prefix LIKE 로 변경.
  - `backend/app/models/item.py`, `__init__.py`, `routers/models.py` — `ItemModel` 클래스/import 모두 제거.
  - `backend/bootstrap/migrate.py` — `DROP TABLE item_models` 추가.
  - DB 백업: `backend/_backup/mes_pre_item_models_drop_2026-05-29.db`.
- 백엔드 pytest 302 passed. 회귀 없음.
