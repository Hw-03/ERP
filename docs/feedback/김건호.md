# 김건호
받은 피드백: 1건 (해결 1 / 미해결 0 / 보류 0)

---

## 2026-05-28
### 관리자(BOM) — 수량 입력 시 소수점 넷째자리까지 표시
#관리자 #BOM #소수점 #불편 #해결

수량 입력 시에 소수점 넷째자리 까지 표시되는 현상.

→ 해결 (2026-05-29) — 허동현 입출고(불량) 소수점 건과 같이 묶어 처리.
- 수정: `BomRow.tsx` — 입력 `step="any"` → `step="0.01"`. 표시에서 `formatQty(row.quantity, { maximumFractionDigits: 2, trimTrailingZeros: true })` 적용 → 1.0000 같은 과도한 자릿수 사라짐.
