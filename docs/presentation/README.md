# DEXCOWIN MES — 사내 발표 슬라이드

2026.05 발표용 HTML 슬라이드 데크. 단일 파일이라 USB·메일로 그대로 전달 가능.

## 실행

방법 1 — 더블클릭
```
docs\presentation\index.html
```
크롬·엣지·파이어폭스 모두 OK.

방법 2 — 풀스크린으로 바로
```powershell
start chrome --start-fullscreen "file:///c:/ERP/docs/presentation/index.html"
```

## 키보드 단축키

| 키 | 동작 |
|---|---|
| `→` `↓` `Space` `PgDn` | 다음 슬라이드 |
| `←` `↑` `PgUp` | 이전 슬라이드 |
| `1` ~ `9` | 슬라이드 번호로 직접 점프 |
| `Home` / `End` | 첫 / 마지막 슬라이드 |
| `F` | 풀스크린 토글 |
| `ESC` | 풀스크린 해제 |
| `N` | 발표자 노트 패널 토글 |
| `T` | 슬라이드 7 시연 체크리스트 다음 항목 체크 |

## 발표자 노트

- 노트 보기: URL 끝에 `?notes=1` 추가하거나 발표 중 `N`키
- 각 슬라이드마다: 말할 한 줄 / 청중 반응 예상 / Q&A 대비 답변
- 인쇄: `Ctrl+P` → 가로 A4. 슬라이드 한 페이지 + 노트 한 페이지로 출력

## 시연 체크리스트 (슬라이드 7)

발표 도중 alt+tab으로 실제 MES로 이동 → 시연 → 슬라이드 복귀 시 어디까지 했는지 표시.

- 항목 클릭 또는 `T`키로 체크 / 체크 해제
- localStorage 저장 — 새로고침해도 유지
- 전부 체크된 상태에서 `T` 한 번 더 누르면 초기화 (다음 발표 대비)

시연 6항목:
1. PIN 로그인
2. 창고 입출고 요청
3. 창고 정/부 승인 처리
4. 부서 재고 현황 확인
5. BOM 입고 → 하위 자동출고 확인
6. 불량 등록

총 시연 시간 ≈ 6분.

## 발표 전 점검

- [ ] 회의실 PC에서 한 번 열어 폰트·로고 표시 확인
- [ ] 프로젝터 연결 시 색상·뒷줄 가독성
- [ ] 인터넷 안 되는 경우 대비 — Tailwind/Pretendard CDN 로딩 확인
- [ ] MES 시연 데이터 사전 입력 (재고·BOM 데이터 등)
- [ ] 시연 체크리스트 초기화 (`T` 6번 또는 localStorage 클리어)

## 슬라이드 구성 (총 9장)

| # | 내용 |
|---|------|
| 1 | 표지 |
| 2 | 기존 방식의 문제점 (4가지) |
| 3 | 품목 분류 (원자재·중간공정·공정완료) |
| 4 | 창고 ↔ 부서 입출고 Before/After |
| 5 | 부서 내 입출고 (BOM 자동출고·낱개 승인) |
| 6 | 앞으로 관리 방식 (3가지 규칙) |
| 7 | 라이브 시연 체크리스트 |
| 8 | 피드백 수집 |
| 9 | Q&A |

## 파일 구조

```
docs/presentation/
  index.html   # 단일 파일 (CSS·JS·노트·콘텐츠 모두 인라인)
  README.md    # 이 파일
```

로고: `frontend/public/dexcowin-logo.png` (상대경로 직접 참조, 별도 복사 불필요)

## 콘텐츠 업데이트

전부 `index.html` 한 파일에 인라인. 슬라이드별로 `<!-- ============ Slide N ============ -->` 주석으로 구분.
