# 현재 데이터 보존 및 품질 병합 전 코드 복원

추천 모델: GPT-5.6 Sol / 추천 추론 수준: Extra High / 실행 방식: 순차 작업·독립 검토

## 승인된 기준

- 코드 기준: `76a564166d48b839131a4d2c99cd965da236457b` (2026-09-11 09:41 KST).
- 현재 직원 DB의 모든 업무 행, 마이그레이션 이력, 오늘 정상 거래와 취소·복구 결과 유지.
- DB 다운그레이드, 과거 백업 덮어쓰기, 버전 값 위조, 복구 재실행 금지.
- 품질개선 전체와 오늘 코드는 보존 브랜치에 보관; 메인에는 구버전에 필요한 수정과 현재 데이터 호환 코드만 포함.
- 메인 이력을 유지하는 새 복원 커밋과 일반 push 사용. 강제 push 금지.
- 김종숙 주임의 원래 9품목 작업은 미완료로 인계. 임의 재입력·이동 금지.

## 실행 체크리스트

- [x] 관련 작업이 idle인지 확인하고 HEAD/미커밋/미추적 상태 및 원본 파일 해시 보존 (2026-09-14).
- [x] 현재 직원 DB 온라인 백업 생성: schema/SQLite/FK 검증 통과 (12:27 KST).
- [x] 실제 직원 배포 코드·로그·비밀 설정의 별도 로컬 보존 (Git 제외).
- [x] `codex/quality-preserved-20260914` 브랜치·워크트리에 모든 변경 보존 및 검증 수준 기록 (커밋 `8968577cf3a2da05dd2439157db56072670e5596`, 릴리스 검증 미통과 상태를 명시).
- [ ] `codex/rollback-pre-quality-20260914` 후보 구성, 금요일 대비 남긴 변경과 이유 작성.
- [ ] 스키마/원장/예약/취소/이후 쓰기 호환성 독립 검토.
- [ ] 최신 직원 DB의 격리 복제본으로 기동·조회 불변성, 신규 입출고, 승인·반려, 취소·재시도, 출하 준비·완료 검증.
- [ ] 오늘 거래·복구 결과 보존 및 미완료 9품목 불변성 대조.
- [ ] 모바일 제출 → 별도 PC 자동 갱신·새로고침 → DB 대조.
- [ ] 동일 DB·부하에서 현행/후보 입출고 내역·생산 가능량·품목 조회 성능 비교.
- [ ] 필수 로컬 검사와 독립 최종 검토.
- [ ] 메인 반영, 일반 push, 예약 동기화·다른 배포 충돌 방지.
- [ ] 직원 쓰기 잠시 정지, 최종 백업, 검증된 코드·의존성·빌드 전환.
- [ ] 재개 전 데이터·핵심 조회·헬스 확인. 실패 시 코드만 직전 배포로 복귀; 최신 데이터 유지.
- [ ] 완료 보고: 보존 브랜치/워크트리, 복원 커밋, 호환 예외, 백업, 검증, 미완료 업무.

## 보존 증적

- 로컬 증적 루트: `C:\ERP\_attic\runtime\rollback-pre-quality-20260914` (Git 제외).
- 초기 HEAD: `a0cf0809f7ac3cb0739ce177e29fb04028080a0e`.
- 오늘 미커밋 항목 106개 및 전체 작업 파일 2635개 원본 바이트·해시 보존.
- 직원 DB: `employee-backup/backups/sqlite/mes-before-pre-quality-rollback-20260914-122704-82e060b9.db` 및 manifest.
- `preservation/source-manifest.json`, `capture.json`, `all-refs.bundle`, `working-tree.patch`, `index.patch`.
- `preservation/employee-code-manifest.json`, `employee-code/`, `employee-private/`, `employee-logs/`.
- 미커밋 코드 보존은 검증 완료 배포를 뜻하지 않는다. 검사 결과는 별도 기록한다.

## 운영 전환 조건

데이터 정합성 또는 핵심 업무 검증이 실패하면 메인·직원 환경에 전환하지 않는다.

## 사전 검토에서 확인된 범위 문제 (2026-09-14)

현재 DB의 contract2 원장, 물리 창고 미배치 수량, 출하 예약·명령 영수증, 요청 fingerprint, operator session을 유지하려면 재고·요청·취소·출하 mutation 처리부 대부분이 함께 남아야 한다는 독립 검토 결과다.
파일 몇 개를 남기는 단순 호환 이식으로 서버 동작 전체를 금요일 상태로 되돌린다고 보장할 수 없다.
화면 위주 복원으로 범위를 축소하거나 별도 호환 계층을 재설계하는 것은 사용자 의도 확인이 필요하므로 메인 반영·직원 전환 전 중단한다.

보존본 필수 검사도 전용 PostgreSQL 환경 미설정으로 미통과 상태다. 검사 생략을 통과로 처리하지 않는다.

## 중단 시 확인 결과

- 보존 커밋에 원래 106개 변경 경로가 모두 포함됨을 대조: 통과.
- 원래 `C:\ERP` 작업 파일 전체 SHA-256 재대조: 변경 없음.
- 직원 배포 backend/frontend/scripts 파일 SHA-256 재대조: 변경 없음.
- 메인 HEAD는 `a0cf0809` 그대로. 원격 push·메인 복원·직원 배포 미실행.
- 직원 온라인 백업 SHA-256 재검증 통과, 재고/예약/원장 진단 blocking 0. 기존 V1 증거 부족 경고 4797개는 그대로 유지했으며 복구 처리는 하지 않음.
- 직원 8010 live/ready 정상, 3000 HTTP 200 확인. 운영 서비스 정지·재기동 없음.
- 후보 워크트리는 계획 문서만 생성했으며, 금요일 코드 복원·호환 이식·업무/브라우저/성능 A/B 검증은 아직 하지 않음.
- 김종숙 주임의 9품목 작업은 미완료로 유지; 추가 이동·재입력 미실행.

## 호환성 근거 (읽기 전용 검토)

- 직원 백업은 revision `20260911_0036`, contract1 작업 427건, contract2 작업 35건, 미배치 창고 행 1050건, fingerprint가 있는 입출고 묶음 30건이다. 개발 DB 집계로 직원 상태를 대신하지 않았다.
- 금요일 `create_business_operation`은 contract1을 생성하지만 현재 `backend/app/services/inventory_operations.py:83`은 contract2를 생성한다.
- 현재 `backend/app/services/warehouse_map.py:406`은 창고 재고 = 박스 + 구역 + 미배치 원장을 검사하며, `:427`의 쓰기 경계가 재고와 물리 원장 델타를 같이 적용한다.
- 현재 `backend/app/dependencies/verified_actor.py:55`는 cookie→operator session→직원을 검증한다. 구 UI의 로컬 로그아웃만 복원하면 서버 세션과 어긋나므로 인증·로그아웃·캐시 처리도 함께 이식해야 한다.
- 현재 `backend/app/routers/shipping.py:519`의 legacy 목록은 최대 50건이다. 금요일 출하 화면을 그대로 붙이면 전체 목록이라는 구 가정과 충돌하므로 페이지 조회 이식도 필요하다.
- 독립 검토에서 현행 유지가 필요한 경계: migration/model/bootstrap, actor/session, command idempotency, 재고·물리 원장 writer, 취소·수량 보정, 예약 가용량·승인 실행, 출하 receipt·상태 전이, 오늘 입출고 안전 처리.
- 이 결과는 호환 범위 검토이며 후보 실행 검증 또는 성능 개선 증거가 아니다.
