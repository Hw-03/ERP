# 입출고 전수 검토·수정 및 오늘 거래 복구

**추천 모델: GPT-5.6 Sol** — 화면·서버·재고 원장 경계 통합.
**추천 추론 수준: Extra High** — 방향·권한·취소 및 기존 요청 의미 보존.
**실행 방식: 하위 에이전트 병렬 작업** — 프런트, 서버 제출, 취소를 분담하고 부모가 통합·검증한다.

**GOAL:** 직원이 선택한 작업과 실제 재고 변동을 일치시키고, 오늘 잘못 반영된 거래의 안전한 복구안을 확정한다.

사용자가 2026-09-14 계획 구현을 승인했다. 자동 커밋·푸시·브랜치 변경은 하지 않는다. 기존 미커밋 파일은 `_attic/runtime/io-safety-20260914/initial-worktree/`와 `initial-manifest.json`, `initial.diff`에 보존했다.

## 구현·검증 체크리스트

- [x] 작업 미선택·권한을 PC/모바일 URL 단계 및 초안 복원에서 검증한다. (2026-09-14, 화면·URL·초안 회귀 테스트와 합성 브라우저)
- [x] 원자재 입고 역할과 작업/하위유형 허용 조합을 서버 모든 입구에서 검증한다. (2026-09-14, `test_io_safety_contract.py`)
- [x] 커스텀 BOM 입고·출고 방향을 제출·승인·재시도까지 보존한다. (2026-09-14, dispatch·취소 재승인 계약 및 PC/모바일 승인 후 독립 SQL)
- [x] 즉시 완료형 입출고 취소를 terminal cancelled로 닫고 같은 키 재시도 결과를 일치시킨다. (2026-09-14, transaction cancel 계약과 PC/모바일 실제 취소)
- [x] 재현 RED/GREEN 및 기존 정상 입출고·연결 업무 회귀를 확인한다. (2026-09-14, `verification-report.md`에 실패 재현·수정 후 증거 정리)
- [x] 합성 DB 브라우저와 독립 SQL을 대조하고 전체 검증 게이트를 실행한다. (2026-09-14, 브라우저 고유 27개, PostgreSQL 동시성, production build 확인. 단일 full 명령의 중단 원인은 수정하고 실패 검사·남은 게이트를 개별 검증)
- [x] 독립 명세 검토·코드 검토를 수행하고 차단 지적을 해결한다. (2026-09-14, `review-contract.md`, `review-frontend-quality.md`; 역사적 취소 상태 복구의 승인형 오판 경계까지 재검토 완료)
- [x] 직원 DB·로그 보존, 오늘 전체 거래 대조, 복구 순서와 품목별 수량표를 확정한다. (2026-09-14 11:04:39 KST까지 대조. 직원이 이미 취소한 수량은 추가 변동 0, 세 배치 상태만 복구하는 복사본 리허설 통과)
- [x] 직원 배포 대상과 실제 반영 여부, 검사하지 못한 영역을 최종 보고한다. (2026-09-14, `deployment-code-manifest.json`, `final-recovery-plan.md`, `verification-report.md`. 직원 배포·실제 DB 정정은 미수행)

## 확정 재현과 수량 계약

- 김종숙 08:13 `9513bb7d64564076a63c6dc7bd5452d0`: 원자재 입고로 저장된 9품목.
- 이계숙 08:24 `1e1d5b71134a48a38c574d2e84877bac`: HANDLE CONNECTOR BD 창고 +1.
- 이계숙 08:54 `5aea8d7baa094b76ab095d04f1b766d3`: HANDLE CONNECTOR BD 창고 +15, BAT BOTTOM RUBBER 창고 +16.
- 08:44 custom produce 자재 두 품목 -5씩은 08:49 역분개 +5씩으로 복원됐으므로 추가 보정하지 않는다.
- 커스텀 생산 입고는 선택 하위만 +, 커스텀 분해 출고는 선택 하위만 -. 기본 BOM 생산/분해의 상위·하위 변화는 기존 업무 계약대로 유지한다.
- 일반 직원의 원자재 입고 preview/save/submit은 403, 잘못된 work_type/sub_type은 422. 거절 시 배치·거래·재고 변화가 없어야 한다.
- 즉시 완료형 취소는 재고 역전과 batch cancelled를 함께 보장한다. 승인형 취소의 승인 전 상태 복원 정책과 원본 원장은 보존한다.

## 검증 범위

원자재, 창고↔부서, 낱개 입출고, 기본/커스텀/혼합 BOM, 여러 부서, 면제·부족 자재, 사용출고와 불량·출하 경계를 확인한다. 신규/초안/승인/거절/취소/재시도 및 재진입/탭재클릭/뒤로·앞으로/PC·모바일 전환을 대상으로 화면 예상 효과 → 요청 → 독립 SQL → 원장·상태를 대조한다. 실제 직원 DB를 브라우저 시험 데이터로 사용하지 않는다.

## 복구 원칙

**2026-09-14 10:32 KST 재조회로 계획 변경:** 권동환 계정이 10:09·10:10·10:11에 세 문제 입고를 이미 취소했다. 원본에 연결된 역전 거래가 확인되므로 아래의 재고 취소 순서는 역사적 리허설 기록으로만 보존하며 다시 실행하지 않는다. 구버전 결함으로 남은 세 배치의 `submitted` 상태만 복구할 방법을 별도 복사본에서 검증한다. 09:09의 두 품목 +5 요청은 10:12 승인됐고, 10:18 HANDLE ASS'Y 생산에서 각각 16개가 소비됐으므로 최신 대조표에 이 후속 거래를 포함한다.

**2026-09-14 11:04:39 KST 최종 확정:** 최신 보존본에서 세 배치의 상태만 복구한 리허설이 통과했다. 다른 55개 테이블은 불변이고 정합성 차단 오류는 3건에서 0건이 됐다. 재고 취소 API는 다시 실행하지 않는다. 실제 운영 반영 직전에는 새 백업과 최신 진단으로 세 상태의 잔여 여부를 확인한다. 기존 수동 보정·승인 요청과 실제 입고 의도를 대조하지 않은 조립 재고 재등록은 하지 않는다. 교차 부서 취소 권한은 기존 정책을 유지했다.

## 증거

실행 로그·검증 결과·복구표: `_attic/runtime/io-safety-20260914/`.

## 후속 운영 반영 완료 — 2026-09-14 11:42 KST

사용자가 실제 적용을 추가 승인했다. 상태 잔여가 배포 사전 검증을 막아 백업·독립 검증 후 세 배치를 먼저 정리했다. 하나의 쓰기 트랜잭션에서 다른 55개 테이블 불변을 확인한 뒤 커밋했으며, 복구 후 전체 백업과 정합성 검사도 통과했다. 최근 활동 보호 시간이 지난 뒤 정식 코드 배포를 수행했고 최종 exit 0, 배포 저널 `CONFIRMED`, 직원 8010/3000 정상 응답을 확인했다. 재고 수량은 변경하지 않았다. 운영 증거는 `live-state-repair-result.json`, `employee-deploy.log`, `post-deployment-data-check.json`, `deployed-code-manifest.json`에 보존했다. 위 체크리스트의 '미수행'은 최초 구현 완료 시점의 기록이다.
