> **추천 모델: GPT-5.6 Terra** - 4개 대형 엑셀의 품목 구조·서식·수식과 현행 MES 마스터를 함께 대조해야 하는 중간 위험도의 데이터 작업입니다.
> **추천 추론 수준: 높음** - 행 순서가 아닌 품목 식별자로 재매칭하고, 추가·삭제·중복·서식 보존을 구분해 검증해야 합니다.
> **실행 방식: 단독** - 동일한 매칭 규칙과 DB 기준을 4개 파일에 일관되게 적용하고 마지막 교차 검증까지 한 흐름으로 처리합니다.

# 최신 생산부 재고 MES 확인본 재구성 계획

**GOAL:** 2026년 7월 최신 생산부 재고 사본 4개에 현행 DEXCOWIN MES 매칭·담당자 확인 자동화를 이식하고, 품목 변동과 원본 보존을 검증한다.

**목표:** `최신` 폴더의 수량·품목 구성은 그대로 유지하면서, 상위 폴더의 수동 보정된 최종본에서 MES 매칭·열 너비·확인 서식을 안전하게 옮긴다.

**구조:** 행 번호로 복사하지 않고 각 품목의 기존 품명·모델·규격·구분을 정규화한 품목 지문으로 대조한다. 작업은 원본이 아닌 별도 스테이징 사본에서 진행하고, 현행 `backend/mes.db`를 읽기 전용으로 재검증한 뒤 모든 검사를 통과한 결과만 `최신` 폴더에 반영한다.

**도구:** Microsoft Excel 형식, `@oai/artifact-tool` 렌더링·검사·편집, 필요 시 Excel 자동화 호환성 검증, Python/PowerShell 비교 스크립트, SQLite 읽기 전용 조회

---

## 대상과 기준

### 수정 대상

- `C:\ERP\_attic\data\2607 생산부 재고\최신\2026.07_생산부 자재_고압,진공,튜닝파트.xlsx`
- `C:\ERP\_attic\data\2607 생산부 재고\최신\2026.07_생산부 자재_조립,출하파트.xlsx`
- `C:\ERP\_attic\data\2607 생산부 재고\최신\2026.07_생산부 자재_튜브 파트.xlsx`
- `C:\ERP\_attic\data\2607 생산부 재고\최신\2026.07_출하_완제품.xlsx`

### 참고 대상

- 같은 파일명의 상위 폴더 최종본 4개: MES 매칭, 열 너비, 헤더, 조건부 서식, 담당자 확인 방식의 참고본
- `C:\ERP\backend\mes.db`: 활성 MES 품목명·코드의 유일한 최종 기준이며 `mode=ro`로만 조회

### 제외 대상

- 창고 파일은 `최신` 폴더에 없으므로 이번 작업에서 수정하지 않는다.
- 입고·출고 내역 및 과거 월 시트는 매칭 대상 시트가 아니면 수정하지 않는다.
- Git 커밋·푸시와 MES DB 변경은 하지 않는다.

---

## Task 1: 변경 전 기준선과 실패 검사 만들기 `[GPT-5.6 Terra / 높음 / 순차]`

**작업 파일:**

- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\audit_latest_inventory.py`
- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\baseline.json`
- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\before-render\`

- [ ] 수정 대상 4개와 `backend/mes.db`의 SHA-256을 기록한다.
- [ ] 각 통합 문서의 시트명, 사용 범위, 병합 셀, 이미지·도형 수, 숨김 행·열, 수식 위치, 열 너비, 행 높이, 인쇄 설정을 기록한다.
- [ ] 실제 대상 시트를 작업 전에 렌더링해 현재 서식을 눈으로 확인한다.
- [ ] 감사 스크립트에 다음 성공 조건을 먼저 작성한다.
  - 모든 품목 행에 MES 품목명·MES 코드·담당자 확인·확인 상태 구조가 있다.
  - 채워진 MES 품목명·코드 쌍이 현행 활성 DB 품목과 정확히 일치한다.
  - 담당자 확인에 `O`를 입력하면 `확인완료`, 지우면 코드 유무에 따라 `확인대기` 또는 `확인필요`로 돌아간다.
  - 변경 허용 범위 밖의 값·수식과 모든 재고 수량은 변경 전 `최신` 파일과 같다.
- [ ] 수정 전 `최신` 파일에 감사 스크립트를 실행해 MES 확인 구조가 없거나 최신 행과 매핑되지 않아 실패하는 것을 확인한다. 이 실패가 이후 작업의 기준 테스트가 된다.

## Task 2: 최종본과 최신본의 품목·서식 차이 분석 `[GPT-5.6 Terra / 높음 / 순차]`

**작업 파일:**

- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\item-diff.json`
- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\style-diff.json`

- [ ] 수량, 합계, 수식, 행 번호, MES 열을 제외하고 기존 품명·모델·규격·구분·메모 등 식별 열로 품목 지문을 만든다.
- [ ] 다음 순서로 각 최신 행을 상위 폴더 최종본의 행과 대조한다.
  1. 모든 식별 필드가 정규화 후 정확히 일치하는 단일 행
  2. 품명+모델+규격의 강한 키가 단일하게 일치하는 행
  3. 현재 DB에서 기존 품명이 단일 활성 품목과 정확히 일치하는 행
  4. 후보 없음·복수 후보·식별 필드 충돌은 미확정 행
- [ ] 파일별로 `그대로 유지`, `순서만 변경`, `신규 추가`, `삭제`, `중복/모호` 건수를 산출하고 품목명 목록을 남긴다.
- [ ] 열 너비, 행 높이, 보기 배율, 틀 고정, 헤더 스타일, 조건부 서식, 데이터 유효성 검사의 차이를 비교한다.
- [ ] 서식은 다음 범위만 이전 대상으로 확정한다.
  - 같은 헤더로 식별되는 기존 열의 최종본 열 너비·보기 설정
  - MES 확인 블록의 헤더·너비·채우기·글꼴·테두리·정렬·유효성 검사·조건부 서식
  - 고유하게 매칭된 행의 MES 블록 셀 스타일
  - 최신본에만 있는 행은 최신본의 인접 행 스타일을 유지하고 MES 블록 스타일만 새로 적용

## Task 3: 최신본 사본에 행 단위로 MES 확인 구조 이식 `[GPT-5.6 Terra / 높음 / 순차]`

**작업 파일:**

- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\staging\` 아래 대상 4개 사본
- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\apply_latest_inventory.py`

- [ ] `최신` 파일 4개를 스테이징 폴더로 복사하고 이후 편집은 사본에서만 수행한다.
- [ ] 상위 최종본의 현재 확인 블록을 기준으로 아래 위치를 우선 사용하되, 최신 파일에서 해당 위치가 새 데이터로 사용 중이면 덮어쓰지 않고 작업을 중단해 충돌로 보고한다.
  - 고압·진공·튜닝: `K:O`
  - 조립·출하 자재: `J:N`
  - 튜브: `I:M`
  - 출하 완제품: `V:Z`
- [ ] 각 블록을 `MES 품목명 / 빈 간격 열 / MES 코드 / 담당자 확인 / 확인`으로 구성한다.
- [ ] 행 번호가 아니라 Task 2의 고유 품목 매칭 결과로만 MES 품목명·코드를 옮긴다.
- [ ] 옮긴 MES 쌍은 현행 DB에 같은 코드·같은 품목명으로 활성 상태일 때만 유지한다.
- [ ] 신규·삭제 대응, 복수 후보, DB 불일치 행은 MES 품목명·코드를 비우고 `확인필요`가 나오도록 한다.
- [ ] 수량 0인 행도 포함해 모든 실제 품목 행에 상태 수식을 채운다.
- [ ] `담당자 확인="O"`이면 `확인완료`, 아니면 MES 코드가 있으면 `확인대기`, 없으면 `확인필요`가 되도록 행별 상대참조 수식을 적용한다.
- [ ] 상위 최종본에서 확인한 열 너비와 서식을 이전하되 최신본의 품목·수량·기존 수식·메모 값은 쓰지 않는다.

## Task 4: 자동 확인 동작과 통합 문서 보존 검증 `[GPT-5.6 Terra / 높음 / 순차]`

**작업 파일:**

- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\verification.json`
- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\after-render\`

- [ ] 스테이징 사본의 매칭된 행과 `확인필요` 행을 각각 골라 담당자 확인에 `O` 입력→저장→재열기→삭제→저장→재열기 순으로 검사한다.
- [ ] `O` 입력 시 `확인완료`와 녹색 확인 서식, 삭제 시 원래 상태와 해당 상태 서식으로 복귀하는지 확인한다.
- [ ] 모든 채워진 MES 코드가 현행 활성 DB에 존재하고 품목명이 정확히 일치하는지 전수 검사한다.
- [ ] 모든 품목 행의 확인 값이 `확인대기` 또는 `확인필요`인지, 담당자가 이미 `O`를 입력한 행만 `확인완료`인지 검사한다.
- [ ] 변경 전 최신본과 스테이징 사본을 비교해 허용 열 밖의 셀 값·수식, 재고 수량, 시트 순서, 병합 셀, 이미지·도형, 숨김 상태, 인쇄 설정이 동일한지 검사한다.
- [ ] 출하 완제품의 `총 합 = 현재고 + 시리얼 부여 X`, `테스트X 제외` 수식과 결과가 변경 전 최신본과 같은지 별도로 검사한다.
- [ ] 대상 시트를 다시 렌더링해 헤더, 열 너비, 행 정렬, 잘림, 색상, 조건부 서식을 눈으로 확인한다.
- [ ] DB SHA-256이 작업 전과 같은지 확인한다.

## Task 5: 검증 통과본만 최신 폴더에 반영 `[GPT-5.6 Terra / 높음 / 순차]`

**작업 파일:**

- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\backup\` 아래 변경 전 최신본 4개
- 수정: `C:\ERP\_attic\data\2607 생산부 재고\최신\` 아래 대상 4개
- 생성: `C:\ERP\_attic\runtime\inventory-2607-latest-20260728\final-report.json`

- [ ] 반영 직전에 대상 파일 해시가 Task 1과 같은지 확인해 사용자의 추가 수정이 없었는지 검사한다.
- [ ] 변경 전 최신 파일 4개를 복구용 백업 폴더에 보존한다.
- [ ] 모든 검사를 통과한 스테이징 사본만 같은 이름으로 `최신` 폴더에 반영한다.
- [ ] 반영된 실제 파일을 다시 열어 Task 1 감사 스크립트를 최종 실행한다.
- [ ] 최종 보고서에 파일별 전체 품목 수, 매칭 수, `확인필요` 수, 신규·삭제·순서 변경·모호 건수, DB 해시 불변, 수량·수식 불변 결과를 기록한다.

---

## 완료 조건

- 최신본의 수량과 기존 수식은 작업 전후가 완전히 같다.
- 품목 추가·삭제·순서 변경이 파일별로 식별되어 보고된다.
- 행 번호 복사 없이 고유 품목 매칭으로만 MES 값이 이전된다.
- 모든 제안 MES 품목명·코드는 현재 활성 DEXCOWIN MES DB와 정확히 일치한다.
- 모든 품목 행에 확인 상태가 있으며, `O` 입력과 삭제가 양방향으로 정상 동작한다.
- 원본 구조·이미지·병합·보기·인쇄 설정이 보존되고 새 열이 렌더링상 정상이다.
- `backend/mes.db`의 해시는 작업 전후 동일하다.
- 실패가 하나라도 있으면 `최신` 폴더 파일을 교체하지 않는다.
