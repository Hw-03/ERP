# 주간보고 이메일

업데이트할 때: 제목 날짜 범위 수정 → 진행 사항 교체

### 작성 규칙 (양식 고정)
- 진행 사항 번호는 항상 **1번부터** 매기기 (항목 추가/삭제 후 재번호 필수)
- 날짜: 제목과 본문 헤더(`■ 이번 주 진행 사항`)의 범위 반드시 일치
- 동료 호칭: "○○○ 사원"이 아니라 "○○○ **사원님**" (모든 직급 포함)
- "1차 초안" 표기는 권동환 사원님 사전 공유용 임시. 정식 발송 시 제목·본문 안내문에서 제거
- 본문에 영어 단어 금지 ("fix" → "수정", "OK" → "확인" 등)
- 종결 톤: 본문은 `~합니다/~하였습니다` 또는 명사형 통일. `~음`(메모체) 금지

---

## 2026-06-06 ~ 06-12

**제목:** [주간보고] 재고관리 시스템 개발 현황 (2026.06.06~06.12)

안녕하세요. 생산부 김현우 사원입니다.

사장님의 지시로 재고관리 시스템 개발 현황을 전사에 공유해 드립니다.
기존 수기 관리와 엑셀 의존도를 낮추고, 입출고·불량·재고 흐름을 한 화면에서 처리할 수 있도록 개발 중입니다.
아직 완성 단계는 아니지만 각 부서 업무에 도움이 될 수 있도록 지속 개선해 나가겠습니다.
현장에서 필요한 기능이 있으시거나 불편한 점이 있으시면 편하게 말씀해 주세요.

이번 주는 불량 처리 패널을 전면 재설계하고 격리 목록·탭 화면 구성을 개선하였으며,
창고 지도와 직원별 품목 순서 기능을 보강하고,
모바일 전용 화면을 개선하여 스마트폰 사용 환경을 갖추었습니다.
아울러 외부 검토를 통해 개선이 필요한 부분을 확인하고 보강 작업을 진행하였습니다.

---

■ 이번 주 진행 사항 (6/8 ~ 6/12)

**1. 불량 처리 패널 재설계 및 핵심 지표 개선 (6/9~6/10)**
- 처리 패널 부서별 그룹 접기·펼치기 도입으로 화면 밀도를 낮추고 담당자의 불량 건 식별 속도 개선
- 해제 메모·처리 분류를 선택 항목으로 전환하여 입력 오류 감소 및 처리 이력 일관성 확보
- 핵심 지표를 '격리 중'·'1년 이상 격리' 2개로 압축하고 부서 범위를 명시하여 현황 파악 속도 향상
- 정상복귀 자동화 테스트 오류 해소로 검증 신뢰도 회복

**2. 격리 목록 및 불량 탭 화면 개선 (6/9)**
- 격리 목록 기본 범위를 '전체'로 전환하고 창고 항목을 별도 그룹으로 분리하여, 담당자 누락 없이 전체 현황 확인 가능
- 출처 기본값·배지·선택 체크박스 추가로 처리 대상 식별과 일괄 선택 효율 향상
- 불량 탭 헤더 제거와 화면 전환 효과 적용으로 이동의 자연스러움 확보
- 부분 수량 복귀·부서 필터·지표 카드 분리로 역할별 정보 접근 경로를 명확히 구분

**3. 창고 지도 및 박스 편집 기능 (6/8~6/10)**
- 열 클릭·자리 분할·슬롯 번호로 공간 활용 안내를 강화하여 창고 위치 파악 시간 단축
- 박스 편집 화면 추가 및 초기 배치 데이터 구성으로 창고 지도 실사용 기반 마련

**4. 직원별 품목 순서 커스터마이징 (6/8)**
- 입출고·격리·불량 품목창에 개인별 정렬 순서를 적용하여 자주 쓰는 품목을 상단 배치, 조회 단계 감소
- 끌어서 순서 변경·초기화 기능으로 운영자 개인 맞춤 환경 지원

**5. 모바일 핵심 화면 구성 (6/10)**
- 하단 탭 4개 + '더보기' 시트 구조로 스마트폰 한 손 조작 범위 안에 주요 기능 배치
- 창고지도·주간보고·입출고(스캔 우선 동선·단품 즉시 입력 방식)를 모바일 전용 화면으로 구성하여 현장 스마트폰 실사용 환경 마련

**6. 모바일 불량 처리 카트 구성 (6/10~6/11)**
- 격리·폐기를 카트 방식으로 전환하여 다품목 일괄 처리가 가능해지고 작업 단계 축소
- 데스크톱과 동일한 처리 패널을 모바일에 구성하여 기기 무관하게 동일한 업무 처리 가능
- 불량 복귀 자동화 테스트 복구·카트 전환 흐름 검증 추가로 모바일 처리 경로 안정성 확보

**7. 모바일 입출고 및 전반 정비 (6/11)**
- 저장 슬롯 분리 버그 수정으로 데이터 손실 위험 제거, 화면 이탈 방지 기능으로 미완료 입력 보호
- 터치 영역 최소 기준 확보로 모바일 오조작 오류 감소, 인수인계·섹션 탭 가시성 확보
- 모바일 미지원 관리 화면 노출 차단으로 오류 위험 제거
- 내역 카드 터치 높이·불량 선택창·자재 명세 폭 반응형 정리로 모바일 전반 조작 신뢰도 향상

**8. 테스트 자동화 강화 (6/8~6/11)**
- 입출고·불량 처리 자동화 테스트 안정화 및 회귀 테스트 신설로, 핵심 경로 이상을 자동 감지하는 체계 마련
- 입출고 미리보기·창고지도·불량 카트 경로 단위 테스트 신설로 기능 변경 시 예상치 못한 오류 차단

**9. 코드 품질 정비 (6/8~6/10)**
- 필터 토글 중복 로직을 통합하여 동일 기능 코드 산발을 제거하고 향후 수정 영향 범위 축소
- 창고 패널·품목 상세 조회를 최신 표준 방식으로 이관해 데이터 캐시 관리 일원화, 백엔드 오류 로그 개선으로 추적 용이
- 화면 구조 모듈 명칭 통일로 코드 탐색 혼선 제거, 환경 설정 방어 처리로 배포 환경 오류 차단
- 데이터베이스 구조도 전면 현행화, 현장 피드백 문서 작성으로 추적 체계화
- 전체 자동화 검증 통과 확인

**10. 시스템 코드 검토 및 안정성 보강 (6/12)**
- 검토 요청 5개 항목을 코드와 변경 이력 기반으로 분석하여 항목별 현황 확인 및 결과 공유
- 모바일 불량 카트 전송 시 네트워크 중단 후 재시도 과정에서 중복 처리가 발생할 수 있는 구조를 확인하고, 격리·폐기 양쪽에 중복 방지 처리 및 15초 쓰기 타임아웃을 추가하여 보강 완료

프로그램은 제 자리 PC가 켜져 있을 때 http://192.168.0.63:3000 에서 확인하실 수 있습니다. (사내망 연결 환경에서만 접속 가능합니다.)

## 2026-05-30 ~ 06-05

**제목:** [주간보고] 재고관리 시스템 개발 현황 (2026.05.30~06.05)

(권동환 사원님께 1차 공유드립니다. 피드백 반영 후 전송하겠습니다.)

안녕하세요. 생산부 김현우 사원입니다.

저번 주는 기준정보 체계와 백엔드 코드 구조를 정리하고,
받은 피드백을 모아 처리하였으며, 개발·운영 환경 분리와
테스트 자동화·결재 규칙 정합으로 시스템 신뢰성을 강화하고,
창고 지도 화면과 튜브 인수인계 기능을 신설하였습니다.

---

■ 저번 주 진행 사항 (6/1 ~ 6/5)

**1. 직원 MES 사용 응대 (6/1)**
- 직원들이 MES를 직접 써보면서 생긴 의문·불편·모르겠는 점을 받아 정리
- 5/27 사내 시연 이후 누적된 후속 질문 위주로 진행
- 받은 내용은 보완 검토용으로 별도 정리

**2. 기준정보 체계 고정 — 코드 보완 (6/1)**
- DB 구조를 직접 살펴보면서 학습 (자료 흐름·테이블 관계 정리)
- 생산·분해 시 BOM 부품을 소속 공정에서 차감하도록
  백엔드 보완 (타 공정 부품 조정은 별개 흐름으로 허용)
- 기준정보 문서 정합: 빠르게 바뀌는 수치(품목 수·공정 종수 등)는 문서에
  박지 않고 기준 수치 조회 도구로 실시간 확인하도록 일원화 + 유령 칸 정리

**3. 기준정보 정합 후속 — 수량·식별자·품목코드 (6/2 오전)**
- 수량을 시스템 전반에서 정수로 통일: 입력 단계에서 소수 입력을 거부하도록
  차단하고, DB 컬럼 선언 타입과 과거 잔여 데이터까지 정수로 정합
- 품목·이동 식별자 형식이 군데군데 어긋나 있던 부분을 공통 처리로 일원화
- 품목코드(부서 + 모델 + 옵션) 생성을 단일 기준에서만 만들도록 정리하여,
  같은 코드가 다른 곳에서 다르게 만들어지는 가능성 차단

**4. 백엔드 코드 구조 정리 — 큰 파일 분리 (6/2 오전)**
- 한 파일에 모여 너무 커져 있던 입출고·재고 요청·재고 관련 백엔드 코드를
  책임별 모듈로 분리

- 외부 동작에 영향 없는 내부 정리입니다.

**5. 운영 로그·사번 감사 보강 (6/2 오전)**
- 5/29에 도입한 사번 자동 기록을, 누락되어 있던 직접 입출고(공정 간 이동 등)
  흐름에도 동일하게 적용
- 모든 품목 이동 경로에서 작업자 사번이 빠짐없이 남도록 보강

**6. 불량 전용 탭 신설 (6/2 정오)**
- 좌측 사이드바 맨 아래에 "불량" 탭을 신설하여 불량 입력·격리·처리 흐름을
  입출고 화면에서 분리
- 작업 화면 사용성은 입출고와 비슷하게 맞춰, 기존에 익숙한 조작 방식 유지

**7. 받은 피드백 정리·처리 (6/1 ~ 6/4)**
직원분들께 받은 피드백을 모아 처리한 결과입니다.

· 권동환 사원님 — 창고·부서 요청함에서 새 요청이 맨 위에 오도록 정렬 변경
  → 해결 (6/2)

· 이지훈 대리님 (3건)
  - 입출고 3단계 입고 품목 선택 영역에 자체 스크롤바 추가 → 해결 (6/2)
  - 관리자 BOM 관리에서 자식 행 삭제가 동작하지 않던 문제 → 해결 (6/2).
    진단 결과 식별자 형식 혼재로 일부 행을 찾지 못하던 문제였고, 위 3번
    식별자 일원화 작업으로 근본 원인까지 제거되었습니다.
  - 특정 BOM 데이터 교체 작업 → 진행 예정 (실데이터 입력 영역, 별도 진행)

· 허동현 사원님 (3건)
  - 입출고 내역 숫자 입력 칸에서 키 입력이 잠기는 현상 → 해결 (6/2)
  - 불량 화면 라벨 설명을 마우스를 올려야 보이던 부분을 본문에 항상 표시
    → 해결 (6/2)
  - 구매요청 후 요청자에게 알림이 가는 채널 추가 요청 → 검토 중
    (앱 내 알림함·메일 등 채널 선정 단계)

· 김건호 과장님 (5건)
  - 4단계 BOM 확인 화면에서 박스 어디를 클릭해도 펼쳐지도록 변경 → 해결 (6/4)
  - 휴지통 아이콘 크기·색상 개선(빨강·확대) → 해결 (6/4)
  - 하위의 하위 BOM 펼쳐보기(읽기 전용, 어떤 부품이 들어갔는지 확인용)
    → 해결 (6/4)
  - 결재 반려 후 요청자 화면에 반려 상태가 표시되지 않던 좀비 현상
    → 해결 (6/4, 30초 자동 새로고침)
  - 승인 요청 수신 시 승인 담당자에게 알림 → 검토 중
    (허동현 사원님 알림 건과 함께 채널 선정)

· 남재원 사원님 — 하위의 하위 BOM 펼쳐보기 → 해결 (6/4,
  김건호 과장님 동일 의견과 함께 처리)

· 이필욱 부장님 — 창고 맵 시각화 구두 요청 → 해결 (6/4,
  아래 10번 창고 지도 항목으로 별도 진행)

이 외 점검 중 식별·반영한 사항:
- 모델 관리 화면에서 신규 모델 추가가 동작하지 않던 문제 → 해결 (6/2).
  신제품(모델 9)을 정식 등록하고, 신규 모델이 화면 곳곳에서 자동 인식되도록
  함께 보강하였습니다.
- 재작업 시 회수 외 항목이 화면에서 사라지는 현상 → 점검 중
  (불량 격리 이동 흐름으로 정정 검토)
- 입출고 작업 중간 저장이 이전 내역에 덮어쓰이는 현상 → 점검 중
  (사번 감사 로그 보강과 묶어 처리 예정)

**8. 개발·운영 환경 분리 (6/2 오후)**
- 개발 환경과 운영 환경을 포트로 분리하여, 개발용 작업이 실제 운영
  데이터에 영향을 주지 않도록 정비
- 브라우저 탭 제목에 환경(개발/운영)을 표시하여 시각적으로 구분
- 운영 환경 시작 스크립트 정비

**9. 시스템 신뢰성 강화 — 자동 점검·결재 규칙·내역 일치성 (6/4 오전)**
- 화면을 사람이 직접 클릭하며 점검하던 절차 일부를 자동으로 옮겨,
  불량 처리·결재 전체 흐름·생산 잠금 등이 코드 변경 후에도 그대로
  동작하는지 매번 자동 확인되도록 마련
- 결재 규칙(어느 단계에서 누가 결재하는지)을 백엔드 한 곳에서만 정의하도록
  통합하고, 프론트와 어긋나면 코드 단계에서 자동 차단되도록 정비
- 입출고 내역을 엑셀/CSV로 내려받을 때 요청자·승인자 이름이 화면 표시와
  일치하도록 보정

**10. 창고 지도 화면 신설 (6/4 오후)**
이필욱 부장님 구두 요청 사항 대응입니다.
- 창고 안 앵글(철제 선반) 9개의 위치와 박스 배치를 한 화면에서 보는 지도뷰 도입
- 앵글 구조 편집·박스 위치 배정 기능 마련
- 데이터 모델·서버 API·화면까지 통합

**11. 코드 구조 정비 + 인수인계 기능 추가 (6/5)**
- 백엔드 거대한 `schemas.py` 파일을 작은 파일들로 나눴습니다 (유지보수성 개선)
- 입출고 필터 기능을 별도 파일로 빼냈습니다 (코드 정리)
- 프론트엔드 폴더명을 `legacy`에서 `mes`로 일괄 변경했습니다
  - 웹 주소도 `/legacy` → `/mes`로 통합
  - 더 이상 안 쓰는 옛날 코드 32개 파일을 정리했습니다
- 불량 탭을 기존 탭들과 스타일을 맞춰 일관성 있게 정리했습니다
- 김도영 주임님께서 요청하신 인수인계 기능을 새로 추가했습니다 (튜브 파트에서 고압·진공 파트로 양품을 넘길 때 작성하는 서류)

화면은 제 자리 PC가 켜져 있을 때 http://192.168.0.63:3000 에서 확인하실 수 있습니다.

---

감사합니다.
김현우 드림

---

## 2026-05-23 ~ 05-29

**제목:** [주간보고] 재고관리 시스템 개발 현황 (2026.05.23~05.29)

(권동환 사원님께 1차 공유드립니다. 피드백 반영 후 전송하겠습니다.)

안녕하세요. 생산부 김현우 사원입니다.

이번 주는 관리자 화면 4종 한꺼번에 보강, 화면 데이터 갱신 방식
정비, 불량 처리·입출고 흐름 정합, 현장 피드백 일괄 반영,
5/27 생산부 사내 시연(MES 설명 + 6월 이후 전환 계획),
그리고 발표 후속 보완을 진행하였습니다.

---

■ 이번 주 진행 사항 (5/23 ~ 5/29)

**1. 직원별 입출고 권한 토글 신설**
- 직원 개인 단위로 입출고 화면 접근을 켜고/끌 수 있는 토글을 새로 추가하였습니다.
- 꺼진 직원은 입출고 화면에 진입 불가
- DB 구조 변경·서버 API·테스트 동반

**2. 관리자 화면 4종 한꺼번에 보강 (5/23)**
- 대시보드에서 입출고 화면으로 바로 이동하는 동선 + 중복 품목 자동 합산
- 품목·모델·직원·부서 변경 시 저장하지 않고 화면을 빠져나가려 하면 "저장 안 됨"
  안내 모달 일괄 도입
- 모델 관리에서 행을 직접 편집하고, 끌어서 순서 바꾸기 기능 추가
- 관리자 사이드바의 "위험 영역" 섹션 제거
- 화면 곳곳에 흩어져 있던 버튼 모양을 공통 부품으로 통일(6곳)
- 입출고 내역 달력에 "연 단위 보기" 추가
- BOM 부서 필터에 "전체" 항목 추가, 부서 색상 입력은 hex 코드 + 30여 색
  프리셋 팔레트로 개선

**3. 화면 데이터 갱신 방식 정비 (React Query 1차 도입)**
화면이 서버에서 데이터(품목 목록·입출고 내역·재고 수량 등)를 받아오는 부분을
통일·자동화하였습니다.
- 도메인 10여 개를 React Query 기반으로 전환
  (이전: 화면마다 따로 / 이후: 한 군데서 관리 + 자동 새로고침)
- 화면 한 곳에 섞여 있던 "데이터 조회 / 입력 폼 / 명령 실행" 세 가지를
  세 부분으로 나눠 정리
- 변경 폭이 커서 일부 화면은 진입 시 한 박자 늦는 경우가 보여 추가 점검이
  필요한 상태입니다.

**4. 백엔드 큰 파일 정리 (5/23~24)**
- 한 파일에 모여 있던 데이터 모델 코드(705줄)를 종류별 폴더로 분리
- 시스템 첫 가동에 쓰는 초기화 코드(865줄)도 같은 방식으로 분리
- 로그인(PIN) 인증 코드 흐름을 한 군데로 모음
- 외부 동작에 영향 없는 내부 정리입니다.

**5. 불량 처리·입출고 흐름 정합 (5/26 ~ 5/28)**
- 불량 입력 → 격리 → 처리 단계를 입출고 흐름과 맞춰 정리
- 불량 처리 시 수량 입력 기능 추가
- 5/28에는 발표 시연 전에 발견된 자동 결재·재작업 트리뷰·요청자/승인자 표시
  버그 등 후속 보완
- 이력 표시 일부 버그 수정 포함

**6. 품목 시리얼 부여(공정 단위) + 5월 재고 검증 (5/26)**
- 품목 시리얼을 공정(원자재·중간공정·공정완료) 단위로 부여하도록 재정비
- 권동환 사원님이 정리한 5월 재고 확정 파일을 김건호 과장님 검토를 거쳐 반영,
  시스템 DB 코드와 엑셀 정합 확보

**7. 품목 관리 — 소프트 삭제 보강 (5/26)**
- 품목을 삭제하면 즉시 사라지지 않고, 비활성 상태로 목록 아래로 내려갑니다.
- 실수로 삭제했을 때 다시 활성화하여 복구할 수 있습니다.

**8. 생산 가능 화면 재설계 — 모델별 대표 제품 5종 (5/26)**
- 기존에는 합계 수량만 표시해서 의미 파악이 어려웠는데, 모델별 대표 완제품
  5종을 보여주는 방식으로 변경
- 아직 마무리 작업이 남아 있으며, 후속 보완 예정입니다.

**9. 사용성 개선 (5/27 오전)**
- 안내 메시지·에러 표시 표현 일관화, PIN 변경 성공 안내를 결과 모달로 통일
- 모달을 끌어 닫거나 바깥 클릭으로 의도치 않게 닫히던 동작 차단
- 입출고 내역 상세 화면에 이미지 표시 + 클릭 시 큰 이미지 보기
- 직원 선택 가나다 순 정렬
- 입출고 작업 중에 다른 화면으로 이동 시 저장 확인 모달
- 달력에서 다른 날짜 거래를 누르면 그 날로 필터가 자동 이동되고 스크롤
- 대시보드와 입출고 내역의 필터 모양 통일
- 저장 안 하고 화면 이동 시 자동 저장된 임시 데이터도 함께 폐기
- 자동 카트(장바구니) 가드 키 확장 (보고된 8-2번 버그 수정)
- 입출고 5단계 화면에 [저장하기] 보조 버튼 추가
- 품목 추가 시 같은 (이름, 카테고리) 조합이 이미 있는지 미리 확인
- 모바일: 입력 시 화면 자동 확대(iOS)되는 현상 방지, 본문 글자 크기 키움
- 부서 색상이 DB와 화면에서 어긋날 때 기본값 처리 정합

**10. 생산부 대상 사내 시연 + 6월 전환 계획 공유 (5/27 10:30 ~ 11:00, 30분)**
생산부 직원을 대상으로 MES 시스템을 처음으로 시연하고, 6월 운영 전환 계획을
공유하였습니다.
- 시연: PIN 로그인부터 모바일까지 화면 흐름 18개
- 운영 정책 변경 안내 (핵심):
  · 창고 입출고는 정·부 2단 승인 / 부서에서 BOM 등록된 품목 입출고는 즉시
  · 부서 수량 개별 조정은 승인 필요 / 불량 등록은 승인 없이 바로
  · 재고를 원자재 → 중간공정 → 공정완료 3단계로 구분
- 6월 이후 전환 계획 (일정 조정):
  · 권동환 사원님 출산휴가로 코드 품질 개선 리뷰 일정이 지연되어 조정
  · 정식 도입: 6월 말 또는 7월 초 예정
  · 전환 방식: 자리에서 직접 사용하며 메모 → 취합 담당자가 메일로 전달
  · 2 ~ 3주는 기존 엑셀 방식과 병행 후, 안정화 판단 시 엑셀 폐기
- 모바일 화면은 방향성 보여주는 미완성 상태임을 사전 공지하였습니다.

**11. 기타 UI 다듬기**
- 주간보고 화면: 달력 주 단위 클릭, 카드 컴팩트, 생산표 열 고정, 카드들이
  같은 가로 위치에 정렬되도록 보정
- 불량 처리 5종 항목의 알약 라벨·색을 빨강 + 아이콘으로 통일
- 입출고 내역의 BOM 가독성 개선, 우측 패널이 열려 있을 때 컴팩트 모드
- 로그인 콤보박스 꺽쇄 클릭 가능, 상단 바 로그아웃 여백 정리
- 사이드바·테마 토글 비활성 아이콘 배경 정리
- 내부 도큐: 커밋 메시지 형식 + 날짜는 운영체제 시계로 확인하는 규칙을
  `CLAUDE.md`에 명시
- 입출고 내역 재작업 우측 패널 제목을 헤더와 동일하게 부모 품목명으로 통일
  (허동현 사원님 피드백)
- 불량 재작업 BOM 자식 펼침 버튼 가시성 보강 (크기·테두리·색 — 허동현 사원님 피드백)
- 재고 용량 패널에 "자세히 보기" 안내 라벨 추가

**12. 주간보고 매트릭스 보강 + 백엔드 동결 (5/29 오전)**
- 매트릭스 집계 거래타입을 생산 단일에서 5종(생산·입고·창고 이동·부서 이동·출하)
  으로 확장. 실 운영에 생산 거래가 거의 없어 매트릭스가 비어 보이던 문제 해결
- 거래타입 11종 전부 분류 + 신규 거래타입 추가 시 분류 누락을 자동 검출하는
  회귀 테스트 추가
- 주간보고 백엔드도 동결 마킹 (프론트 동결과 페어링, 동결일 2026-05-29)

**13. 운영 로그 시스템 + 사번 기반 추적 (5/29 오전)**
권동환 사원님께서 말씀하신 로그 개선입니다.
- 로그인 시 작업자 사번이 모든 요청에 자동 부착되도록 미들웨어 추가
- 입출고·재고 요청·불량 처리·BOM·품목 변경·부서 조정 등 도메인 이벤트
  약 15군데에 사번 자동 기록
- 관리자 감사 로그에 작업자 사번 컬럼 추가 (관리자 작업도 누가 했는지 추적)
- 모든 품목 이동·관리 작업에 작업자 사번이 빠짐없이 남도록 인프라 정비

**14. 부품 코드 자동 갱신 + DB 정합성 정리 (5/29 정오)**
- 품목 마스터에서 모델·공정·옵션 변경 시 부품 코드가 자동으로 재계산되도록
  변경. 저장 후 우측 카드 즉시 정상 표시
- 부품·모델 정보가 별도 테이블에 흩어져 있던 구조 폐기 → 부품 코드 자체에서
  모델 정보를 읽도록 단순화
- 일회성 정합 스크립트로 어긋난 부품 코드 34건 → 0건
- 김민재 대리님 SOLO 필터 + 사내 부품 코드 prefix 자동 갱신 신고 해결

**15. 다중 사용자 피드백 처리 — PIN·수량·담당자 (5/29 정오)**
- PIN 4자리 고정 (로그인·관리자 PIN 변경 모두) — 계정 잠금 위험 해소
  (허동현 사원님 피드백)
- 수량 소수점 입력 지원 (불량 입력·BOM 자재량 — 허동현 사원님 + 김건호 과장님
  피드백)
- 담당자 입력 자유 텍스트 → 직원 마스터 자동완성 드롭다운
  (허동현 사원님 피드백)

화면은 제 자리 PC가 켜져 있을 때 http://192.168.0.63:3000 에서 확인하실 수 있습니다.

---

감사합니다.
김현우 드림
