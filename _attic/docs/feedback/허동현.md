# 허동현
받은 피드백: 26건 (해결 23 / 미해결 0 / 보류 1 / 종결 2)

---

## 2026-05-28
### 입출고 내역 — 재작업 행 우측 패널 자식 부품명 표시
#입출고내역 #재작업 #불편 #해결

재작업 행을 클릭 했을 때 오른쪽에 열리는 정보가 '튜브 고정 보드 에노드 (나사) [ADX6000]'로 이상하게 표시됨.

→ 해결 (2026-05-28)
- 원인: 헤더 행은 `DISASSEMBLE` 부모 로그를 기준으로 표기하는데, 우측 패널은 `bundles[0].title`을 그대로 써서 자식 부품 번들이 0번에 올 경우 자식명이 표시됨.
- 수정: `frontend/app/legacy/_components/_history_sections/DesktopHistoryRightPanel.tsx` — 재작업 batch는 `DISASSEMBLE` 로그를 찾아 "{부모 품목명} 재작업"으로 통일.

---

### 입출고(불량) — 재작업 BOM 펼침 버튼 안 보임
#입출고불량 #재작업 #BOM #불편 #해결

재작업 시에 하위 품목을 펼치게 하는 버튼이 눈에 잘 보이지 않음.

→ 해결 (2026-05-28)
- 원인: 접힌 상태 chevron 색이 회색(`muted2`) + 24px 크기 + 투명 배경 → 시인성 낮음.
- 수정: `frontend/app/legacy/_components/_defect_hub/DisassembleTree.tsx` — 28px + blue 14% 옅은 배경 + blue 30% 테두리 + chevron 색 항상 blue.

---

### 주간보고 — 재작업이 출고로 안 잡힘
#주간보고 #재작업 #기타 #종결

재작업 시에 재고가 줄어드는건데, 주간보고 화면에서 출고로 적용되지 않는데, 이게 맞는지 의문.

→ 종결 (2026-06-23, 정책 결정)
- 재작업(DEFECT_SCRAP·DISASSEMBLE)은 주간보고 출고 집계 대상이 아님 — 출하·생산과 성격이 다른 별개 사안으로 구분.
- 주간보고 현행 정책 유지, 개발 변경 없음.

---

## 2026-05-29
### 입출고 내역 — 제품별 사진 촬영 정책 문의
#입출고내역 #사진 #정책 #기타 #해결

제품별로 사진 찍어야 하는지?

→ 해결 (2026-05-29) — 사용자 결정: 필요 없음.

---

### 입출고 내역 — 담당자 이름 오타/미등록 시 수정 불가
#입출고내역 #담당자 #검증 #기타 #해결

입출고 내역에서 담당자 이름 적을때 없는 사람이나 이름 오타인 경우 수정이 안되게 표시됨.

→ 해결 (2026-05-29) — 사용자 결정: 명단에서 고르기 (자동완성).
- 수정: `TransactionEditUnifiedModal.tsx` — `produced_by` 자유 텍스트를 `ProducedByCombobox` 자동완성으로 교체. 직원 마스터에서 검색 가능, 마스터에 없는 사람 검색 시 "관리자에게 등록 요청" 안내.
- 기존 자유 텍스트 값 호환: 모달 열렸을 때 그대로 표시, 마스터에 없으면 안내 메시지.

---

### 입출고(불량) — 소수점 입력이 반올림됨
#입출고불량 #소수점 #반올림 #기타 #해결

소수점으로 적으면 반올림으로 적히는 현상 발생.

→ 해결 (2026-05-29) — 김건호 BOM 소수점 건과 같이 묶어 처리.
- 수정: `frontend/lib/mes-format.ts` — `formatQty(value, { maximumFractionDigits, trimTrailingZeros })` 옵션 시그니처 확장. 기본값(0) 유지로 재고 일반 표시 회귀 없음.
- 불량 입력 폼들(`DefectActionStep.tsx`, `DisassembleTree.tsx`, `AddQuarantineModal.tsx`): 표시 시 `{ maximumFractionDigits: 2, trimTrailingZeros: true }` 적용. `Number()` → `parseFloat()` 로 교체해 소수점 보존.

---

### 로그인 — PIN 변경 자리수 일관성 깨짐 (4자리 이상 변경 후 로그인 불가)
#로그인 #PIN #버그 #기타 #해결

핀번호 비밀변경 4자리 제한없어서 4자리 이상 변경하면 변경은 되는데 다시 로그인시 4자리밖에 적을 수없어서 로그인 안됨.

→ 해결 (2026-05-29) — 사용자 결정: 4자리 고정.
- 백엔드 진실 소스: `backend/app/services/pin_auth.py` 에 `validate_pin()` 추가 (정확히 4자리 숫자 아니면 HTTP 422). `change_employee_pin` 진입점에서 호출. `schemas.py` 의 `EmployeePinChangeRequest.new_pin` 도 `min_length=4, max_length=4`.
- 프론트 일관성: `frontend/lib/auth/constants.ts` 신규 `PIN_LENGTH=4`. 로그인·PIN 변경·관리자 PIN 초기화 폼 모두 이 상수 import. `DesktopTopbar`, `AdminEmployeesSection` 의 maxLength·숫자 필터·제출 비활성화 통일.
- **잠긴 사용자 응급 처리**: 관리자가 `reset_employee_pin` 으로 0000 초기화 → 사용자 로그인 → 4자리 PIN 새로 설정.

---

## 2026-06-01
### 입출고 내역 — 숫자 입력 칸에서 키보드로 못 적음 (0부터 시작될 때)
#입출고내역 #숫자입력 #버그 #불편 #해결

키보드로 숫자를 적으려고 할 경우 0부터 시작될 때 숫자가 안 적히는 버그. 클릭으로만 증감 가능, 키가 잠겨버려서 지우고 쓰는 것도 안 됨.

→ 해결 (2026-06-02)
- `frontend/app/legacy/_components/_history_sections/TransactionEditUnifiedModal.tsx` L242 — `type="number"` + `min="0"` + `step="any"` → `type="text"` + `inputMode="numeric"` 로 전환. 브라우저 number 유효성 검사 우회. onChange 에서 빈 문자열·양수 소수점 패턴만 허용, 음수 차단. submit 시 `Number(qty)` 변환 로직 그대로 유지.

---

### 구매요청 — 승인 요청 후 알림 (요청자에게)
#구매요청 #알림 #추가요청 #해결

구매요청하고 승인 요청한 뒤 "요청이 되었다"는 알람이 왔으면 좋겠다는 의견. (고압 이모님 의견. 아마도 메일이나 chat으로 됐다고 보낼 거 같은데 개선 필요.)

→ 해결 (2026-06-04, F2·F3 — 커밋 `494fb287`)
- 채널 결정: **앱 내 알림 인박스** 로 구현. 상단 헤더 종 아이콘 + 안읽음 배지 + 드롭다운 패널 + 30초 폴링, 클릭 시 해당 결재 화면 딥링크.
- 백엔드: `Notification` 모델/테이블 + `/api/notifications` 라우터. `notify_request_arrived`/`decided` — 요청 트랜잭션과 원자적 커밋.
- 요청자 알림: 승인/반려가 **실제 처리 완료(COMPLETED 전이)** 될 때 요청자에게 알림. (이 건 = 요청자 방향, 김건호 건 = 승인담당자 방향 — 한 시스템으로 함께 구현.)
- 프론트: `NotificationBell`/`NotificationPanel` + `useNotificationsQuery`(30s 폴링), 데스크탑·모바일 모두 종 노출.

---

### 입출고(불량) — 라벨 설명을 hover 말고 라벨에 직접 표시
#입출고불량 #UI #모바일일관성 #불편 #해결

불량 화면에서 라벨 위에 마우스를 올려야 설명이 뜨는 것보다 모바일 버전처럼 라벨에 같이 써져 있었으면 좋겠음.

→ 해결 (2026-06-02)
- `frontend/app/legacy/_components/_warehouse_v2/IoLineRow.tsx` — 체크박스 버튼의 `title={...}` 속성 제거 + 품목명 셀 하단(mes_code 옆)에 설명 텍스트 span inline 노출. hover 없이 항상 표시 (qtyLocked·included·exclusion_note 분기 유지).

---

## 2026-06-08

> 불량처리 관련 건은 김현우 사원이 직접 작업하며 확인 예정 (구두 회신).

### 불량 추가 — 창고 재고·부서 재고가 장바구니에 공용으로 묶임
#입출고불량 #불량격리 #바로폐기 #장바구니 #창고재고 #부서재고 #버그 #불편 #해결

새 불량 추가 시 같은 제품의 **창고 재고**와 **부서 재고**가 장바구니에 공용(한 묶음)으로 들어가, 출처별로 따로 수량을 지정할 수 없음.
- 예 1: 필터 2t 를 창고재고에서 1개, 부서재고에서 2개 — 따로 빼는 게 안 됨.
- 예 2: 창고재고에서 필터 1t 1개를 빼고 → 부서재고를 눌러 0.5t 를 누르면 부서재고에서 한 번에 빠지는 것처럼 동작 (수량이 합쳐지거나 출처가 섞임).

→ 해결 (2026-06-10)
- 공용 장바구니 방식을 폐기하고, 새 불량 추가를 **창고 재고 / 부서 재고 출처 라디오 단일 선택** 방식으로 재설계(`AddQuarantineModal`). `source` + `source_dept`/`target_dept` 를 출처별로 전송해 출처가 섞일 구조 자체를 제거.

---

### 불량처리 — '처리' 버튼이 정상복귀와 기능 중복
#불량처리 #처리버튼 #정상복귀 #중복 #불편 #해결

건수 선택 시 정상복귀/폐기/반품 3개 기능이 있는데, 부품 맨 오른쪽 '처리' 버튼을 누르면 정상복귀로 동작 — 기능이 겹침.

→ 해결 (2026-06-10)
- 행 우측 '처리' 버튼이 곧바로 정상복귀로 동작하던 구조를 폐기. '처리' → 정상복귀/재작업/전체폐기/반품을 **라디오로 고르는 처리 화면**(`DefectProcessPanel`)의 진입점으로 통합 → 정상복귀와의 기능 중복 해소.

---

### 불량처리 — 총합 갯수 표시 무의미 + 필터/클릭 조회 요청
#불량처리 #총합갯수 #필터 #표시개선 #추가요청 #해결

불량 5건·총 9개인 경우, 불량제품 5건 각각의 갯수는 의미 있으나 **총합 갯수**는 의미 없는 정보 아닌지. 불량마다 갯수 차이가 천차만별이라 필터가 있으면 더 잘 활용할 듯. 아니면 클릭으로 총 갯수를 보는 방식도.

→ 해결 (2026-06-10)
- 불량 허브 KPI 정리: 의미 낮던 '결재 대기'·'오늘 처리' 카드 제거, '격리 중'을 **"N건" 단위**로 명확화 + 부서 범위(내 부서/전체) 연동 + '1년 이상' 클릭 필터 제공. 무의미한 총합 갯수 혼란 제거.

---

### 창고지도 — 앵글 위치를 숫자(123456789) 대신 제품별 명칭으로 + 도움말
#창고지도 #앵글 #명칭 #도움말 #제품코드 #불편 #해결

앵글 위치가 1·2·3…9 숫자보다 **제품별로 나눈 구분**으로 기억하는데, 그 이름을 적어주면 편할 듯.
명칭으로 나눌 거면 **도움말**이 있으면 좋겠음 — 제품코드만 보고 파악하기는 처음에 너무 어렵고(나중에도 어려울 듯).

→ 해결 (2026-06-10) — 기능 구현 완료, 명칭/배치 입력은 담당자 몫
- 박스가 놓인 자리는 제품명·`mes_code` 를 노출하고, 호버 시 자리 내 전체 품목·수량을 툴팁으로 표시(`JariColumn`).
- 실제 자리별 제품 구분/명칭 배치는 담당자가 입력.
- 관련: 창고 맵 기능(이필욱 요청으로 구축, 2026-06-04).

---

### 불량 — 새 불량 추가 시 격리중/오늘처리 표시 동작 설명 필요
#불량 #격리중 #오늘처리 #격리필터 #도움말 #UX #기타 #해결

새 불량 추가 시 '격리중'과 '오늘처리'가 같이 나오는 동작·표시가 헷갈림. 바뀐 점에 대한 설명이 필요.
- 새 불량 추가하면 '격리중'과 '오늘처리'가 같이 나오는데 정상인지?
- 격리중인 걸 처리해버리면 '오늘처리' 내역도 안 보이는데 원래 초기화되는 건지?
- '오늘처리'를 하려는 게 아니라 내역이면, 오늘 처리한 내역(오늘내역)을 뜻하는지?
- 빨간색으로 '격리필터 활성' 표시는 무슨 의미인지?

→ 해결 (2026-06-10)
- 혼란 요소 자체를 제거: '오늘 처리' KPI 카드 삭제, '격리 중' 카드 클릭/빨간 '격리필터 활성' 박스 삭제.
- '격리 중' 카드에 **"OO부서 기준 · N건"** 부제를 붙이고 부서 필터(내 부서/전체)에 숫자를 연동해 의미·집계 범위를 명확화.

---

### 창고내역 — 박스/선반 바코드로 제품 개수 확인 가능 여부
#창고내역 #바코드 #재고확인 #추가요청 #질문 #보류

박스나 선반 어디든 바코드를 붙여, 스캔하면 제품 개수가 몇 개 있는지 확인 가능한지 문의/요청.

→ 보류 (실사용 가능성 낮아 보류 — 사용자 판단)
- 화면: 창고내역.
- 기대: 박스·선반에 바코드 부착 → 스캔 시 해당 위치 품목·수량 조회.
- 현황: 바코드 스캔 인프라 + 자리별 재고 조회는 있으나, 박스 바코드 → 위치 역검색(컬럼·API·전용 화면)은 미구현.
- 점검 후보: 위치(앵글/선반) 식별자 ↔ 재고 매핑 + 바코드 스캔 입력 경로. 창고지도(앵글 위치 데이터)와 연계 가능.

---

## 2026-06-22
### 입출고 내역 — 목록 정렬을 승인 순이 아니라 요청일시 순으로
#입출고내역 #정렬 #요청일시 #승인 #불편 #해결

(김현우·허동현 공동 피드백) 창고 입출고 등 관리자 승인이 필요한 건은, 화면의 '요청일시'가 요청자가 요청을 작성한 시각인데, 입출고 내역 목록의 나열 순서는 승인 순서로 보임. 승인 순서와 상관없이 요청일시 순으로 정렬되게 해달라.

→ 해결 (2026-06-22)
- 원인: 목록 정렬이 `TransactionLog.created_at` 기준(`backend/app/routers/inventory/transactions.py:251`). 승인 필요 건은 승인 시점에 거래로그가 생성되어 created_at = 승인 시각 → 목록이 '승인 순'. 화면의 '요청일시'는 `IoBatch.submitted_at`(없으면 created_at)이라 서로 어긋남(`_tx_filters.py:347`).
- 수정: `list_transactions` 정렬을 `COALESCE(IoBatch.submitted_at, IoBatch.created_at, log.created_at)` 내림차순으로 변경(화면 '요청일시'와 동일 기준). 같은 요청·동시각은 created_at·log_id 로 안정 정렬. 데스크톱은 서버 정렬 그대로, 모바일은 날짜 그룹 내에서 요청일시 순.
- 비고: CSV/XLSX export 정렬은 기록 시각(created_at) 유지.

---

## 2026-07-07
### 부서입출고 — 모바일 출고 처리 후 화면 전환 안 됨
#부서입출고 #모바일 #출고 #검색 #화면전환 #버그 #불편 #해결

폰으로 재고 처리 중 `CTR` 검색 후 부서 내 출고를 했을 때 화면이 넘어가지 않는 버그 제보.

다른 품목 검색 후에도 비슷하게 발생하는 것으로 보임.

재현 맥락:
- 화면: 부서입출고 모바일 화면
- 작업: 출고 품목 선택 단계에서 `CTR` 검색
- 현상: 부서 내 출고 처리 후 다음 화면으로 넘어가지 않음

→ 해결 (2026-07-08): 단품 출고 품목 추가 후 검색어를 비우고 결과 목록을 접어 담은 품목/수량/저장/제출 액션이 즉시 보이도록 수정. 검색창 포커스도 다시 돌려 다음 품목을 바로 검색할 수 있게 함.

---

## 2026-07-08
### 부서입출고 — 품목 추가 후 다음 품목 검색 흐름 불편
#부서입출고 #모바일 #품목검색 #연속추가 #UX #불편 #해결

부서 입출고에서 품목을 담은 뒤 추가로 다른 품목을 넣으려 할 때, 다시 품목명을 직접 입력해야 해서 불편하다는 피드백.

확인한 문제점:
- 브라우저 확인(2026-07-08): 모바일 폭 393x852에서 `입출고 → 부서 입출고 → 조립 → 출고 → 검색(solo) → 낱개 추가`까지 진행. 품목 추가 후 검색창 값이 `solo`로 그대로 남고, 포커스가 검색창이 아니라 본문으로 빠지는 것을 확인.
- 품목 선택 검색값은 `MobileIoComposeWizard`가 상태로 들고 있고, `IoTargetPicker`는 `search`/`onSearchChange`를 받아 표시만 한다.
- 품목 추가 성공 후 검색어를 비우거나 검색창에 다시 포커스를 주는 후처리가 없어, 다음 품목을 계속 담는 흐름이 끊긴다.
- 관련 위치: `frontend/app/mes/_components/mobile/warehouse/MobileIoComposeWizard.tsx`, `frontend/app/mes/_components/_warehouse_v2/IoTargetPicker.tsx`

수정 방향:
- `addItem` 성공 후 검색어를 초기화하고, 품목 목록 스크롤을 상단으로 복귀시킨다.
- 모바일에서는 검색창을 다시 포커스하거나, "계속 추가" 흐름이 자연스럽게 보이도록 검색 입력을 상단 sticky 영역에 유지한다.
- 회귀 테스트: 검색어 입력 → 품목 추가 → 검색어 초기화/목록 재표시 → 다른 품목을 바로 검색·추가할 수 있는지 확인.

→ 해결 (2026-07-08): 단품 inline 폼에서 품목 추가 성공 후 검색어 초기화, 결과 목록 스크롤 상단 복귀, 검색창 refocus를 적용. 결과 목록이 장바구니를 계속 밀지 않도록 추가 후 접히는 흐름으로 정리.

---

### 불량 바로 폐기 — 검색 후 목록 스크롤 안 움직임
#불량 #바로폐기 #모바일 #품목검색 #스크롤 #버그 #해결

불량 바로 폐기 화면에서 이름을 검색한 뒤 결과 목록 스크롤이 움직이지 않는다는 피드백.

확인한 문제점:
- 브라우저 확인(2026-07-08): 모바일 폭 393x852에서 `불량 → 바로 처리 → 폐기 → 다음 → 검색(solo)`까지 진행. 브라우저 휠/스크롤 이벤트 기준으로 내부 결과 목록의 `scrollTop`이 0에서 650으로 바뀌어, 개발 브라우저에서는 스크롤 자체는 동작함.
- 다만 스크롤 소유자가 화면 전체가 아니라 내부 결과 목록 div 하나(`min-h-0 flex-1 overflow-y-auto`)이고, 모바일 불량 화면/바로 폐기 흐름도 여러 flex/overflow 컨테이너를 중첩해서 사용한다. 실제 폰 터치에서는 사용자가 스크롤해야 하는 영역이 애매하거나 바깥 컨테이너가 제스처를 가져가 "안 움직임"처럼 느껴질 수 있다.
- 관련 위치: `frontend/app/mes/_components/mobile/screens/MobileDefectScreen.tsx`, `frontend/app/mes/_components/_defect_hub/DefectCartFlow.tsx`, `frontend/app/mes/_components/_defect_hub/DefectItemPicker.tsx`

수정 방향:
- 바로 폐기 step2에서는 화면 전체를 `h-full/min-h-0/overflow-hidden` 단일 flex 레이아웃으로 고정하고, 검색 결과 표만 스크롤 소유자가 되게 정리한다.
- 모바일 터치 스크롤용 `touch-pan-y`/`overscroll-contain`/`-webkit-overflow-scrolling: touch` 적용 여부 확인.
- 검색어 변경 시 결과 표 `scrollTop`을 0으로 초기화해 검색 직후 첫 결과부터 안정적으로 보이게 한다.
- 회귀 테스트: 모바일 폭에서 바로 폐기 진입 → 검색어 입력 → 결과 목록을 실제 터치/휠로 스크롤할 수 있는지 확인. 브라우저 자동화에서는 `scrollTop` 변화도 함께 검증.

→ 해결 (2026-07-08): 모바일 바로 폐기 Step 2를 고정 높이 flex 레이아웃으로 정리하고, 검색 결과 표를 `touch-pan-y`/`overscroll-contain`/`-webkit-overflow-scrolling: touch`가 적용된 명확한 스크롤 소유자로 분리. 검색어 변경 시 표 스크롤도 0으로 초기화.

---

### 창고·부서입출고 — 작성 중 저장 및 수정 흐름 동작 안 됨
#창고입출고 #부서입출고 #작성중 #임시저장 #수정 #모바일 #버그 #해결

창고 입출고와 부서 입출고에서 작성 중 저장 기능 및 수정이 안 된다는 피드백.

확인한 문제점:
- 브라우저 확인(2026-07-08): `부서 입출고 → 조립 → 출고 → 검색(solo) → 낱개 추가` 후 화면에는 `단품 출고 제출 1건` 버튼만 보이고, 저장 버튼은 보이지 않음. 제출은 DB 변동이 생기므로 누르지 않음.
- 모바일 작성 화면에는 step4/step5에 저장 버튼이 연결되어 있으나, 단품 출고처럼 inline 처리되는 흐름에서는 사용자가 기대하는 "작성 중 저장" 버튼이 노출되지 않는다.
- `작성 중` 탭은 IO draft(`IoBatch`)를 이어서 작업할 수 있지만, `내 요청` 수정 패널은 기존 `StockRequest` 중심으로 구성되어 있어 새 IO 제출건의 수정 전환 흐름이 충분히 노출되지 않을 가능성이 있다.
- 관련 위치: `frontend/app/mes/_components/mobile/warehouse/MobileIoComposeWizard.tsx`, `frontend/app/mes/_components/_warehouse_sections/DraftCartPanel.tsx`, `frontend/app/mes/_components/_warehouse_sections/MyRequestsPanel.tsx`, `frontend/app/mes/_components/_warehouse_sections/WarehouseDraftPanelTabs.tsx`

수정 방향:
- step4/step5 저장 버튼 클릭 시 실제 draft가 생성/갱신되고 `작성 중` 탭 카운트와 목록이 즉시 갱신되는지 확인한다.
- 저장 완료 후 사용자가 바로 확인할 수 있도록 "작성 중으로 이동" 또는 저장 완료 토스트/상태 문구를 강화한다.
- IO 제출건도 `내 요청`에서 수정 가능한 상태라면 수정 버튼과 draft 복원 경로를 명확히 연결한다. 불가능한 상태라면 수정 불가 사유를 표시한다.
- 회귀 테스트: 창고 입출고/부서 입출고 각각에서 저장 → 작성 중 목록 노출 → 이어서 작업 복원 → 제출 후 수정 전환 가능 여부를 확인.

→ 해결 (2026-07-08): 모바일 단품 입고/출고 inline 폼에도 `작성 중 저장` 버튼을 노출하고 기존 draft 저장 함수를 연결. 저장 draft 복원 시 단품 폼 Step 3으로 돌아오도록 복원 위치를 조정.

---

## 2026-09-04 (기록일 — 정확한 접수일 미확인)

### 주간보고·대시보드 — 완제품 생산 가능수량과 대표 모델 확인
#주간보고 #대시보드 #생산가능수량 #추가요청 #해결

주간보고에서 품명을 누르면 해당 완제품을 총 몇 개 만들 수 있는지와,
이 완제품을 만드는 대표 아세이의 수량을 확인할 수 있으면 좋겠음.
예전에 비슷한 기능이 있었던 것으로 기억하지만 대시보드에서는 출하 수량만 보였음.

→ 해결
- 대시보드 생산 가능수량 기능에서 모델별 대표 완제품과 출하 대기·빠른 생산·총생산 수량을 확인할 수 있도록 구현 완료.

---

### 불량 내역 — 대시보드와 같은 모델·공정 검색 필터 적용
#불량 #불량내역 #검색 #필터 #추가요청 #해결

대시보드에서 사용하는 모델 구분과 공정 구분 검색 필터를
불량 내역에서도 사용할 수 있으면 좋겠음.

→ 해결 (2026-09-04)
- 불량 목록에 대시보드형 부서·모델·공정 다중 필터와 별도 검색창을 적용함.
- 같은 분류 안에서는 OR, 분류 사이에서는 AND로 조합하며 품명·품목 코드·부서·불량 사유·처리자를 검색함.
- 기존 필터 고정 설정을 v2로 이전하고 데스크톱·모바일의 기본 부서 범위를 일치시킴.
- 관련 프런트 필터·검색·설정 이전 테스트로 동작을 검증함.
- 추가 수정 (2026-09-07): 필터 고정 상태에서 부서 범위와 선택값을 연속 변경할 때 이전 상태가 저장값을 덮어쓰던 문제를 수정. 브라우저에서 부서 변경→새로고침 후 동일 부서 복원과 초기화 후 검색어 유지를 확인함.

---

### 불량 내역 — 동일 부품 건 통합 처리
#불량 #불량내역 #통합처리 #추가요청 #해결

불량 내역에서 같은 부품에 해당하는 여러 불량 건을
한 번에 처리할 수 있는 버튼이 있으면 좋겠음.

→ 해결 (2026-09-04)
- 동일 부서·동일 품목 그룹에서 `여러 건 선택`으로 처리할 기록만 체크하고 `선택 처리 N건`으로 전달하도록 구현함.
- 정상 복귀는 하나의 원자적 요청으로 처리하며, 폐기·반품·재작업은 선택 기록별 라인을 가진 하나의 요청으로 즉시 실행함. 최신 main의 일반 직원 즉시 처리 정책을 적용하며 새 승인 대기를 만들지 않음.
- 선택 후 수량이나 기존 처리 대기 상태가 달라지면 서버에서 전체 요청을 거부하고 선택을 해제한 뒤 목록을 갱신함. 다건 재작업은 BOM 처리 1회와 기록별 차감·원장을 하나의 작업으로 연결함.
- 다건 정상 복귀·재작업·수량 변경·중복 및 혼합 품목 거부 테스트로 정합성을 검증함.
- 브라우저 재검증 (2026-09-07): 데스크톱에서 동일 품목 기록 중 선택한 2건의 합산 수량만 처리 패널·BOM에 전달되는 것, 그룹 접기 시 선택 해제, 모바일에서 선택한 1건만 전달되는 것을 확인함. 재고 변경 최종 제출은 별도 테스트 화면 서버 실행이 보안 정책에 차단되어 브라우저 검증 미완료이며, 관련 프런트·백엔드 자동화 테스트로만 검증함.

---

### 재작업·불량 격리 — 구성품 처리 단위를 작업자가 조정
#재작업 #불량격리 #구성품 #추가요청 #종결

재작업에서 일부 부품만 바로 처리하고 나머지를 불량 격리할 때,
격리된 구성품들을 작업자가 원하는 하나의 제품 단위처럼 관리할 수 있으면 좋겠음.

예: 불량 격리된 고압보드에서 트랜스포머만 교체하려 해도
순간적으로 모든 구성품이 분해된 낱개 상태가 됨.

→ 종결 (정책 결정)
- 고압보드·트랜스포머 사례를 검토함.
- 임의로 다시 하나의 제품처럼 묶는 기능은 추가하지 않고, 불량 격리된 구성품은 전부 낱개로 관리하는 현행 방향을 유지함.

---

### 불량 탭 — 주간·월간·연간 불량 통계 페이지 추가
#불량 #불량통계 #주간통계 #월간통계 #연간통계 #추가요청 #해결

불량 데이터를 집계하여 주간·월간·연간 단위로 확인할 수 있는
통계 페이지가 불량 탭 안에 있으면 좋겠음.
구두로 전달받은 요청.

→ 해결 (2026-09-04)
- 불량 작업 선택 화면에 `불량 통계` 카드를 추가하고 주간·월간·연간 전체 폭 통계 화면을 구현함.
- KST 달력 경계에 맞춘 건수·수량·기간 추이·품목·사유·부서 집계를 제공하며 빈 기간도 0으로 표시함.
- 최초 격리와 격리 없이 바로 처리한 발생만 집계하고, 후속 처리 중복과 신뢰할 수 없는 레거시 합산 기록을 분리함.
- KST 기간 경계·윤년·필터·중복 제거·빈 구간 및 프런트 기간 이동·오류·빈 결과 테스트로 검증함.
- 추가 수정 (2026-09-07): 통계의 높이 제한·flex 축소 때문에 긴 품목 순위가 하단 집계와 겹치는 문제를 수정. 차트·품목 순위 높이를 맞추고 순위 내부만 스크롤하며, 통계 전체는 자연스럽게 세로 스크롤하도록 정리함. 모바일 기간 이동 버튼을 별도 행으로 묶고 좁은 데스크톱의 필터 배치를 조정함.
- 추가 수정 (2026-09-07): 모바일 목록의 `작업 선택`이 화면만 바꾸고 history를 남겨 통계 뒤로가기가 목록으로 돌아가던 오류를 수정. 목록→작업 선택→통계→작업 선택이 실제 허브로 복귀하는 것을 브라우저에서 확인함.
- 후속 배치 요청 반영 (2026-09-07): 데스크톱 왼쪽에 `기간별 불량 추이 → 사유별 집계 → 부서별 집계`를 기존 추이 카드와 같은 폭·높이로 세로 정렬하고, 오른쪽 `품목별 순위`는 왼쪽 세 카드 전체 높이를 사용하도록 변경함. 집계 목록은 카드 내부에서 스크롤하며 좁은 화면은 같은 읽기 순서의 단일 열로 표시함. 1916·1280px 데스크톱과 390·320px 모바일에서 크기·정렬·빈 결과·스크롤을 확인했고 통계 컴포넌트 테스트 8개와 대상 ESLint가 통과함.
- 스크롤 요령 반영 (2026-09-07): `_attic/docs/EXTERNAL_SCROLLBAR_RAIL_GUIDE.md` 기준으로 사유·부서·품목 목록의 실제 스크롤 영역을 분리함. 카드 크기·배치·고정 제목·둥근 테두리는 유지하고, 데스크톱 스크롤바만 카드 오른쪽 10px 바깥 레일로 이동하며 gutter를 예약함. 모바일은 기존 내부 스크롤을 유지함. 1916·1280px의 외부 레일과 카드 간 간섭, 390·320px의 내부 레일과 가로 넘침 없음, 마지막 행 접근·키보드 스크롤을 브라우저에서 확인함. 회귀 테스트 수정 전 1개 실패·수정 후 8개 통과, 대상 ESLint 통과.

- 전체 스크롤 후속 수정 (2026-09-07): 통계 바깥 영역의 `scrollbar-hide`를 제거하는 대신 통계 전용 외부 레일·고정 프레임 구조를 적용하여 전체 페이지 스크롤바를 표시함. 내부 집계 목록의 `overscroll-contain`을 해제하여 목록 중간에서는 내부만, 최상단·최하단에서 더 스크롤하면 바깥 페이지가 움직이도록 수정함. 브라우저에서 위쪽 경계의 전체 페이지 위치 `681→499`, 아래쪽 경계 `499→608` 이동과 목록 중간의 바깥 위치 유지, 1916·1280px 외부 레일·가로 넘침 없음, 390px 모바일 배치를 확인함. 회귀 테스트 수정 전 2개 실패·수정 후 관련 18개 통과, 대상 ESLint 통과.

- 작업 선택 배치 통일 (2026-09-08): 데스크톱을 위쪽 `불량 격리 · 바로 처리`, 아래쪽 `격리 목록 · 불량 통계`의 동일 크기 2×2 배치로 변경함. 기존 공용 카드 디자인과 모바일 세로 목록은 유지함. 1918×908·1280×720 데스크톱과 390×844 모바일에서 브라우저 확인. 배치·카드 순서·네 가지 이동 대상 회귀 테스트 수정 전 실패·수정 후 통과, 대상 ESLint 통과.

- 통계 헤더 개선 (2026-09-08): 제목 아래에 있던 날짜를 이전·다음 버튼 사이로 옮기고, 주·월·연 선택 및 현재 기간 복귀와 하나의 조회 기간 영역으로 묶음. 기존 색상 토큰·버튼 크기를 유지하고 좁은 화면에서는 세로로 배치함. 관련 테스트 9개·대상 ESLint 통과. 1928px 데스크톱 배치와 월간 이전 기간의 실제 날짜 변경을 확인함. 390·320px 모바일 로딩 상태의 배치는 확인했으나 검증 중 개발 서버가 종료되고 재실행이 환경 정책에 차단되어 모바일 실제 날짜 표시 검증은 미완료.

- 필터 항목 정리 (2026-09-08): 대시보드·불량 공용 필터에서 모델 `미분류` 버튼을 제거함. 불량 목록·통계의 공정 `미분류`를 제거하고 `legacy_item_type=불용` 기준의 불용 필터를 추가함. 불용은 선택 공정·부서·모델과 교집합하며, 조건 없는 전체 조회에는 기존 불량 발생을 그대로 포함함. 저장된 불량 필터의 제거된 미분류 선택만 복원 시 해제하고 나머지 설정은 보존함. 불량 화면의 전체 초기화는 부서·모델·공정 중 선택된 조건이 있을 때만 표시함. 관련 프런트 테스트 37개·백엔드 통계/라우트 테스트 18개 및 대상 ESLint 통과. 3002 개발 서버가 중지되어 이번 변경의 브라우저 검증은 미완료.

- 위 두 항목의 미완료 브라우저 검증 보완 (2026-09-08): 워크트리 프런트 3002 재실행 및 백엔드 8021 새 코드 재시작 후 검증함. 대시보드 모델 미분류 제거, 불량 통계·격리 목록의 불용 선택과 초기화 버튼 표시/해제 후 숨김을 확인함. 연간 전체 82건·540개 → 불용 0건·0개 → 초기화 후 82건·540개 복원을 확인함(검증 시점 데이터). 390px 모바일 날짜 한 줄, 320px 두 줄 표시와 화살표 겹침 없음을 확인하고 화면 크기를 복원함. 실제 재고 변경 없이 조회만 수행함.

#### 2026-09-07 브라우저 재검증 근거와 한계

> 아래는 당시 검증 범위의 역사적 기록이며, 실제 제출 미완료 항목은 아래 2026-09-08 통합 검증으로 보완함.

- 필터 순서 후속 수정 (2026-09-07): 대시보드와 동일하게 창고 다음 생산 부서 순으로 정렬하고, 모델은 가나다순 재정렬 없이 모델 관리 목록 순서를 유지함. 공정 필터는 대시보드처럼 2열로 표시함. 통계 화면에서만 `내 부서`를 제거하고 격리 목록에는 유지함. 1918px 데스크톱에서 대시보드·통계 순서를 직접 대조했고, 390px 모바일에서도 통계의 버튼 제거·순서·가로 넘침 없음을 확인함. 관련 회귀 테스트 수정 전 5개 실패·수정 후 관련 테스트 50개와 대상 ESLint 통과.

- 검증 환경: `C:\ERP\.worktrees\defect-feedback`, `http://192.168.0.63:3002/mes?tab=defect`. 프런트의 일반 API·실시간 요청 모두 worktree 백엔드 8021을 향하도록 실행 환경을 분리함.
- 1916×908, 1280×720, 1024×768, 390×844, 320×740에서 카드·필터·통계 배치를 확인. 작은 모바일 하단 집계와 긴 품목 순위의 마지막 항목까지 접근 가능하고 가로 넘침이 없는 것을 확인함.
- 주간 빈 결과, 월간·연간 전환, 이전·다음 기간, 부서·모델 OR 및 공정 AND 조합, 전체 초기화, 레거시 제외 안내를 확인함. 기존 모바일 격리 등록의 검색·목록 스크롤도 확인함.
- 수정 후 불량 화면 관련 프런트 테스트 138개 통과, 관련 백엔드 테스트 통과, 프로덕션 빌드(타입·린트 포함) 통과. 필터 연속 저장과 모바일 history 복귀는 수정 전 실패·수정 후 통과 회귀 테스트를 추가함.
- 실제/복사 실데이터에 재고 변경을 제출하지 않음. 전용 테스트 DB는 따로 준비했지만 별도 프런트 서버 실행이 차단되어 최종 제출·승인까지의 브라우저 검증은 남아 있음. 통계 오류·재시도와 승인 대기 선택 제한도 이번 브라우저 순회가 아닌 자동화 테스트 검증 범위임.

#### 2026-09-08 main 통합 검증

- 최신 main의 즉시 실행 정책을 유지하면서 다건 처리 검증을 통합함. 기존 처리 대기 기록은 남은 가용 수량과 요청 수량이 일치하더라도 다건 선택 처리에서 거부하도록 보강함. 제출 시 409·422 또는 선택 수량/대기 상태 변경은 선택 해제·목록 갱신으로 연결함.
- 관련 백엔드 116개, 프런트 159개 테스트 통과. 일반 직원의 폐기·반품·합산 재작업 즉시 실행, 두 번째 기록 처리 실패 시 요청·재고·원장 전체 롤백, 기록별 차감·취소 복구와 통계 경계·중복 방지를 포함함.
- 실데이터와 분리된 테스트 DB 및 3003→8023 서버에서 관리자 브라우저 제출을 검증함. 정상 복귀·폐기·재작업은 3개 기록 중 수량 1·2의 두 기록만 처리하고 수량 3의 미선택 기록을 보존함. 창고 반품 두 기록도 즉시 처리됨. 폐기·반품·재작업 요청은 모두 COMPLETED이며 창고·부서 승인 요구가 없음.
- 테스트 DB를 읽기 전용으로 재검사하여 기록별 차감 원장과 합산 재작업의 단일 작업 ID, 구성품 6개 복구를 확인함. 최초 격리 20건·39개는 후속 처리 뒤에도 통계에서 중복 증가하지 않음. 불용 3건·6개와 불용·원자재 교집합의 빈 결과도 브라우저에서 확인함(테스트 데이터 기준).
- 프로덕션 빌드(타입·린트 포함) 통과. 통계 그래프 포함 번들 실측 2,953,372바이트(2.817MB)에 대해 사용자 승인으로 한도를 2.414MB→2.82MB로 변경했고 한도 회귀 테스트 및 실제 번들 검사를 통과함.
- 기존 3002→8021 검증 서버도 통합 코드로 다시 실행한 뒤 직원 목록·2×2 작업 카드·통계 헤더를 재확인함. 사유 목록 최상단에서 위로 스크롤할 때 전체 페이지가 796→523으로 이동함. 390px 날짜 한 줄·320px 두 줄 표시, 화살표 겹침과 가로 넘침 없음도 확인하고 뷰포트를 복원함. 이 서버에서는 재고 변경을 제출하지 않음.
- staged smart 게이트가 인프라 변경으로 전체 검증에 승격됨. 프런트 전체 2,474개 테스트(271파일), 커버리지·타입·린트·빌드·번들 검사 통과. 백엔드는 기존 백업 테스트의 1초 타이밍 조건 1건만 실패했으며 코드 변경 없이 단독 재실행하여 통과함. 따라서 최초 전체 게이트 종료 코드는 1이며 전체 명령 통과로 간주하지 않음. 이후 실패 테스트·OpenAPI 일치·문서 게이트를 각각 재검증해 통과함.
