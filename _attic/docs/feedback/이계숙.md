# 이계숙
받은 피드백: 2건 (해결 0 / 미해결 1 / 확인 1)

> 호칭: 주임. 2026-06-05 구두 전달.

---

## 2026-06-05
### 품목 리스트 — 모델 필터 순서 + 필터 내 품목 순서 사용자 지정 정렬
#품목 #정렬 #모델필터 #UX #요청 #미해결

품목 리스트가 **COCOON → SOLO → ADX6000FB** 순서로 정렬되길 원함.
그리고 각 모델 필터 안에서도 주임님이 원하는 순서대로 품목이 정렬되길 원함.

→ 미해결
- 요청 1 (모델 필터 순서): 모델 필터/그룹 표시 순서를 COCOON → SOLO → ADX6000FB 로 고정.
- 요청 2 (필터 내 품목 순서): 각 모델 안에서 품목 정렬 순서를 주임님 지정값대로. → **지정 순서 목록을 주임님께 받아야 함** (현재 미수령).
- 점검 후보:
  - 어느 화면의 품목 리스트인지 확인 필요 (입출고 품목 선택 / 품목 관리 / 재고 대시보드 등 모델 필터가 있는 화면).
  - 현재 정렬 기준(모델 slot 순? mes_code 순?) 확인 후, 사용자 지정 순서 컬럼(sort_order 등) 도입 검토.
- 관련: [project_item_drag_reorder] — 품목 관리 화면 드래그 정렬(미구현 예약)과 같은 맥락. 함께 설계하면 자연스러움.

---

### 품목 동일성 확인 — COCOON "4-2번 하네스" vs SOLO "5번 하네스"
#품목 #하네스 #데이터 #확인 #질문

COCOON의 "4-2번 하네스"와 SOLO의 "5번 하네스"가 동일한 품목이 맞는지 확인 요청.

→ 확인 필요 (미확인)
- 현장 호칭("4-2번", "5번")이라 mes_code/품목명과 직접 매칭 안 됨 — 실제 품목 식별 먼저 필요.
- 확인 방법: COCOON·SOLO 모델의 하네스 품목 목록 조회 후 동일 품목(같은 mes_code) 여부 대조.
- 결과에 따라: 동일하면 한 품목 공유, 다르면 별도 품목 — 위 정렬/BOM 작업에도 영향.
