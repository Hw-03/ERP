# 김도영
받은 피드백: 2건 (해결 1 / 미해결 0 / 보류 1)

---

## 2026-06-02
### 입출고(부서 간 이관) — 튜브→고압 인수인계서 작성 기능
#입출고 #부서이관 #인수인계 #요청 #해결

튜브 라인에서 고압 라인으로 물품을 넘길 때 인수인계서를 작성할 수 있는 기능 요청.

→ 해결 (2026-06-04, F1 — 커밋 `03cd6fb3`, UX 개선 `9ab9e66a`)
- 입출고 탭에 '인수인계' 섹션 신설. 튜브 담당자가 앱 폼으로 작성·제출 → 인수 부서(고압/진공) 담당자가 PIN 인수확인 시 품목 수량만큼 튜브→인수부서 PRODUCTION 자동 이동(TRANSFER_DEPT).
- 백엔드: `HandoverDoc`/`HandoverLine` 모델 + `/api/handovers` 라우터. 기존 부서이동·PIN·부서권한(can_approve_department) 재사용, 멱등 처리.
- 프론트: `HandoverSectionPanel`(작성/내 인수인계/인수 대기함) + `HandoverComposeForm` + `handoverPrint.ts`(별도 창 양식 인쇄). 품목 드롭다운(TF 품목만), 인수인계 도착 알림(handover_arrived).
- 인쇄: 인수인계서 양식 `window.print` 지원.

---

### 입출고(불량) — 불량 내역 인쇄 기능
#입출고불량 #인쇄 #요청 #보류

불량 내역을 인쇄할 수 있는 기능 요청.

→ 보류 (실사용 가능성 낮아 보류 — 사용자 판단)
- 현재 동작: 불량 내역 화면 내 인쇄/출력 기능 없음 (미구현).
- 기대 동작: 불량 내역(품목·수량·사유·날짜 등) 인쇄 또는 PDF 저장.
- 점검 후보:
  - 불량 내역 화면(`_defect_hub/` 또는 관련 내역 탭) 에 인쇄 버튼 추가.
  - `window.print()` + CSS `@media print` 로 화면 출력, 또는 별도 인쇄 레이아웃 컴포넌트.
- 인수인계서(#1)와 같은 인쇄 인프라 공유 가능.
