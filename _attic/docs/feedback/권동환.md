# 권동환
받은 피드백: 7건 (해결 5 / 미해결 1 / 보류 1)

> 2026-05-29 휴직 직전 가이드라인 메일로 일괄 전달. 휴직 (~2026-06-25 복귀).
> 메모리 [project_kwon_donghwan.md] — 직책 단정 금지(정보보안 담당자 아님).

---

## 2026-05-29
### 공정별 재고 — 안전재고·현장재고 엑셀 취합 후 MES 반영
#재고 #공정별 #엑셀취합 #요청 #해결

각 공정별 담당자에게 메일로 품목별 안전재고량 + 현장재고량 엑셀 취합. 현장재고량은 향후 현장 담당자 협조 아래 실시간 최신화. MES 최종 적용 당일에는 취합 후 변동 수량만 빠르게 업데이트 후 즉시 반영.

→ 해결 (2026-06-23)
- 안전재고 기준값 MES 적용 완료. R품목 기준 1대당 BOM사용량×200 규칙 적용, A/F는 NULL. 개발 DB 반영 완료(2026-06-22, R703·NULL239·변경275).

---

### UI/UX 신규 — 좌측 탭 가장 아래에 "불량 내역" 탭 신규 개설
#UI #신규기능 #불량 #요청 #해결

MES 좌측 기능 탭의 가장 아랫칸에 불량 내역을 한눈에 모아서 확인할 수 있는 탭 신규 개설.

→ 해결 (2026-06-02)
- 불량 전용 탭 신설 — 입출고에서 분리 (커밋 `4408fa60`).
- 이후 작업 화면 "입출고 손맛" 복원 (커밋 `7c03a572`).

---

### 시스템 안정화 — Log 파일 누락 항목 보충
#안정화 #로그 #audit #요청 #해결

현재 누락된 Log 항목들을 권동환 사원과 사전에 파악한 대로 보충.

→ 해결 (2026-05-29 ~ 2026-06-02)
- `aa5ea014` (5/29) — 운영 로그 시스템 신설 (액세스·도메인 이벤트·사번 부착) + AdminAuditLog 사번 컬럼.
- `b88a8190` (6/2) — 직접 입출고 사번 감사 보강 (B C6).

---

### 시스템 안정화 — 클린코드 유지 (작업 범위 재확인 요청)
#안정화 #클린코드 #리팩토링 #요청 #보류

시스템 향후 확장성을 위해 가독성 높고 정돈된 코드 유지.

→ 보류 (2026-06-16, 사용자 판단) — 복귀 후 재개
- code-review 대규모 리팩토링 진척 (cleanup/code-review-fixes 브랜치, PR #18/#19 머지).
- 권동환 사원 복귀(~6/25) 시 전체 리뷰 예정 — 그때까지 추적 목록에서 보류.

---

### 도메인 변경 — `ItemCode` → `erp_code` 또는 `mes_code` 기준
#도메인 #ItemCode #mes_code #리팩토링 #요청 #해결

큰 틀은 ItemCode 가 아닌 `erp_code` 또는 `mes_code` 기준으로 진행. 세부 구현은 주간보고 사전 피드백으로 공유.

→ 해결 (2026-06-01 ~ 2026-06-02)
- `658b2515` (6/1) — backend item_code → mes_code 전면 리네임 (DB·스키마·OpenAPI).
- `30525682` (6/1) — frontend item_code → mes_code 리네임 (백엔드 API 키 lockstep).
- `d67f450c` (6/2) — mes_code 생성열 진실소스화 + 모델 마스터 단일화.

---

## 2026-06-25
### 알림 창 — 알림 삭제 기능
#알림 #UX #요청 #미해결

알림 창에서 알림을 삭제할 수 있으면 좋겠음.

→ 미해결

---

## 2026-06-01
### 입출고(창고·부서 요청함) — 새 요청이 맨 위에 오도록 정렬 변경
#입출고 #창고요청함 #부서요청함 #정렬 #불편 #해결

창고·부서 요청함에서 새로운 요청이 맨 아래 생기는 게 아니라 맨 위에 생기도록.

→ 해결 (2026-06-02)
- `backend/app/routers/stock_requests.py` — `list_warehouse_queue`(L150), `list_department_queue`(L215) 두 곳 `.order_by(created_at.asc())` → `.order_by(created_at.desc())` 변경. 프론트 정렬 로직 없음(백엔드 그대로 사용).

