# 권동환
받은 피드백: 6건 (해결 0 / 미해결 6 / 보류 0)

> 2026-05-29 휴직 직전 가이드라인 메일로 일괄 전달. 휴직 17일 (~2026-06-15 전후 복귀).
> 메모리 [project_kwon_donghwan.md] — 직책 단정 금지(정보보안 담당자 아님).

---

## 2026-05-29
### 공정별 재고 — 안전재고·현장재고 엑셀 취합 후 MES 반영
#재고 #공정별 #엑셀취합 #요청 #미해결

각 공정별 담당자에게 메일로 품목별 안전재고량 + 현장재고량 엑셀 취합. 현장재고량은 향후 현장 담당자 협조 아래 실시간 최신화. MES 최종 적용 당일에는 취합 후 변동 수량만 빠르게 업데이트 후 즉시 반영.

→ 미해결
- 사용자 회신에서 진행 합의 완료. 작업 시작 필요.
- 공정별 담당자 명단 + 요청 메일 양식 준비 → 발송 → 취합 → 변동 수량 갱신 적용 순서.

---

### UI/UX 신규 — 좌측 탭 가장 아래에 "불량 내역" 탭 신규 개설
#UI #신규기능 #불량 #요청 #미해결

MES 좌측 기능 탭의 가장 아랫칸에 불량 내역을 한눈에 모아서 확인할 수 있는 탭 신규 개설.

→ 미해결
- 사용자 회신에서 진행 합의 완료.
- 프론트 작업 영역 — 권동환 휴직 중 사용자가 단독 진행 가능.

---

### 시스템 안정화 — Log 파일 누락 항목 보충
#안정화 #로그 #audit #요청 #미해결

현재 누락된 Log 항목들을 권동환 사원과 사전에 파악한 대로 보충.

→ 미해결
- 사용자 회신에서 "%TEMP% 정리는 이 작업에 포함, 사번 기반 audit 로그는 현재 일부 구현 위에 보강하는 방향" 으로 답함.
- 사전에 파악한 누락 항목 목록 위치 확인 필요 (이전 회의록·메모).

---

### 시스템 안정화 — 클린코드 유지 (작업 범위 재확인 요청)
#안정화 #클린코드 #리팩토링 #요청 #미해결

시스템 향후 확장성을 위해 가독성 높고 정돈된 코드 유지.

→ 미해결 (작업 범위 재확인 답변 대기)
- 사용자 회신에서 작업 범위 재확인 요청: "공석 기간 동안 모든 파일(백엔드 DB 포함) 작업 후 복귀 시 리뷰 방향이 맞는지" 질문.
- 답에 따라 17일 작업 범위 확정 (큰 변경 진행 vs 작은 정리만).
- 답 받기 전 안전한 작업: 데드 코드 제거, 빈 DB 파일 정리, 폴더 청소.
- 답 받은 후: DB 스키마 변경(C+B, 도메인 mes_code 통일 등) 진행 여부 결정.

---

### 도메인 변경 — `ItemCode` → `erp_code` 또는 `mes_code` 기준
#도메인 #ItemCode #mes_code #리팩토링 #요청 #미해결

큰 틀은 ItemCode 가 아닌 `erp_code` 또는 `mes_code` 기준으로 진행. 세부 구현은 주간보고 사전 피드백으로 공유.

→ 미해결
- 사용자 회신에서 `mes_code` 기준으로 진행 답변 완료.
- 세부 구현은 매주 주간보고 사전 피드백 메일로 권동환 사원에게 단계별 공유 → 피드백 → 진행 반영.
- 5/22 메일에서 "사전 협의 없는 독단 변경" 우려 표명한 영역 — 절차 준수 중요.

---

## 2026-06-01
### 입출고(창고·부서 요청함) — 새 요청이 맨 위에 오도록 정렬 변경
#입출고 #창고요청함 #부서요청함 #정렬 #불편 #미해결

창고·부서 요청함에서 새로운 요청이 맨 아래 생기는 게 아니라 맨 위에 생기도록.

→ 미해결
- 현재 동작 추정: 요청 목록이 `created_at ASC` 또는 ID 오름차순으로 정렬 → 새 요청이 맨 아래.
- 기대 동작: `created_at DESC` 로 최신 요청이 상단.
- 점검 후보:
  - 백엔드 — 창고/부서 요청 목록 라우터의 ORDER BY 절
  - 프론트 — 요청함 리스트 컴포넌트의 sort 로직 (백엔드 정렬을 그대로 받는지, 프론트에서 다시 sort 하는지)
- 작업량: 작음. 한 곳(또는 두 곳)의 정렬 기준만 변경. 페이지네이션·필터·승인 흐름에 부수 영향 없는지 확인 필요.

