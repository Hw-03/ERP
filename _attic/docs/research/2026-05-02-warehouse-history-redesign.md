# 입출고·내역 화면 재설계 — 2026-05-02

> **작업 ID:** MES-WAREHOUSE-001~002, MES-HISTORY-001~002  
> **작성일:** 2026-05-02 (토)  
> **기준 브랜치:** `feat/hardening-roadmap` (단일 — 초기 분석 브랜치 `claude/analyze-dexcowin-mes-tGZNI` 폐기)  
> **수정 여부:** 없음 (설계 문서만)

---

## MES-WAREHOUSE-001 — 입출고 화면 동선 개선안

### 현재 WorkType → 50~60대 용어 매핑

| WorkType (코드) | 현재 레이블 | 개선 레이블 | 이유 |
|---|---|---|---|
| `raw-io` | 원자재 입출고 | **창고 입고 / 출고** | "원자재" 는 현장에서 안 씀 |
| `warehouse-io` | 창고 이동 | **창고 ↔ 부서 이동** | 방향 명확화 |
| `dept-io` | 부서 입출고 | **부서 입고 / 부서 출고** | 부서 기준임을 강조 |
| `package-out` | 패키지 출고 | **묶음 출고** | 더 짧고 직관적 |
| `defective-register` | 불량 등록 | **불량 격리** | 격리 행위 강조 |

### 개선된 위저드 흐름 (5단계 → 4단계)

```
현재:  [직원선택] → [작업유형] → [품목+수량] → [확인] → [완료]
개선:  [직원선택] → [작업유형]  →  [품목+수량]  → [최종확인+완료]
                                   ↑
                            재고 현황 상단 고정 노출
```

### 화면 상단 고정 영역 (개선 핵심)

```
┌─────────────────────────────────────────────────────┐
│  [품목명]  현재 창고 수량: 150개                      │
│  가용 수량: 120개 (예약 30개)   ← pending_quantity    │
│  안전재고: 50개  ⚠ 가용 < 요청 시 빨간 배너           │
└─────────────────────────────────────────────────────┘
```

### 수량 입력 실수 방지

| 기능 | 현재 | 개선 |
|---|---|---|
| 음수 차단 | 서버 검증만 | UI 레벨 `min={0}` + 빨간 테두리 |
| 최대 수량 안내 | 없음 | 가용 수량 표시 + 초과 시 경고 |
| 스텝퍼 | 없음 | `+1/-1` 버튼 + 직접 입력 병행 |
| 모바일 키패드 | inputMode 미설정 | `inputMode="decimal"` |

### 재고 부족 경고 규칙

```
가용 수량 = warehouse_qty - pending_quantity

요청 수량 > 가용 수량 → 빨간 배너 "재고 부족 (가용: N개)"
요청 수량 > 가용 수량 × 0.8 → 노란 경고 "가용 재고 여유 없음"
가용 수량 < min_stock → 노란 배너 "안전재고 미만"
```

### 작업 후 피드백

```
완료 시:
1. Toast "입고 완료 — 품목명 N개" (3초)
2. 하단에 최근 작업 5건 미니 리스트 갱신
3. 수량 입력 초기화, 포커스 품목 검색으로 복귀
```

### 데스크톱 Tab→Enter 흐름

```
직원 선택 [Tab] → 작업 유형 선택 [Tab] → 품목 검색 [Tab] → 수량 입력 [Enter] → 확인 모달 [Enter] → 완료
```

### 모바일 UX

- 버튼 최소 44×44px (Tailwind `h-11 min-w-11`)
- 수량 입력 시 숫자 키패드 자동 표시 (`inputMode="decimal"`)
- 품목 검색창: 화면 상단 고정

---

## MES-WAREHOUSE-002 — 예약재고/가용재고 표시 위치

### API 필드 확인

```ts
// InventoryLocation (lib/api.ts)
warehouse_qty: number        // 창고 현재 수량
pending_quantity: number     // 예약/미확정 수량
available = warehouse_qty - pending_quantity  // 가용 수량 (파생)
min_stock: number | null     // 안전재고 기준
```

### 표시 위치 권장

| 위치 | 표시 내용 | 우선도 |
|---|---|---|
| 입출고 Step 3 품목 선택 후 | 창고 N개 / 가용 N개 / 예약 N개 | 최우선 |
| 재고 화면 품목 리스트 | 간단히 가용/총량 칩 | 중 |
| 품목 상세 슬라이드 패널 | 전체 상세 + 이력 | 중 |

---

## MES-HISTORY-001 — 내역 화면 검색·필터 개선

### 현재 vs 개선 필터 비교

| 필터 | 현재 | 개선 |
|---|---|---|
| 거래 타입 | ✅ ALL/RECEIVE/SHIP/... | ✅ 유지 + 한국어 칩으로 교체 |
| 기간 | ✅ ALL/TODAY/WEEK/MONTH | ✅ + 사용자 지정 날짜 범위 추가 |
| 텍스트 검색 | ✅ 있음 (범위 불명확) | ✅ 품목명+erp_code+바코드+직원명 통합 |
| 부서별 | ❌ | ✅ 부서 칩 선택 |
| 작업자별 | ❌ | ✅ 직원 드롭다운 |
| 상태별 | ❌ | ✅ DRAFT/SUBMITTED/COMPLETED/CANCELLED |
| 취소·정정 연결 | ❌ | ✅ 원거래 ↔ 정정거래 링크 |
| CSV/Excel 내보내기 | ❌ | ✅ `export_helpers.py` 연동 |

### 개선 UI 구조

```
┌─────────────────────────────────────────────────────────┐
│ 🔍 통합 검색: [품목명/ERP코드/바코드/직원명          ]  │
├─────────────────────────────────────────────────────────┤
│ 타입: [전체] [입고] [출고] [조정] [생산] [폐기] [손실]  │
│ 기간: [전체] [오늘] [7일] [30일] [직접입력]             │
│ 부서: [전체] [조립] [고압] [진공] ...                   │
├─────────────────────────────────────────────────────────┤
│ 뷰:  [📅 캘린더] [📋 리스트]          [⬇ CSV 내보내기] │
└─────────────────────────────────────────────────────────┘
```

### 50~60대 날짜 표현

```ts
// 개선 전
"2026-05-01T09:30:00"

// 개선 후
"2026년 5월 1일 오전 9:30"  (한국어 로케일)

// 상대 시간 (6시간 이내)
"3시간 전"
```

### 거래 타입 9종 한국어 매핑

| TransactionType | 한국어 라벨 | 색상 칩 |
|---|---|---|
| RECEIVE | 입고 | 🟢 초록 |
| SHIP | 출고 | 🔵 파랑 |
| ADJUSTMENT | 조정 | 🟡 노랑 |
| PRODUCE | 생산 소비 | 🟣 보라 |
| SCRAP | 폐기 | 🔴 빨강 |
| LOSS | 분실 | 🟠 주황 |
| RETURN | 반품 | 🩵 하늘 |
| RESERVE | 예약 | 🌸 분홍 |
| CANCEL | 취소 | ⚫ 회색 |

---

## MES-HISTORY-002 — CSV/Excel 내보내기 점검

### 기존 서비스 확인

`backend/app/services/export_helpers.py` 존재 확인.  
`GET /api/items/export` (CSV), `GET /api/items/export-xlsx` (Excel) 이미 구현됨.

### 내역(TransactionLog) 내보내기 추가 필요

```python
# 현재 없음. 추가 후보:
GET /api/inventory/transactions/export?type=csv&from=2026-05-01&to=2026-05-31
```

### 내보내기 컬럼 (권장)

```
날짜, 거래유형, 품목코드, 품목명, 공정구분, 수량, 창고수량(후), 직원명, 부서, 참조번호, 메모
```

### 위험도

API 추가: C등급 (서버 확인 필요) / 프론트 버튼: B등급
