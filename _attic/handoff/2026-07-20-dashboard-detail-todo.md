# 2026-07-20 DEXCOWIN MES 대시보드 상세 패널 TODO

## 전체 현황

- 구현 완료된 기준 TODO: 5건
- 구현해야 할 후속 TODO: 0건
- 총 관리 TODO: 5건
- 확정 후속 TODO: 0건
- 자체 식별 추가 TODO: 0건
- 사용자 추가 TODO: 5건 (5건 구현 완료 / 0건 후속)
- 공통: 0건
- 대시보드: 5건 (5건 구현 완료 / 0건 후속)
- 구현 완료 우선순위: 높음 0건 / 중간 1건 / 낮음 4건
- 구현 전 정책 확인 필요: 0건
- 문서 목적: 2026-07-20 PC 대시보드 품목 상세 패널의 상태 정보 배치 변경을 다음 구현 세션에 인계한다.

## 참고한 기준과 근거

- 브라우저 확인: `http://192.168.0.63:3001/mes?tab=dashboard`, 1918×908 PC. 우측 품목 상세 패널에서 품목 코드 `46-AA-0080`과 상태 배지 `정상`을 확인했다.
- 코드 확인:
  - `frontend/app/mes/_components/_hooks/useDesktopInventoryDerivations.tsx:82-97` — 선택 품목의 재고 상태 배지를 만든다.
  - `frontend/app/mes/_components/_inventory_sections/DesktopInventoryRightPanel.tsx:38-42` — 품목명, 품목 코드, 상태 배지를 우측 패널로 전달한다.
  - `frontend/app/mes/_components/DesktopRightPanel.tsx:47-66` — 현재 상태 배지는 제목 행의 우측, 품목 코드는 제목 아래 보조행에 렌더한다.

## 확정 결정 요약

- 품목 상세의 재고 상태 배지(`정상`/`부족`/`품절`)는 제목 행 우측에서 제거한다.
- 상태 배지는 품목 코드 뒤에 작은 간격을 두고 같은 보조행에 표시한다.

## 대시보드

### 1. 상세 헤더 상태 배지를 품목 코드 옆으로 이동

- 상태: 구현 완료
- 결과: 상태 배지를 제목 우측에서 제거하고 품목 코드 보조행 뒤에 표시했다.
- 화면/영역: PC 대시보드 > 자재 목록 > 선택 품목 우측 상세 패널 헤더
- 증상: 재고 상태 배지 `정상`이 품목명 제목과 닫기 버튼 사이의 우측 공간에 배치되어, 바로 아래의 품목 코드와 상태 정보가 분리되어 보인다.
- 사용자 불편: 품목을 식별하는 코드와 그 품목의 재고 상태를 한 묶음으로 읽기 어렵고, 제목 행의 우측 공간도 불필요하게 차지한다.
- 확정 정책: 상태 배지는 제목 행에서 제거하고, 품목 코드 뒤에 간격을 둔 인라인 요소로 표시한다. 품목명은 첫 줄, `품목 코드 + 상태`는 둘째 줄의 정보 묶음으로 유지한다.
- 구현 방향: 데스크톱 품목 상세 패널에서 상태 배지를 보조행으로 전달·렌더하도록 조정한다. 공용 우측 패널의 다른 화면 헤더와 모바일 대시보드의 배치에는 영향을 주지 않는다. 상태 판단 로직과 배지 색상·문구는 재사용한다.
- 수용 기준: 선택 품목의 제목 우측에는 상태 배지가 남지 않는다. 둘째 줄에 `46-AA-0080` 뒤 간격을 두고 `정상` 배지가 표시되며, 부족·품절 상태도 같은 위치와 기존 색상 체계를 사용한다. 닫기 버튼 위치와 긴 품목명·품목 코드의 줄바꿈/말줄임 동작은 유지된다.
- 브라우저 확인 시나리오: 1918×908 PC 대시보드에서 정상·부족·품절 품목을 각각 선택한다. 각 패널에서 제목 오른쪽에 상태 배지가 없는지, 품목 코드 바로 뒤에 해당 상태 배지가 있는지, 긴 품목명 또는 코드에서도 닫기 버튼·배지가 겹치지 않는지 확인한다.
- 우선순위: 낮음

### 2. 대시보드 상세 패널 닫기 버튼을 입출고내역과 같은 붉은 버튼으로 통일

- 상태: 구현 완료
- 결과: 기본 `SlidePanel` 닫기 버튼을 숨기고, 기존 모달·ESC·포커스 트랩은 유지한 채 카드 헤더의 붉은 원형 닫기 버튼을 사용한다.
- 화면/영역: PC 대시보드 > 자재 목록 > 선택 품목 우측 상세 패널의 닫기 버튼
- 증상: 대시보드 상세 패널은 `SlidePanel`의 기본 닫기 버튼을 사용해 붉은 원형 닫기 버튼이 아닌 형태로 표시된다. 입출고내역 상세 패널은 카드 헤더 안에서 붉은 원형 `X` 버튼을 사용한다.
- 사용자 불편: 동일한 우측 상세 카드임에도 탭마다 닫기 동작의 시각적 위치와 색상 문법이 달라 사용자가 일관된 종료 동작으로 인식하기 어렵다.
- 확정 정책: 대시보드 우측 품목 상세 패널도 입출고내역 카드와 같은 붉은 원형 닫기 버튼을 카드 헤더 우측에 표시한다.
- 구현 방향: 대시보드 패널에서는 `SlidePanel`의 기본 닫기 버튼을 숨기고, 공용 `DesktopRightPanel`의 `onClose` 버튼을 사용한다. ESC·포커스 트랩·선택 해제 등 기존 패널 종료 동작은 유지한다.
- 수용 기준: 대시보드 우측 상세 카드 헤더 우측에 붉은 원형 `X` 버튼이 하나만 보인다. 이를 누르면 기존과 같이 패널이 닫히고 선택이 해제된다. 기본 `SlidePanel` 닫기 버튼은 중복 표시되지 않는다.
- 브라우저 확인 시나리오: 1918×908 PC 대시보드에서 아무 품목이나 선택한다. 우측 패널의 닫기 버튼이 입출고내역 상세 카드의 붉은 원형 버튼과 같은 위치·색상 문법인지 확인한다. 버튼 클릭과 ESC로 각각 패널이 정상 종료되고, 재선택 시 패널이 정상적으로 다시 열리는지 확인한다.
- 코드 확인: 현재 대시보드는 `frontend/app/mes/_components/_inventory_sections/DesktopInventoryRightPanel.tsx:36`에서 기본 `SlidePanel` 닫기 버튼을 사용한다. 입출고내역은 `frontend/app/mes/_components/_history_sections/DesktopHistoryRightPanel.tsx:66-79`에서 기본 버튼을 숨기고 `DesktopRightPanel`에 `onClose`를 전달한다. 붉은 버튼 정의는 `frontend/app/mes/_components/DesktopRightPanel.tsx:66-78`에 있다.
- 우선순위: 낮음

### 3. 대시보드 빠른 작업의 출고 버튼을 붉은색으로 구분

- 상태: 구현 완료
- 결과: 입고는 파란색을 유지하고, 데스크톱 출고 버튼을 붉은색으로 변경했다.
- 화면/영역: PC 대시보드 > 자재 목록 > 선택 품목 우측 상세 패널 > `빠른 작업`
- 증상: `입고` 버튼은 파란색이지만, 대응 동작인 `출고` 버튼은 중립 회색으로 표시되어 두 방향의 의미가 시각적으로 짝을 이루지 못한다.
- 사용자 불편: 빠른 작업에서 출고가 입고의 반대 방향이라는 점을 색상만으로 빠르게 구분하기 어렵다.
- 확정 정책: 입고는 기존 파란색을 유지하고, 출고 버튼은 붉은색으로 표시한다.
- 구현 방향: 데스크톱 빠른 작업의 최상위 `출고` 버튼 배경과 글자색만 기존 경고 색상 토큰으로 변경한다. 출고 세부 선택지, 클릭 후 입출고 화면 이동, 메뉴 열림/닫힘 상태는 변경하지 않는다.
- 수용 기준: 우측 상세의 `입고`는 파란색, `출고`는 붉은색 버튼으로 나란히 보인다. 출고 버튼을 눌렀을 때 기존 출고 선택지가 열리고, 각 선택지는 기존과 같이 입출고 화면의 해당 흐름으로 이동한다.
- 브라우저 확인 시나리오: 1918×908 PC 대시보드에서 아무 품목이나 선택한다. 빠른 작업의 입고·출고 버튼 색상이 각각 파랑·빨강인지 확인한다. 출고를 눌러 세부 선택지를 연 뒤, 하나를 선택해 기존 출고 흐름으로 이동하는지 확인한다.
- 코드 확인: `frontend/app/mes/_components/_inventory_sections/InventoryDetailPanel.tsx:352-359`에서 데스크톱 출고 버튼이 현재 중립 표면 토큰(`LEGACY_COLORS.s3`)을 사용한다. 모바일은 같은 파일 `273-280`에서 이미 `LEGACY_COLORS.red`를 사용한다.
- 우선순위: 낮음

### 4. 빠른 작업 세부 선택지를 펼칠 때 패널 본문을 해당 위치까지 자동 스크롤

- 상태: 구현 완료
- 결과: 입고·출고 선택지가 본문 아래로 넘치는 경우에만 필요한 만큼 우측 패널 본문을 부드럽게 스크롤한다.
- 화면/영역: PC 대시보드 > 자재 목록 > 선택 품목 우측 상세 패널 > `빠른 작업`의 입고·출고
- 증상: 입고 또는 출고를 눌러 세부 선택지가 펼쳐지면 패널 본문의 전체 높이와 스크롤바만 변하고, 새로 열린 선택지 영역은 화면 아래에 남아 바로 보이지 않는다.
- 사용자 불편: 사용자가 버튼을 눌렀는데도 변화가 보이지 않아 다시 누르거나, 별도로 패널을 스크롤해야 세부 선택지를 찾을 수 있다.
- 확정 정책: 입고 또는 출고 세부 선택지가 열리면, 패널 본문은 새 선택지 영역이 즉시 보이고 조작 가능한 위치까지 자동 스크롤한다.
- 구현 방향: 데스크톱 우측 패널 본문 스크롤 컨테이너를 사용해, 메뉴 열림 상태가 바뀐 뒤 해당 빠른 작업 영역을 `scrollIntoView` 또는 동등한 범위 계산으로 스크롤한다. 메뉴를 닫거나 다른 메뉴로 전환하는 기존 상태 전환과 사용자의 수동 스크롤은 유지한다.
- 수용 기준: 패널을 위쪽에 둔 상태에서 입고·출고를 눌러도 해당 세부 선택지의 제목과 최소 첫 선택지가 즉시 화면에 보인다. 스크롤은 선택지로 이동하는 데 필요한 만큼만 발생하고, 다른 상세 정보의 내용·순서는 바뀌지 않는다.
- 브라우저 확인 시나리오: 1918×908 PC 대시보드에서 상세 패널 상단을 보이는 상태로 스크롤한 뒤, 하단의 입고와 출고를 각각 누른다. 매번 새 선택지 영역이 즉시 보이는지, 다시 누르면 접히는지, 입고에서 출고로 바꾸면 출고 선택지가 보이는지 확인한다.
- 코드 확인: 데스크톱 빠른 작업 토글과 선택지는 `frontend/app/mes/_components/_inventory_sections/InventoryDetailPanel.tsx:315-383`에 있다. 공용 패널 본문 스크롤 컨테이너 접근 훅은 `frontend/app/mes/_components/DesktopRightPanel.tsx:10-13,81`에 있다.
- 우선순위: 중간

### 5. 빠른 작업 세부 선택지 상자를 더 긴 높이로 표시

- 상태: 구현 완료
- 결과: 데스크톱 선택지 컨테이너와 내부 버튼의 세로 여백·설명 글자 크기를 늘렸다.
- 화면/영역: PC 대시보드 > 자재 목록 > 선택 품목 우측 상세 패널 > 펼친 입고·출고 세부 선택지 상자
- 증상: 현재 선택지 상자는 두 항목의 텍스트를 매우 촘촘하게 담아, 패널 하단에서 작은 조각처럼 보인다.
- 사용자 불편: `부서 입고`·`창고 반입`처럼 목적과 설명을 함께 읽어야 하는 선택지가 너무 압축되어 빠르게 고르기 어렵다.
- 확정 정책: 펼친 세부 선택지 상자는 브라우저 표시 예시처럼 두 선택지가 충분한 세로 여백을 갖는 더 긴 상자로 표시한다. 각 선택지의 제목과 설명을 한눈에 읽을 수 있어야 한다.
- 구현 방향: 데스크톱 입고·출고 세부 선택지 컨테이너와 내부 선택지의 세로 패딩/최소 높이를 늘린다. 패널 전체의 스크롤 구조는 유지하고, 넓어진 상자가 하단을 넘길 경우 항목 4의 자동 스크롤로 접근 가능하게 한다.
- 수용 기준: 펼친 상자는 두 개의 세부 선택지가 제목·설명과 함께 여유 있게 표시될 만큼 길어지며, 스크린샷에서 지정한 패널 하단 영역과 같은 밀도로 보인다. 각 선택지는 기존 클릭 영역보다 작아지지 않고, 선택 시 기존 입출고 흐름으로 이동한다.
- 브라우저 확인 시나리오: 1918×908 PC 대시보드에서 입고와 출고를 각각 펼친다. 두 선택지 상자가 압축되지 않고 충분한 높이로 보이는지, 제목·설명이 잘리지 않는지, 항목을 눌러 기존 입출고 흐름으로 이동하는지 확인한다.
- 코드 확인: 데스크톱 세부 선택지 컨테이너와 내부 버튼 크기는 `frontend/app/mes/_components/_inventory_sections/InventoryDetailPanel.tsx:325-349,360-383`에서 정의한다.
- 우선순위: 낮음
