# 일일 작업 일보 개선 TODO 핸드오프

- 수집 상태: 진행 중
- 최초 수집: 2026-08-03 브라우저 코멘트 1개와 사용자 요구 2개

## 전체 현황

- 구현 완료된 기준 TODO: 0개
- 구현해야 할 후속 TODO: 3개
- 총 관리 TODO: 3개
- 확정 후속 TODO: 2개
- 자율 점검 추가 TODO: 0개
- 사용자 추가 TODO: 3개
- 일일 작업 일보: 3개
- 우선순위: 높음 3개 / 중간 0개 / 낮음 0개
- 구현 전 정책 확인 필요: 1개
- 문서 목적: 기존 엑셀 일보에 익숙한 생산부 사용자가 MES 일일 작업 일보로 자연스럽게 전환하도록, 용어·데스크톱 가용 폭·양식 구조의 차이를 구현 단위로 정리해 후속 계획 세션에 인계한다.

## 참고한 기준과 근거

- 기존 엑셀 원본: `\\NAS\Nas 문서\03. 생산부\03. 일일작업일보\07. 2026년\2026년 02월\2026.02월_일일 작업 일보_김현우.xlsx`의 `02.02` 시트.
  - 제목은 과거 파일에서 `일일 작업 일지`로 표기됐지만, 제품 UI의 공식 명칭은 사용자가 확정한 `일일 작업 일보`로 사용한다.
  - 날짜·부서·작성자·승인자 헤더와 `모델명 / 작업내역 / 총 작업 수량 / 불량 수량 / 실제 작업 완료 / 비고` 열, 10분 단위 작업 구간, 하단 `기타 사항`을 확인했다.
- 브라우저 확인: 2026-08-03 `/mes?tab=dailyReport` 데스크톱 화면. 상단·작성 카드·거래 활동 카드 전체가 중앙 `max-w-[1180px]` 안에 묶여 좌우 가용 공간이 크게 남는다.
- 코드 확인:
  - 일보 화면의 폭 제한과 현재 표시 명칭: `frontend/app/mes/_components/_daily_report/DailyWorkReportScreen.tsx:132-141`
  - 데스크톱 탭 제목: `frontend/app/mes/_components/DesktopMesShell.tsx:61`
  - 현재 MES 활동 요약은 모델별 행이 아니라 작업 종류별 요약 카드: `frontend/app/mes/_components/_daily_report/DailyWorkActivity.tsx:31-90`
  - 이력·재고·출하 등 인접 데스크톱 화면은 가용 본문을 직접 채우는 `flex-1` 컨테이너를 사용한다: `frontend/app/mes/_components/DesktopHistoryView.tsx:460`, `frontend/app/mes/_components/DesktopInventoryView.tsx:159`, `frontend/app/mes/_components/DesktopShippingView.tsx:1336`

## 자율 점검 기준

- 제품 화면의 공식 명칭, 사이드바, 탭, 모바일 더보기, 안내·오류 문구는 한 용어를 사용한다.
- 일보의 핵심 정보는 자유 서술 한 칸보다 먼저 눈에 들어오고, 기존 사용자가 엑셀의 어느 칸이 옮겨졌는지 바로 알 수 있다.
- MES 자동 집계와 사용자의 서술 입력이 같은 사실을 중복 요구하지 않는다.
- 시간대별 직접 입력을 다시 강제하지 않으며, 과거에 확정한 개인별 하루 1건 정책·본인만 수정·승인/잠금 없음은 유지한다.
- 데스크톱 본문은 다른 업무 탭과 같은 가용 폭을 쓰되, 카드·표의 읽기 가능한 열 폭과 44px 조작 영역을 유지한다.
- 디자인은 `LEGACY_COLORS`와 기존 `rounded-[20px]` 카드·`var(--c-card-shadow)`·Lucide 아이콘만 사용해 라이트/다크 테마를 함께 보장한다.

## 확정 결정 요약

- 제품에서 보이는 명칭은 `일일 작업 일보`로 통일한다. 내부 기술 식별자 `dailyReport`는 호환성을 위해 유지한다.
- 데스크톱 일보 화면은 중앙 1180px 제한을 제거하고, 다른 데스크톱 업무 탭처럼 본문 가용 폭을 사용한다.
- 새 양식은 시간대별 입력을 그대로 복원하지 않고, 기존 엑셀의 정보 구조를 살리는 방향으로 다시 설계한다. 모델 행의 범위와 각 수량의 자동/수동 기준은 구현 전 확정한다.

## 일일 작업 일보

### 1. 제품 표기를 `일일 작업 일보`로 통일

- 화면/영역: 데스크톱 사이드바·상단 제목·브라우저 탭 제목, 모바일 더보기와 전용 화면, 작성/조회/오류 안내 문구.
- 증상: 현재 화면과 메뉴가 `일일 작업 일지`로 표시된다. 사용자가 정한 업무 명칭은 `일일 작업 일보`다.
- 사용자 불편: 현장에서 쓰는 문서명과 제품명이 달라 같은 업무 화면인지 즉시 인식하기 어렵다.
- 확정 정책: 한국어로 사용자에게 보이는 모든 표현을 `일일 작업 일보`로 통일한다. API 경로·프런트 탭 ID 등 기술 식별자 `dailyReport`는 바꾸지 않는다.
- 구현 방향: 탭 메타데이터, 데스크톱/모바일 진입점, 화면 제목, 입력 라벨·빈 상태·오류 문구, 접근성 이름과 관련 테스트의 사용자 문구를 함께 점검한다. 과거 엑셀 파일의 제목과 백엔드 내부 클래스명은 제품 UI 표기 변경 대상이 아니다.
- 수용 기준:
  - 데스크톱 사이드바, 상단 제목, `내 일보`/`전체 일보` 흐름, 모바일 더보기에서 `일일 작업 일보`가 표시된다.
  - 사용자에게 노출되는 `일지` 표현이 이 기능 화면·진입점에 남지 않는다.
  - 기존 `dailyReport` URL, 직원별 탭 숨김 설정, 저장·조회 API는 호환된다.
- 브라우저 확인 시나리오: 데스크톱에서 사이드바로 진입한다 → 상단 제목과 작성·조회 상태를 확인한다 → 모바일 더보기에서 같은 항목을 연다 → 날짜 전환·저장 실패·활동 빈 상태의 문구까지 확인한다.
- 우선순위: 높음.

### 2. 데스크톱 일보를 다른 업무 탭과 같은 가용 폭으로 확장

- 화면/영역: 데스크톱 `/mes?tab=dailyReport`의 상단 헤더, 본인 일보 작성 영역, MES 활동 영역, 전체 일보 목록.
- 증상: `DailyWorkReportScreen`이 `max-w-[1180px]`와 중앙 정렬을 사용해 1919×910 화면에서 양쪽 빈 공간이 과도하다. 이력·재고·출하 탭의 가용 본문 사용 방식과 다르다.
- 사용자 불편: 작성 영역과 활동 정보가 불필요하게 압축되고, 다른 업무 탭에서 이동했을 때 화면 밀도와 작업 공간이 단절된다.
- 확정 정책: 좌우 본문 여백은 데스크톱 셸의 표준 패딩만 남기고, 일보의 헤더와 본문 모두 가용 폭을 채운다. 모바일은 현재의 풀폭 흐름을 유지한다.
- 구현 방향: 중앙 최대 폭 제한만 제거하는 수준으로 시작하되, 넓어진 폭에서 작성 양식·자동 집계 표·전체 일보 목록의 열 비율을 재조정한다. 새 외곽 패널이나 장식용 배경을 추가하지 않고 기존 카드·토큰·그림자 문법을 따른다.
- 수용 기준:
  - 표준 데스크톱에서 좌우 여백이 이력·재고·출하 탭의 본문 여백과 같은 수준이다.
  - 상단 헤더만 넓고 작성/활동 카드가 좁게 남는 불일치가 없다.
  - 넓은 화면과 최소 지원 데스크톱 폭에서 표의 열, 긴 작업 내용, 날짜 선택, 저장 버튼, 활동 상세가 겹치거나 잘리지 않는다.
  - 라이트·다크 테마에서 토큰 대비, 카드 반경, 포커스·눌림 상태와 44px 조작 영역이 유지된다.
- 브라우저 확인 시나리오: 1919×910과 최소 지원 데스크톱 폭에서 내 일보·전체 일보를 각각 연다 → 이력 탭과 좌우 본문 여백을 비교한다 → 긴 텍스트·여러 활동 요약·상세 펼침 상태를 확인한다 → 모바일 폭에서는 가로 오버플로가 없는지 확인한다.
- 우선순위: 높음.

### 3. 엑셀 핵심 구조를 살린 단순 일일 작업 일보 양식으로 재설계

- 화면/영역: 본인 일보 작성·조회 화면과 MES 자동 활동 표시, 전체 일보 읽기 전용 화면.
- 증상: 기존 엑셀은 작성자에게 익숙한 헤더 정보, 모델별 행, 작업 내용·수량·불량·완료·비고, 하단 기타 사항으로 하루를 읽게 한다. 현재 MES 화면은 자유 서술 카드와 작업 종류별 자동 거래 카드가 분리되어 있어, 같은 일보의 대응 관계를 찾기 어렵다.
- 사용자 불편: 사용자는 엑셀에서 쓰던 내용을 어디에 적고 어떤 값이 자동으로 채워지는지 알기 어렵고, 기존 작성 습관을 버리고 새 화면 문법을 배워야 한다.
- 정책 확인 필요:
  - 모델 행은 기존 다섯 모델(DX3000·COCOON·ADX6000FB·ADX4000W·SOLO)을 고정으로 둘지, 해당 날짜에 MES 거래가 있는 모델만 동적으로 보일지 결정한다.
  - `총 작업 수량`, `불량 수량`, `실제 작업 완료`를 MES 거래에서 자동 산출할 범위와, 사용자가 직접 보정하거나 서술할 수 있는 범위를 결정한다. 현재 활동 API는 작업 종류별·단위별 요약만 제공하므로 모델별 일보 행이 필요하면 데이터 계약을 함께 확장해야 한다.
  - 과거 확정인 시간대별 직접 입력 없음·승인/제출/잠금 없음은 유지한다. 이를 되돌릴 의도가 있는 경우에만 별도 정책으로 확정한다.
- 구현 방향: 날짜·부서·작성자 자동 헤더를 엑셀의 읽는 순서에 맞추고, 모델·작업 내용·수량·불량·완료·비고/기타 사항을 한 개의 일보 표 또는 대응하는 요약 블록으로 재구성한다. MES 자동 값은 읽기 전용으로 출처를 표시하고, 사용자의 자유 서술은 `비고` 또는 `기타 사항`처럼 역할을 분명히 한다. 기존의 상세 거래는 표 행을 검산하는 보조 영역으로 내려, 주 양식과 경쟁하지 않게 한다.
- 수용 기준:
  - 현장 사용자가 기존 엑셀의 각 핵심 정보(날짜·부서·작성자·모델·작업내역·수량·불량·완료·비고/기타 사항)를 MES 화면의 한 위치와 일대일로 이해할 수 있다.
  - 사용자가 시간대별 행을 반복 입력하지 않아도 개인별 하루 1건을 저장·수정할 수 있다.
  - MES에서 자동 집계한 값과 직접 작성할 값의 구분·출처가 명확하며 같은 수량을 두 번 입력하지 않는다.
  - 본인 일보는 편집 가능하고 전체 일보의 타인 기록은 읽기 전용이며, 과거 날짜·미작성·거래 없음 상태가 명확하다.
  - 자동 수량·불량·완료 계산의 기준 거래와 취소 제외 규칙은 기존 활동 API와 상세 거래에서 검산 가능하다.
- 브라우저 확인 시나리오: 거래가 없는 날·생산/입출고/불량 거래가 섞인 날·과거 작성 일자를 각각 연다 → 헤더와 모델/작업/수량/비고의 대응을 엑셀 `02.02` 시트와 비교한다 → 자동값의 거래 상세를 펼쳐 검산한다 → 본인 저장·수정, 전체 일보의 타인 읽기 전용, 모바일 폭을 확인한다.
- 우선순위: 높음.
