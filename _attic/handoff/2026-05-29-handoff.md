# 세션 인수인계 (2026-05-29 오후)

다음 Claude 세션이 이 파일을 읽고 작업을 이어갈 수 있게 하기 위함.

## TL;DR — 현재 상태

- **방금 끝낸 작업**: ERP 파일 대규모 재배치 ("강제 위치 없는 자료는 모두 `_attic/`")
- **브랜치**: `cleanup-attic-20260529` (4 커밋, verify_local 통과)
- **main 머지/푸시**: 아직 안 함 (사용자 명시 요청 시에만)
- **사원님(권동환) 답장 대기**: 오늘 오전 메일 발송 — 코드 품질 가이드 요청 (본보기 PR · 1순위 작업 · 리뷰 순서 3문항)
- **사원님 2주 휴가 직전**: 답장 받기 전엔 백엔드 작업 보류, 답장 받으면 그 본보기 따라 2주간 작업

## 이번 세션 컨텍스트

사용자 김현우(Hw-03, 비전공자)는 사원님 휴가 직전 ERP 코드/파일 정리를 끝내두고 싶어함.

이번 세션 `/grill-me` 결과로 확정된 7가지 원칙:
1. 정리 범위: 어디든 옛/1회성 파일
2. 유지 기준: 강제 위치(도구 자동 참조) 아니면 attic
3. `docs/` 통째로 `_attic/docs/`
4. `vault/` 삭제 (빈 껍데기)
5. `ONBOARDING.md` → `_attic/`
6. `start.bat` 루트 유지 (사용자 매일 entry point)
7. 안전망 ① CLAUDE.md ③ README ④ MEMORY 갱신

**attic 정의 재정의**: "6개월 미참조 사본" → "강제 위치 없는 모든 자료의 보관소". [_attic/docs/ATTIC_POLICY.md](../docs/ATTIC_POLICY.md)

## 방금 끝낸 4 커밋 (`cleanup-attic-20260529`)

```
15afaa09  chore: _scratch_dead_scan.py → _attic/scripts/
787cd977  docs: 정리 후 위치 갱신 — CLAUDE·README·ATTIC_POLICY + backend-scripts/README 신설
b4b1cf16  chore: backend 1회성 스크립트 _attic 이전 + sys.path 패치
8dd5c9a9  chore: 루트 정리 — vault·mes.db 제거, docs·ONBOARDING → _attic
```

루트 listing 9개만 남음: `CLAUDE.md README.md _attic _dev backend docker frontend scripts start.bat`

## 자료 위치 (변경 후)

| 종류 | 위치 |
|---|---|
| 도메인 사전·가이드 | `_attic/docs/` (GLOSSARY/CONTEXT/ARCHITECTURE/ERD/ADR/OPERATIONS/ITEM_CODE_RULES/ATTIC_POLICY) |
| 1회성 backend 스크립트 | `_attic/backend-scripts/` (seed·sync·archive·backup 7개) |
| 1회성 분석 스크립트 | `_attic/scripts/` (DB 잔재 분석류 등) |
| DB 백업 | `_attic/data/db_backups/` (.gitignore 매칭 — 추적 X) |
| 신규 합류자 가이드 | `_attic/ONBOARDING.md` |
| 활성 DB | `backend/mes.db` 단일 |

자세한 건 [CLAUDE.md](../../CLAUDE.md) 의 "자료 위치" 섹션과 [_attic/backend-scripts/README.md](../backend-scripts/README.md).

## 남은 작업 후보

### A. 답장 전 안전하게 가능 (frontend·소량 작업)

**A-0. 머지·푸시** — 사용자 명시 요청 시
```bash
git checkout main
git merge --no-ff cleanup-attic-20260529 -m "2026-05-29 chore: 머지 — 파일 정리 (강제 위치 없는 자료 _attic 통합)"
git push origin main
git branch -d cleanup-attic-20260529
```

**A-1. start.bat DB 자동 변경 충돌 점검**
- 사용자 매일 더블클릭하는 `start.bat` 가 `bootstrap_db.py --schema --migrate --seed` 자동 실행
- CLAUDE.md 의 "Starting the server must not change the DB" 와 충돌
- 진짜 위험한지(시드가 사용자 데이터 덮어쓰는지) 확인 필요
- 위험하면 패치, 안전하면 메모로만

**A-2. Frontend deepening 3개** (이전 `/improve-codebase-architecture` 발견)
- BOM 동기화 정책 모듈 추출 (`frontend/app/legacy/_components/_warehouse_v2/bomSync.ts` + `ioWorkType.ts:isBomForced`)
- Batch 라벨 렌더링 모듈 (`frontend/lib/io/labelRenderer.ts` 신설)
- 부서 선택 규칙 모듈 (`frontend/lib/io/departmentSelection.ts` 신설)
- 다 frontend-only · surgical · 사원님 영역 안 건드림
- 참고: [improve-codebase-architecture-2026-05-29-summary](../docs/improve-codebase-architecture-2026-05-29-summary.md)

### B. 답장 후 시작 (보류 — 사원님 영역)

| 항목 | 보류 이유 |
|---|---|
| P0-3 서버측 identity/권한 검증 | 인증 방식 결정 필요 (PIN 확장 / 세션 / JWT) |
| P1-4 `services/io.py` god-service 분리 (1,150줄) | PR 순서·테스트 안전망 결정 필요 |
| P1-5 Decimal/UTC 직렬화 정상화 | 수량 정밀도 결정 필요 |
| P2-2 백엔드 auth boundary negative test | P0-3 후 |
| P3-2 `IoComposeView` 책임 분리 (970줄) | E2E 시드 전략 후 |

## 사원님께 보낸 메시지 (오늘 오전)

```
사원님, 다음 주부터 2주간 자리 비우신다고 들었어요.
그동안 제가 AI 활용해서 프로젝트 코드 작업을 좀 해두려고 합니다.

세 가지만 여쭤보고 싶어요.

1. 사원님이 직접 짜신 코드 중에 "이런 식으로 가면 된다" 하는
   본보기 PR 하나만 알려주실 수 있을까요?
   그걸 보고 같은 스타일로 진행하려 합니다.

2. 휴가 동안 1순위로 정리되면 좋겠는 게 뭘까요?

3. 복귀하셔서 리뷰하실 때 어떤 순서로 보실 건가요?
   미리 어떤 식으로 정리해두면 보기 편하실까요?

사원님이 원하시는 방향대로 미리 정리해보려고요.
복귀하셔서 리뷰하시기도 편하고, 안 그래도 바쁘신데
사원님 일도 조금이나마 줄지 않을까 싶어서요.
```

답장 오면 그걸 기준으로 작업 우선순위·스타일 정렬.

## 다른 세션이 동시 진행 중인 작업 (조심)

이번 세션 시작 전 git status 에 modified 16건 + scratch 6개가 있었음. 사용자 본인 작업 아님 → 다른 Claude 세션이 진행 중인 작업. 본 정리 작업 시작 직전에 다른 세션이 다음을 처리:
- `42c53eb7 db: M1 잔재 3종 제거 — variance_logs / process_flow_rules / items.symbol_slot`
- `_scratch_db_audit/orphan_check/pfr_dump/verify.py` 4개 정리

후속 분석용 `_scratch_dead_scan.py` 만 남았었고, 우리가 `_attic/scripts/` 로 보관.

→ **다른 세션이 또 무언가 진행 중일 가능성**. 새 작업 시작 전 `git status` 로 unstaged 확인하고, 다른 세션 작업이 보이면 사용자에게 확인 후 진행.

## 1차 코드베이스 개선 작업 (이미 main 머지됨)

오늘 오전 별도 작업 — main 에 머지·푸시 완료. 참고용:

```
b4c4a2f0  refactor: 머지 — 코드베이스 아키텍처 개선
1f141ef2  docs: 주간보고·개발현황 5/29 갱신
48a984fc  docs: 자율 실행 결과 요약 + 보류 항목 정리
1ccdb2ae  frontend: Playwright repo-native E2E 스캐폴딩 (P2-1)
61b0c5b1  frontend: IoDraftWorkCard nested interactive 제거 (P3-1)
1ef4b7c2  docs: ADR 디렉터리 + CONTEXT.md 도입 (P1-2)
7b4c9a06  docs: GLOSSARY 에 출하 규칙 + 트랜잭션 라벨 변경 반영 (P0-2)
2d67cce0  frontend: 출입고 도메인 라벨 단일 사전 도입 (P0-1) ★
d3ec7e3a  docs: OpenAPI baseline 경로 정합
8814bf68  docs: 핵심 문서 _attic 에서 복귀
17a09d06  docs: _attic/_backup 정책 문서 추가
```

★ P0-1 라벨 단일 사전 = `frontend/lib/io/glossary.ts`. 화면 라벨 drift 방어 단일 소스.

## 롤백 방법

이 cleanup 작업 통째 되돌리기:
```bash
git checkout main
git branch -D cleanup-attic-20260529
```

또는 단계별 revert:
```bash
git revert 15afaa09  # _scratch_dead_scan 만 되돌리기
git revert 787cd977  # 안전망(CLAUDE/README/ATTIC_POLICY) 되돌리기
git revert b4b1cf16  # backend 1회성 .py 이동 되돌리기
git revert 8dd5c9a9  # 루트 정리 되돌리기
```

## 참고 파일

- 이번 정리 plan: `C:\Users\user\.claude\plans\improve-codebase-architecture-generic-lightning.md`
- attic 정책: [_attic/docs/ATTIC_POLICY.md](../docs/ATTIC_POLICY.md)
- 도메인 한눈 보기: [_attic/docs/CONTEXT.md](../docs/CONTEXT.md)
- 1회성 스크립트 실행 가이드: [_attic/backend-scripts/README.md](../backend-scripts/README.md)
- 사용자의 auto-memory: `C:\Users\user\.claude\projects\c--ERP\memory\MEMORY.md` (옛 `docs/` 경로 3건 갱신됨)
