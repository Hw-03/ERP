# 출하 탭 수정 TODO

작성일: 2026-07-15
상태: 구현 후 점검 중

## 전체 현황
- 구현 완료 기준 TODO: 12개
- 구현해야 할 후속 TODO: 0개
- 대체·보류 TODO: 2개
- 총 관리 TODO: 14개
- 확정 후속 TODO: 0개
- 에이전트 발굴 추가 TODO: 0개
- 사용자 추가 TODO: 14개
- 출하 관리: 14개
- 우선순위: 높음 0개 / 중간 0개 / 낮음 0개
- 구현 전 정책 확인 필요: 1개
- 문서 목적: 출하 탭 수정사항의 구현 결과를 점검하고, 완료 결과와 동결 정책 충돌 1건을 분리해 관리한다.

## 참고한 기준과 근거
- 기존 출하 PC UI TODO: `_attic/handoff/2026-07-13-shipping-ui-followup-todo.md`
- 현재 수집 화면: 출하 탭 > 출하 요청 작성
- 브라우저 확인: 수집 항목 추가 시 해당 화면 상태와 선택값을 함께 기록한다.

## 작업 원칙
- 이 문서는 구현용 계획이 아니라 수정사항 수집용이다.
- 사용자 요청이 있기 전까지 이 문서의 항목을 구현하지 않는다.
- 기존 출하 TODO와 같은 문제로 확인되면 중복 등록하지 않고, 기존 항목과의 관계를 명시한다.
- 모바일 출하 화면, 모바일 하단 탭바 frozen 영역, 주간보고 frozen 영역은 명시 요청 없이는 범위에 넣지 않는다.

## 확정 결정 요약
- 출하 요청 작성의 품목코드는 화면 단계와 목록 종류가 달라도 공정 코드(PR·PA·PF·AR·AA·AF 포함 18개)를 같은 색상표로 구분한다. PA=주황, PF=파랑, AA=자홍, PR=보라를 사용한다.
- 적용 범위는 기준 PF 선택, BOM 구성 조정, BOM 매칭, 최종 확인의 출하 품목·BOM·동반 출하품이다.
- 기존 계층 규칙은 유지한다. PA 구성품에는 AF가, PF 구성품에는 PA가 포함된다.
- BOM에 추가한 품목과 최종 확인 목록 모두 표준 BOM 정렬 순서를 사용한다.
- 5단계 카드 높이·열 수·스크롤은 동결한다. `BOM·동반 출하품`은 남은 높이와 내부 스크롤을, `변경된 구성품`은 58px 한 행·2열과 내부 스크롤을 유지한다.
- 신규 PA/PF 예상 코드는 조회 시점의 비예약 참고값으로만 표시하고, 저장 시 변경 가능 안내 문구는 화면에서 숨긴다.
- 최종 확인의 예상 코드는 품명 가까이 배치한다. 출하 수량은 하단 `출하 요청` 버튼 바로 왼쪽에 둔다.
- 신규 PA·PF가 함께 생성될 때 PF 구성품의 첫 행은 부모 PF가 아니라 새 PA다. PF만 새로 생성되는 경우에는 기존 PA BOM 행만 사용하며 부모 PF를 구성품 행으로 추가하지 않는다.

## 구현 결과
- 1·2·3·5단계의 코드 표시는 18개 공정 색상 공통 컴포넌트로 통일했다. PR=보라, PA=주황, PF=파랑, AA=자홍, AF=초록 규칙을 사용한다.
- 2단계와 5단계 BOM 행은 부서·공정·일련번호·MES 코드·품목 ID 순의 표준 BOM 정렬을 사용한다.
- 5단계 AF/PA 목록 제목은 각각 PA/PF 품목명으로 표시한다. 새 PA·PF가 함께 생성되면 PF 구성품 목록 첫 행에는 새 PA를 표시하며, 부모 PF는 중복 표시하지 않는다.
- 2단계 구성품 제목은 기준 PF 품명과 PA/PF 구분을 함께 표시한다.
- 3·5단계의 신규 PA/PF에는 비예약 예상 MES 코드만 표시하며, `예상 코드 · 저장 시 변경 가능` 안내 문구는 표시하지 않는다.
- 3단계의 중복 변경 구성 요약 카드는 제거하고 하단 상세 목록을 유지한다.
- 5단계 카드 구조는 동결 기준으로 전환했다. 기존 TODO 11의 420px 고정 높이·자연 높이 변경 요구는 동결 정책과 충돌하므로 보류한다.

## 출하 관리

### 1. 출하 요청 전체의 PR·PA·AF·PF 품목코드 색상 구분 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성의 1단계 기준 PF 검색 결과, 2단계 PA·PF 구성품 편집 목록, 3단계 매칭 결과, 5단계 출하 품목 및 `BOM·동반 출하품` 요약 목록
- 증상: 동일한 품목코드가 화면마다 기본 보조 텍스트 색으로 표시된다. 특히 5단계에는 PR·PF·AF만 별도 색상 처리하는 `SummaryCode`가 있으나 PA는 기본값으로 떨어지고, 1·2·3단계의 동일한 코드 표시는 이 규칙을 사용하지 않는다.
- 사용자 불편: `3-PR-…`, `3-PA-…`, `3-AF-…`, `3-PF-…`의 공정 구간을 코드 전체를 읽기 전에는 빠르게 구분하기 어렵고, 같은 품목코드가 단계마다 서로 다른 강조 수준으로 보인다.
- 확정 정책: 공정 코드가 포함된 MES 품목코드는 표시 위치와 관계없이 가운데 공정 구간(PR·PA·AF·PF)을 색으로 구분한다. 품목명·수량·상태 배지의 기존 의미와 색은 바꾸지 않는다.
- 정책 확인 필요: 네 코드의 최종 색상표를 확정한다. 현재 5단계의 기존 규칙은 PR=보라, AF=초록, PF=파랑이고 PA는 기본 파랑으로 처리된다. PA까지 다른 코드와 구분되는 색을 지정한 뒤 전 화면에 같은 표를 사용한다.
- 구현 방향: `mes_code`를 직접 텍스트로 렌더하는 1·2·3·5단계의 코드 표시를 공통 코드 프레젠테이션으로 통일한다. `NN-공정코드-NNNN` 형식일 때 가운데 공정코드만 강조하고, 비표준 코드·코드 없음·자동 생성 예정 안내는 기존 보조 텍스트 문법을 유지한다. 확인된 주요 렌더 경로는 `DesktopShippingView.tsx`의 PF 검색 결과(1498줄), BOM 편집 목록(2211줄), BOM 매칭 결과(2507~2513줄), 최종 확인 목록(2627줄), 출하 품목 헤더(2648줄)이다.
- 수용 기준:
  - PR·PA·AF·PF 코드가 섞여 표시되어도 각 공정코드 구간이 확정된 색상표에 따라 서로 구분된다.
  - 1·2·3·5단계에서 같은 MES 품목코드는 같은 공정코드 색을 사용한다.
  - 5단계 `BOM·동반 출하품`의 PA 코드가 PF 기본색으로 보이지 않고, 확정된 PA 색으로 표시된다.
  - 코드의 앞 모델 번호와 뒤 일련번호, 품목명, 수량, `기본`·`추가됨`·`제외됨` 상태 색은 기존 표시를 유지한다.
  - 비표준 코드, 코드 없음, `품목코드는 저장/준비 완료 시 자동 생성 예정` 문구는 오류 없이 기존 보조 텍스트로 보인다.
- 브라우저 확인 시나리오:
  1. 1단계 PF 검색 결과에서 `3-PF-0001`의 PF 구간 색을 확인한다.
  2. 2단계 PA 구성품의 `3-AF-0002`, PF 구성품의 `3-PA-0002`와 PR 코드가 있는 행을 함께 확인한다.
  3. 3단계 매칭 결과의 PA·PF 코드와 5단계 출하 품목 코드가 같은 규칙으로 보이는지 확인한다.
  4. 5단계 `BOM·동반 출하품`에서 PR·PA·AF·PF 코드가 섞인 목록을 확인하고, 코드 외 정보와 상태 배지가 바뀌지 않았는지 확인한다.
- 우선순위: 중간

### 2. 추가 BOM 품목과 최종 확인 목록을 표준 BOM 순서로 정렬 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 2단계 `BOM 구성 조정`의 PA·PF 구성품 목록, 5단계 `BOM·동반 출하품`의 PA·PF 목록
- 증상: 2단계에서 새로 추가한 품목은 현재 목록의 마지막에 붙는다. 5단계도 드래프트 배열의 현재 순서를 그대로 사용해, 기준 BOM과 추가·제외 항목이 표준 BOM 순서와 다르게 보일 수 있다.
- 사용자 불편: BOM 편집 직후 추가한 품목을 찾기 어렵고, 2단계에서 확인한 구성 순서와 최종 확인의 순서가 달라질 수 있다.
- 확정 정책: PA 구성품에는 AF가, PF 구성품에는 PA가 포함되는 기존 계층을 유지한다. 각 구성품 목록은 기존 BOM 표준 정렬을 따른다. 새로 추가한 항목도 같은 정렬 기준으로 재배치되어 해당 코드 순서상 첫 항목이면 목록 맨 위에 표시된다.
- 구현 방향: 프런트 드래프트에 새 행을 단순 append하지 않고, 백엔드 `bom_child_item_ordering()`의 표준 순서(부서 → 공정 단계 F·A·R → 일련번호 → MES 코드)를 프런트 표시·저장 순서의 단일 기준으로 사용한다. 2단계의 PA·PF 목록과 5단계 최종 확인 목록은 같은 정렬 함수를 거친 데이터를 렌더한다. 제외된 항목도 원래 BOM 상대 순서를 보존한다.
- 수용 기준:
  - AF 항목을 PA 구성품에 추가하면 표준 BOM 순서상 위치(예: AF가 선행이면 맨 위)에 즉시 표시된다.
  - PF 구성품에 PA 항목을 추가해도 같은 기준으로 재정렬된다.
  - 2단계의 포함·제외·추가 행 순서와 5단계 최종 확인 목록의 순서가 일치한다.
  - 동일 정렬 키일 때는 품목코드와 품목 ID까지 사용해 새로고침·재진입 후에도 순서가 흔들리지 않는다.
  - 수량, 추가됨·기본·제외됨 상태, 수정 기능은 기존과 동일하게 유지된다.
- 브라우저 확인 시나리오:
  1. 2단계 PA 구성품에 AF 코드 품목을 추가하고 즉시 목록 맨 위 또는 표준 순서 위치로 이동하는지 확인한다.
  2. PA·PF 양쪽에 여러 코드 종류를 추가·제외한 뒤 목록 순서가 표준 BOM 순서인지 확인한다.
  3. 5단계로 이동해 같은 구성품이 2단계와 동일 순서로 표시되는지 확인한다.
  4. 요청을 저장 후 다시 열어도 순서가 유지되는지 확인한다.
- 우선순위: 중간

### 3. 최종 확인의 새 PF 배치과 구성품 제목을 실제 품목명으로 표시 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 5단계 `BOM·동반 출하품`의 PA·PF 두 목록과 자동 생성 예정 품목 행
- 증상: 새 대상 PF가 자동 생성될 상황에서 `품목코드는 저장/준비 완료 시 자동 생성 예정` 행이 PA 구성품의 맨 위에 표시될 수 있다. 또한 두 목록 제목은 고정된 `PA 구성품`·`PF 구성품`이라 현재 어떤 PA/PF 품목의 구성인지 제목만으로 식별하기 어렵다.
- 사용자 불편: 최종 확인에서 새로 생성될 출하 PF가 어느 구성 목록에 속하는지 오해할 수 있고, 실제 품목명을 확인하려면 목록 행을 다시 찾아야 한다.
- 확정 정책: PA 구성품에는 AF, PF 구성품에는 PA가 포함되는 기존 BOM 계층은 변경하지 않는다. 자동 생성될 최종 PF는 PF 구성품 목록의 첫 행에서 표시한다. 두 구성품 목록의 고정 제목 대신 각 목록이 나타내는 실제 품목명을 표시한다.
- 확정 정책: AF 목록의 제목에는 PA 품목명을, PA 목록의 제목에는 PF 품목명을 표시한다. 새 PA와 새 PF가 동시에 생성되는 경우에도 이 부모-자식 관계와 제목 규칙을 유지한다.
- 구현 방향: 최종 확인의 새 항목을 `new PA`와 `new PF`로 구분해 렌더하며, 새 PF는 PF 목록 맨 위에 둔다. AF 목록은 PA 품목명, PA 목록은 PF 품목명과 생성 예정 상태를 제목으로 표시하고, 기존 정적 명칭은 보조 정보로만 남기거나 제거한다. 현재 `FinalRequirementReview`는 `newPaName` 행만 PA 그룹으로 보내므로, 생성 유형과 그룹을 분리한다.
- 수용 기준:
  - 새 PF가 필요한 요청의 자동 생성 예정 행은 PF 구성품 목록 맨 위에 표시된다.
  - 새 PF 행이 PA 구성품 목록에 잘못 나타나지 않는다.
  - AF 목록 제목에는 PA 품목명, PA 목록 제목에는 PF 품목명이 표시되어 어떤 상위 품목을 기준으로 한 구성인지 알 수 있다.
  - PA 구성품의 AF, PF 구성품의 PA 포함 구조와 기존 BOM 행·수량은 바뀌지 않는다.
  - 새 PA와 새 PF가 동시에 필요한 요청에서도 두 생성 예정 항목의 이름·그룹·순서가 구분된다.
- 브라우저 확인 시나리오:
  1. 기준 PF를 다른 출하 대상용으로 변경해 새 PA·PF가 필요한 요청을 만든다.
  2. 5단계에서 새 PF 이름과 자동 생성 안내가 PF 구성품의 첫 행인지 확인한다.
  3. AF 목록에는 PA 품목명, PA 목록에는 PF 품목명이 제목으로 표시되는지 확인한다.
  4. PA 구성품의 AF와 PF 구성품의 PA가 계속 포함되고, 각 목록의 순서가 2단계와 일치하는지 확인한다.
- 우선순위: 높음

### 4. 최종 확인의 BOM·변경 구성품 카드 높이와 스크롤 복구 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 5단계 `저장 및 전환`의 `BOM·동반 출하품`, `변경된 구성품`, 해당 본문 스크롤 영역
- 최종 결정: 현재 BOM 카드의 424px 실측 높이를 기준으로 `BOM·동반 출하품`은 432px 고정 높이를 사용한다. `변경된 구성품`은 후속 결정에 따라 자연 높이를 사용한다.
- 사용자 재확정: 변경된 구성품이 하나라도 있으면 `BOM·동반 출하품` 박스 높이는 내용량과 무관하게 항상 432px 고정으로 유지한다.
- 구현 방향: `shipping-final-summary`는 출하 품목 헤더, 432px 고정 BOM 카드, 자연 높이 변경 카드의 행 구조를 사용한다. BOM 목록과 외곽 본문은 각각 세로 스크롤하고, 하단 action bar는 고정한다.
- 수용 기준:
  - BOM 변경이 있을 때 `BOM·동반 출하품` 카드의 외곽 높이가 432px로 유지된다.
  - BOM 카드의 목록은 내부 스크롤되고, 변경 카드는 행 수에 맞는 자연 높이를 사용한다.
  - 출하 품목 헤더와 두 카드를 합친 높이가 본문보다 크면 5단계 본문에서 세로 스크롤할 수 있다.
  - 본문 스크롤 중에도 하단 `이전`·`출하 요청` action bar는 화면 하단 위치와 동작을 유지한다.
  - BOM 변경이 없는 경우에는 기존처럼 `BOM·동반 출하품`이 남는 높이를 사용하고, 빈 변경 구성품 카드를 만들지 않는다.
- 브라우저 확인 시나리오:
  1. 변경 구성품이 3개 이상인 요청을 5단계까지 진행한다.
  2. `BOM·동반 출하품` 카드가 432px인지, `변경된 구성품` 카드가 행 수에 맞는 자연 높이인지 확인한다.
  3. BOM 카드에 충분한 행을 만들어 내부 목록이 스크롤되고, 변경 카드가 바깥 본문 스크롤을 사용하는지 확인한다.
  4. 5단계 본문을 스크롤해 두 카드 전체를 확인하고, 하단 action bar가 고정되는지 확인한다.
  5. BOM 변경 없는 요청에서도 본문 높이·스크롤과 출하 요청 동작이 정상인지 확인한다.
- 우선순위: 높음

### 5. BOM 구성 조정의 두 구성품 제목에 기준 PF 품명 표시 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 2단계 `BOM 구성 조정`의 `PA 구성품 N개 포함`, `PF 구성품 N개 포함` 제목
- 수집 상태: 구현 완료
- 증상: 두 제목이 고정된 `PA 구성품`·`PF 구성품`과 개수만 표시한다. 현재 어느 기준 PF를 대상으로 BOM을 조정하는지 제목만으로 알 수 없다.
- 사용자 결정: 두 구성품 제목 모두 1단계에서 선택한 기준 PF의 실제 품목명을 표시한다. PA/PF 구성품 구분과 포함 개수 표시는 기존처럼 유지한다.
- 기존 항목과의 관계: 완료된 3번은 5단계 최종 확인 목록의 실제 품목명 제목 표시를 다룬다. 이 항목은 2단계 BOM 구성 조정 제목에만 적용한다.
- 구현 방향: 2단계의 두 `BomEditor`에 현재 기준 PF 품목명을 전달해, 제목에 품목명과 기존 구성품 구분·개수를 함께 표시한다. 기준 PF를 선택하지 않은 빈 상태와 PA/PF BOM 행의 계층·정렬·편집 동작은 변경하지 않는다.
- 수용 기준:
  - 1단계에서 기준 PF를 선택하면 PA 구성품과 PF 구성품 두 제목 모두 그 PF의 실제 품목명을 표시한다.
  - 두 제목에서 PA/PF 구성품 구분과 포함 개수는 계속 식별할 수 있다.
  - 기준 PF를 다른 품목으로 바꾸면 두 제목의 품명도 새 기준 PF로 즉시 갱신된다.
  - 기준 PF 미선택 상태의 안내와 BOM 구성품 행, 검색·추가·제외 동작은 기존과 동일하다.
- 브라우저 확인 시나리오:
  1. 1단계에서 기준 PF를 선택하고 2단계로 이동한다.
  2. PA 구성품과 PF 구성품 두 제목에 같은 기준 PF 품명이 표시되는지, PA/PF 구분 및 포함 개수가 유지되는지 확인한다.
  3. 이전 단계에서 다른 기준 PF로 변경한 뒤 재진입해 두 제목이 함께 갱신되는지 확인한다.
- 우선순위: 중간

### 6. 자동 생성 예정 품목의 예상 MES 코드 표시 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 3단계 `BOM 매칭`의 `새 PA 생성 예정`·`새 PF 생성 예정` 카드, 5단계 `저장 및 전환`의 자동 생성 예정 품목 행 및 출하 품목 헤더 안내
- 수집 상태: 구현 완료
- 증상: 새 PF/PA가 필요한 경우 `품목코드는 저장/준비 완료 시 자동 생성 예정`이라는 안내만 표시되어, 어떤 공정 코드와 일련번호의 품목이 생성될지 최종 확인 단계에서 판단할 수 없다.
- 사용자 요청: 3단계와 5단계의 자동 생성 예정 안내 모두에 예상 MES 품목코드를 `NN-공정코드-NNNN 예정` 형식으로 표시한다.
- 확정 정책: 저장 전 예상 코드는 기준 PF의 모델 기호와 PA/PF별 다음 일련번호로 산출하는 비예약 참고값이다. 다른 품목이 먼저 생성되면 실제 저장 코드가 달라질 수 있으므로 `예상 코드 · 저장 시 변경 가능`을 함께 표시한다.
- 기존 항목과의 관계: 완료된 1번은 기존 MES 코드의 공정 구간 색상 표시를 다룬다. 이 항목은 아직 생성되지 않은 품목의 예상 코드 표시를 추가한다. 완료된 3번의 자동 생성 예정 행 위치·제목 정책은 유지한다.
- 구현 방향: 자동 생성 대상의 공정 코드와 코드 생성 규칙을 이용해 예상 MES 코드를 산출하고, 3단계 새 PA/PF 생성 예고 카드 및 5단계 기존 자동 생성 안내와 함께 렌더한다. 실제 저장 과정에서 코드가 달라질 수 있다면 그 사실을 화면 문구로 명확히 알린다.
- 수용 기준:
  - 3단계 새 PA/PF 생성 예고 카드와 5단계 자동 생성 예정 PA/PF 행에서 예상 MES 코드와 `예정` 상태를 확인할 수 있다.
  - 예상 코드의 공정 구간은 생성 대상(PA 또는 PF)과 일치한다.
  - 새 PA와 새 PF가 동시에 필요한 경우 각 행의 예상 코드가 구분되어 표시된다.
  - 코드가 아직 확정되지 않은 사실과 저장 시 변경 가능 여부는 확정 정책에 따라 오해 없이 안내된다.
  - 기존 생성 예정 행의 그룹·정렬·품목명 표시와 저장 후 실제 품목코드 생성 동작은 변경하지 않는다.
- 브라우저 확인 시나리오:
  1. 새 PA 또는 PF가 필요한 출하 요청을 만들고 3단계와 5단계로 각각 이동한다.
  2. 3단계 새 PA/PF 생성 예고 카드와 5단계 자동 생성 예정 행에 대상 공정과 일치하는 예상 코드가 `예정` 상태로 표시되는지 확인한다.
  3. 새 PA와 PF가 모두 필요한 요청에서 두 예상 코드가 각각의 카드·행·그룹에 표시되는지 확인한다.
  4. 저장 후 실제 생성된 코드가 화면 정책(예약 코드 또는 참고값)에 맞는지 확인한다.
- 우선순위: 중간

### 7. 변경된 구성품 카드 높이를 행 수에 맞게 조정 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 5단계 `저장 및 전환`의 `변경된 구성품` 카드
- 수집 상태: 구현 완료
- 증상: 변경 구성품이 4개여서 두 줄만 표시되어도 카드가 고정 높이를 차지해, 마지막 행 아래에 불필요한 빈 공간이 생긴다.
- 사용자 결정: `변경된 구성품` 카드는 고정 높이를 사용하지 않고, 표시할 변경 행 수에 맞게 유동 높이로 표시한다.
- 기존 항목과의 관계: 완료된 4번의 `BOM·동반 출하품` 432px 고정 높이 정책은 유지한다. 이 항목은 같은 화면의 `변경된 구성품` 카드에만 적용한다.
- 구현 방향: 변경 구성품 카드의 432px 고정 높이 및 내부 고정 스크롤을 제거하고, 카드 본문이 변경 행의 실제 줄 수만큼만 높이를 갖게 한다. 5단계 바깥 본문 스크롤과 하단 action bar 고정 동작은 유지한다.
- 수용 기준:
  - 변경 구성품이 4개로 두 줄이면 카드 높이가 두 줄과 제목·여백에 필요한 만큼만 표시되고, 하단에 불필요한 빈 공간이 남지 않는다.
  - 변경 구성품 행 수가 늘거나 줄면 카드 높이도 그에 맞게 함께 변한다.
  - `BOM·동반 출하품` 카드는 변경 구성품 유무와 관계없이 기존 432px 고정 높이를 유지한다.
  - 변경 구성품이 많아져 5단계 본문보다 길어지면 바깥 본문에서 스크롤할 수 있으며, 하단 action bar는 고정된다.
  - 변경 구성품이 없는 경우 기존처럼 해당 카드는 표시하지 않는다.
- 브라우저 확인 시나리오:
  1. 변경 구성품이 4개인 요청을 5단계까지 진행한다.
  2. 변경 구성품 카드가 두 행 높이만 사용하고 마지막 행 아래에 큰 빈 공간이 없는지 확인한다.
  3. 변경 행 수를 추가·제외해 카드 높이가 함께 변하는지 확인한다.
  4. 같은 화면에서 `BOM·동반 출하품` 카드가 계속 432px 고정인지, 하단 action bar가 고정인지 확인한다.
- 우선순위: 중간

### 8. BOM 매칭의 변경 구성 요약 카드 제거 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 3단계 `BOM 매칭` 상단의 `N개 변경 구성`·`구성 변경 내용을 확인했습니다.` 카드
- 수집 상태: 구현 완료
- 증상: 상단 요약 카드는 변경 개수와 일반 안내만 보여주며, 바로 아래에 변경 품목의 실제 내용이 표시되는 `변경된 구성품` 목록과 정보가 중복된다.
- 사용자 결정: 3단계 상단의 변경 구성 요약 카드를 제거한다. 하단 `변경된 구성품` 목록을 변경 내용 확인의 단일 표시 영역으로 사용한다.
- 구현 방향: 상단 그리드에서 변경 구성 요약 카드와 관련 문구를 제거하고, 새 PA/PF 생성 예고 카드 및 출하 수량 영역의 기존 동작은 유지한다. 변경 품목 데이터와 하단 목록 렌더링은 변경하지 않는다.
- 수용 기준:
  - 3단계에 `N개 변경 구성`·`구성 변경 내용을 확인했습니다.` 요약 카드가 표시되지 않는다.
  - 변경된 구성품이 있으면 하단 `변경된 구성품` 목록에서 품목·코드·수량·제외 상태를 계속 확인할 수 있다.
  - 새 PA/PF 생성 예고 카드, 출하 수량 변경, 다음 단계 진행 동작은 기존과 동일하다.
  - 변경 구성품이 없는 요청에서도 3단계 레이아웃과 다음 단계 진행이 정상이다.
- 브라우저 확인 시나리오:
  1. 변경 구성품이 있는 요청으로 3단계에 진입한다.
  2. 상단 변경 구성 요약 카드가 없고, 하단 목록에 변경 상세가 계속 표시되는지 확인한다.
  3. 새 PA/PF 생성 예고와 출하 수량 변경 후 다음 단계 이동이 정상인지 확인한다.
- 우선순위: 중간

### 9. 최종 확인 PF 구성품에 부모 PF가 중복 표시되는 계층 오류 수정 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 5단계 `저장 및 전환` > `BOM·동반 출하품`의 PF 구성품 목록
- 증상: 새 PA·PF가 함께 생성되는 요청에서 새 PF의 품목명·예상 코드가 PF 구성품 목록의 첫 행으로 다시 표시된다. 부모 PF가 자기 자신의 자식 구성품처럼 보인다.
- 사용자 불편: 실제 BOM 계층(PA 구성품에는 AF, PF 구성품에는 PA)과 화면이 달라 출하 전 구성품을 잘못 판단할 수 있다.
- 확정 정책: 새 PA·PF가 함께 생성될 때 PF 구성품 목록 첫 행은 새 PF가 아니라 새 PA를 표시한다. PF만 새로 생성되고 PA를 재사용하는 경우에는 기존 PF BOM의 PA 행을 그대로 사용하며, 부모 PF를 별도 구성품 행으로 추가하지 않는다.
- 구현 방향: 최종 확인의 특수 생성 예정 행을 `new PF`가 아니라 `new PA`로 PF 구성품 목록에 전달한다. PA·PF 생성 여부별로 행을 분기하되, 실제 저장 시 PA 생성 후 PF BOM이 해당 PA를 참조하는 백엔드 규칙은 변경하지 않는다.
- 수용 기준:
  - 새 PA·PF가 함께 필요한 요청에서 PF 구성품 첫 행이 새 PA의 품명·예상 코드·수량으로 표시된다.
  - 부모 PF의 품명·예상 코드가 PF 구성품 행으로 중복 표시되지 않는다.
  - PF만 새로 생성되는 요청에서는 재사용 PA와 기존 PF BOM 행이 유지되며 부모 PF 행이 추가되지 않는다.
  - PA 구성품의 AF, PF 구성품의 PA라는 기존 계층과 실제 저장 BOM 생성 규칙이 바뀌지 않는다.
- 브라우저 확인 시나리오:
  1. BOM을 변경해 새 PA와 새 PF가 모두 필요한 요청을 5단계까지 진행한다.
  2. PF 구성품 첫 행이 새 PA이고 부모 PF가 행으로 중복되지 않는지 확인한다.
  3. PA를 재사용하고 PF만 새로 만드는 요청도 확인해 기존 PA 행과 부모 PF 비중복을 검증한다.
  4. 요청을 저장한 뒤 생성된 PF BOM이 생성·재사용 PA를 자식으로 참조하는지 확인한다.
- 우선순위: 높음

### 10. 최종 확인의 예상 코드·출하 수량 정보 위치와 한 줄 안내 정리 — TODO 12로 대체
- 상태: 코드 안내 문구 숨김 정책과 상단 품목 코드 배치는 TODO 12의 최종 정책으로 통합한다. 출하 수량의 하단 action bar 배치는 구현 완료했다.
- 화면/영역: PC 출하 > 출하 요청 작성 > 5단계 `저장 및 전환`의 출하 품목 요약, PA·PF 구성품 제목·행, 하단 action bar
- 증상: 출하 품목의 예상 코드가 품명과 멀리 떨어져 있고, 출하 수량이 상단 오른쪽에 있어 하단 `출하 요청` 동작과 분리돼 보인다. PA·PF 구성품 제목에는 예상 코드가 없고, 구성품 행의 `예상 코드 · 저장 시 변경 가능` 안내가 품목 코드 다음 줄을 추가로 차지한다.
- 사용자 불편: 신규 PA/PF의 코드와 상태를 품명 가까이에서 빠르게 확인하기 어렵고, 한 행으로 읽을 수 있는 코드 안내 때문에 카드 높이가 불필요하게 늘어난다.
- 확정 정책: 출하 품목 요약은 품명 가까이에 예상 코드를 둔다. 신규 PA·PF 구성품 제목에도 각 예상 코드를 표시하되, `예상 코드 · 저장 시 변경 가능` 안내 문구는 표시하지 않는다. 구성품 행에서도 해당 안내 문구는 표시하지 않는다. 출하 수량은 하단 action bar의 `출하 요청` 또는 `변경사항 반영` 버튼 바로 왼쪽에 둔다. 이 문구 숨김 정책은 후속 TODO 12의 사용자 확정 결정으로 갱신한다.
- 구현 방향: 최종 확인 요약의 flex 배치를 재구성해 코드 영역을 품명 뒤로 이동하고, 하단 action bar에 수량 전용 슬롯을 추가한다. 구성품 그룹 제목에 생성 예정 코드 정보를 전달하고, 행의 코드·안내를 줄바꿈 없는 보조 정보 행으로 렌더한다. 기존 색상 토큰·코드 공정 구간 강조·긴 품명 말줄임 규칙은 유지한다.
- 수용 기준:
  - 상단 출하 품목에서 예상 코드가 품명 가까이에 표시되고, 출하 수량은 하단 요청 버튼 바로 왼쪽에 표시된다.
  - 새 PA·PF가 있는 경우 각 구성품 제목 옆에서 대상 예상 코드와 저장 시 변경 가능 안내를 확인할 수 있다.
  - 생성 예정 구성품 행의 예상 코드 안내가 품목 코드 오른쪽 한 줄에 표시된다.
  - 긴 품명·코드에서도 수량, 버튼, 코드 안내가 겹치거나 가로 스크롤되지 않는다.
  - 기존 PA/PF 재사용 요청에서는 예상 코드 안내가 불필요하게 표시되지 않는다.
- 브라우저 확인 시나리오:
  1. 새 PA·PF가 필요한 요청을 5단계까지 진행한다.
  2. 상단 출하 품목의 품명·예상 코드 위치와 하단 출하 수량 위치를 확인한다.
  3. PA·PF 구성품 제목과 생성 예정 행에서 코드·안내가 한 줄 문법으로 표시되는지 확인한다.
  4. 긴 품명과 긴 코드가 있는 요청, 기존 PA/PF 재사용 요청을 각각 확인한다.
- 우선순위: 중간

### 11. 최종 확인 BOM·동반 출하품 고정 높이를 420px로 조정 — 보류
- 상태: 현재 5단계 카드 높이·열 수·내부 스크롤은 동결됐다. 이 항목을 재개하려면 동결 정책 변경을 명시적으로 승인해야 한다.
- 화면/영역: PC 출하 > 출하 요청 작성 > 5단계 `저장 및 전환`의 `BOM·동반 출하품`, `변경된 구성품`, 최종 확인 본문 스크롤 영역
- 증상: 기록 당시 `BOM·동반 출하품` 432px 고정 높이와 출하 품목 요약·변경 구성품 한 행을 합치면 1918×908 화면에서 본문 내용이 10px 넘쳐 내부 스크롤이 생겼다.
- 사용자 불편: 변경된 구성품이 1~2개로 한 줄이면 화면에 모두 들어갈 수 있는데도 최종 확인 본문을 스크롤해야 한다.
- 확정 정책: 변경 구성품이 있으면 `BOM·동반 출하품`은 420px 고정 높이를 사용한다. 변경된 구성품 카드는 행 수에 맞는 자연 높이를 유지하며, 1~2개가 한 줄인 경우 최종 확인 본문 스크롤 없이 함께 표시한다.
- 구현 방향: 5단계의 고정 행 높이와 BOM 카드 높이를 420px으로 함께 변경한다. BOM 구성품 목록의 내부 스크롤, 변경 구성품 카드의 자연 높이, 하단 action bar 고정은 유지한다.
- 수용 기준:
  - BOM 변경이 있는 요청에서 `BOM·동반 출하품` 카드 높이가 420px로 고정된다.
  - 1918×908 화면에서 변경 구성품이 1~2개로 한 줄일 때 최종 확인 본문이 스크롤되지 않는다.
  - BOM 목록은 420px 카드 내부에서만 스크롤되고, 변경 구성품 카드는 행 수에 맞게 높이가 변한다.
  - 변경 구성품이 많아 본문보다 길어지면 바깥 본문 스크롤을 사용하고 하단 action bar는 고정된다.
- 브라우저 확인 시나리오:
  1. 변경 구성품이 1개와 2개인 요청을 각각 5단계까지 진행한다.
  2. 1918×908 화면에서 BOM 카드가 420px이고 본문 스크롤이 없는지 확인한다.
  3. 변경 구성품을 여러 행으로 늘려 BOM 목록의 내부 스크롤과 바깥 본문 스크롤 전환을 확인한다.
  4. 각 경우 하단 `이전`·`출하 요청` action bar가 화면 하단에 고정되는지 확인한다.
- 우선순위: 중간

### 12. 출하 요청 작성의 품목명·코드 헤더 정렬과 안내 문구 정리 — 완료
- 점검 결과(2026-07-16): 3단계·5단계의 코드 안내 문구 제거와 PA/PF 구성품 헤더 우측 정렬을 완료했다. 상단 `출하 품목`은 품목명의 `flex-1` 확장을 제거해 실제 텍스트 끝에서 코드가 12px 간격으로 이어지도록 보정했다. 긴 품목명의 `max-w-[42%]` 말줄임은 유지한다.
- 화면/영역: PC 출하 > 출하 요청 작성 > 3단계 `BOM 매칭`의 새 PA/PF 생성 예정 카드, 5단계 `저장 및 전환`의 상단 `출하 품목` 요약과 `BOM·동반 출하품`의 PA/PF 구성품 헤더
- 증상: 3단계 새 PA/PF 생성 예정 카드와 5단계 상단 출하 품목에서 코드와 `예상 코드 · 저장 시 변경 가능` 안내가 품목명과 분리되어 읽는 흐름이 어색하다. 5단계 PA/PF 구성품 헤더에서는 품목명 뒤의 코드·안내가 카드 폭 중간에 머물러, 두 카드의 오른쪽 끝과 정렬되지 않는다.
- 사용자 불편: 사용자가 품목명에 대응하는 저장 전 코드를 빠르게 읽기 어렵고, 병렬 카드의 헤더 정보 위치가 달라 화면이 정돈되지 않아 보인다.
- 확정 정책: 예상 코드 뒤의 `예상 코드 · 저장 시 변경 가능` 안내 문구는 3단계 새 PA/PF 생성 예정 카드와 5단계 상단 출하 품목, PA/PF 구성품 헤더, 새 PA 행을 포함한 모든 출하 요청 작성 영역에서 숨긴다. 5단계 상단 출하 품목은 품목명 바로 뒤에 코드만 자연스럽게 연결해 표시한다. PA/PF 구성품 헤더는 품목명을 왼쪽에 유지하고 코드는 각 카드의 오른쪽 끝에 우측 정렬한다. 코드가 없는 기존/재사용 품목의 표시 및 줄임 동작은 유지한다.
- 구현 방향: `DesktopShippingView.tsx`의 `ShippingConclusionCard`, `ShippingShipmentHero`, `FinalRequirementGroup`에서 코드 안내 렌더링을 제거하고 헤더 레이아웃만 조정한다. 상단 요약은 품목명과 코드 메타를 동일한 읽기 흐름으로 묶고, 구성품 헤더는 제목 영역과 코드 영역을 양 끝으로 분리한다. 긴 품목명·긴 코드에서도 카드 폭을 넘기지 않도록 기존 truncate/min-width 규칙을 유지·보완한다. 5단계 카드 높이·그리드·내부 스크롤의 동결된 동작은 변경하지 않는다.
- 수용 기준:
  - 상단 `출하 품목`에서 품목명 뒤에 코드만 자연스러운 한 흐름으로 표시된다.
  - PA 및 PF 구성품 카드에서 품목명은 왼쪽, 코드는 각 카드의 오른쪽 경계에 맞춰 우측 정렬된다.
  - 기존 `예상 코드 · 저장 시 변경 가능` 문구가 3단계 새 PA/PF 생성 예정 카드와 5단계 상단 출하 품목, PA/PF 구성품 헤더 및 새 PA 행을 포함한 출하 요청 작성 화면 어디에도 표시되지 않는다.
  - 긴 품목명 또는 긴 코드가 있어도 서로 겹치거나 가로 스크롤이 생기지 않고, 필요한 텍스트만 기존 규칙에 따라 줄임 표시된다.
  - 코드가 없는 기존/재사용 품목과 카톤·동반 출하품 카드의 기존 헤더 표시는 변하지 않는다.
  - `BOM·동반 출하품`과 `변경된 구성품` 카드의 고정 높이, 열 수, 내부 스크롤 동작은 변하지 않는다.
- 브라우저 확인 시나리오:
  1. 새 PA/PF가 필요한 출하 요청의 3단계에서 생성 예정 카드에 안내 문구가 표시되지 않는지 확인한다.
  2. 새 PF 생성이 필요한 출하 요청을 5단계까지 진행해 상단 출하 품목의 이름·코드가 인접해 보이고 안내 문구가 없는지 확인한다.
  3. 새 PA와 새 PF가 모두 필요한 요청에서 PA/PF 구성품 카드의 코드가 각각 카드 오른쪽 끝에 정렬되고 안내 문구가 없는지 확인한다.
  4. 긴 품목명과 긴 코드가 있는 요청, 기존 PA/PF 재사용 요청, 카톤·동반 출하품만 있는 요청을 각각 확인해 줄임·빈 상태·카드 레이아웃 회귀가 없는지 확인한다.
- 우선순위: 중간

### 13. 최종 확인의 중복 보조 제목 제거 — 완료
- 화면/영역: PC 출하 > 출하 요청 작성 > 5단계 `저장 및 전환`의 상단 진행 제목 아래 `출하 요청` 보조 제목과 `변경된 구성품` 카드 우측 `세부 목록` 보조 제목
- 증상: 화면 맥락상 이미 전달된 정보를 `출하 요청`과 `세부 목록`이 한 번 더 표시해, 실제 데이터 영역보다 장식성 텍스트가 눈에 들어온다.
- 사용자 불편: 필요한 정보가 아닌 반복 레이블 때문에 화면이 덜 간결해 보이고, 특히 변경 구성품 카드의 우측 보조 제목은 목록 내용과 직접적인 구분 역할을 하지 못한다.
- 확정 정책: 상단 진행 제목 아래 `출하 요청` 보조 제목과 변경된 구성품 카드의 `세부 목록` 보조 제목을 표시하지 않는다. 상위 제목, 카드 제목, 변경 구성품 행의 정보 구조는 유지한다.
- 구현 방향: 해당 두 텍스트 렌더링만 제거하고, 부모 컨테이너의 여백은 제목 제거 후 빈 공간이 남지 않도록 최소 범위에서 조정한다. 5단계의 카드 크기·열·스크롤 및 다른 단계의 제목 체계는 변경하지 않는다.
- 수용 기준:
  - 상단 `출하 요청 작성` 제목 아래에 `출하 요청` 보조 제목이 표시되지 않는다.
  - `변경된 구성품` 카드 우측에 `세부 목록`이 표시되지 않는다.
  - 제거 뒤 상단 진행 영역과 변경된 구성품 카드에 불필요한 빈 여백이 남지 않는다.
  - 단계 제목, 변경 구성품 개수, 행 정보, 이전/출하 요청 동작은 유지한다.
- 브라우저 확인 시나리오:
  1. 출하 요청 작성의 5단계에서 상단 제목 아래 보조 제목이 사라졌는지 확인한다.
  2. 변경 구성품이 있는 요청을 5단계까지 진행해 카드 우측 `세부 목록`이 사라지고 행·스크롤이 정상인지 확인한다.
  3. 변경 구성품이 없는 요청과 다른 진행 단계에서도 제목·레이아웃 회귀가 없는지 확인한다.
- 우선순위: 중간

### 14. 출하 공정 코드 색상 체계 확립 — 구현 완료, 브라우저 재확인 필요
- 점검 결과(2026-07-16): 18개 공정 색상 단일 매핑과 AA/PR 대비 조정은 반영됐다. 라이트·다크 테마에서 실제 AA·PR 코드가 함께 보이는 화면으로 최종 대비만 재확인한다.
- 화면/영역: PC 출하 > 출하 요청 작성의 1·2·3·5단계, 특히 2단계 기준 PF 품명 뒤 `PA/PF 구성품` 제목, 3단계 `기존/새 PA/PF` 상태 라벨, 5단계 `BOM·동반 출하품`의 PA/PF 그룹 헤더·예상 코드·행 코드와 출하 준비·이력에서 MES 코드가 표시되는 공통 영역
- 증상: 2단계 `PA/PF 구성품` 제목과 3단계 `기존 PA/PF 재사용` 상태 라벨은 공정 색상이 적용되지 않는다. 5단계 PA 그룹 헤더와 포함 개수 배지는 초록색이지만 PA 코드(`3-PA-0037`)는 공정 색상 매핑이 없어 파란색으로 표시된다. PF와 AA도 기본 파랑으로 귀결되어, 하나의 공정 코드가 화면 요소마다 다른 색으로 보이고 서로 다른 공정 코드도 같은 색으로 보인다. PR=보라, AF=초록, PA/PF/AA 및 미분류 코드=파랑이라는 암묵적 규칙도 화면과 문서에서 명확히 드러나지 않는다.
- 사용자 불편: `PA`, `PF`, `AA`, `AF` 등 함께 표시되는 공정 코드를 색만으로 빠르게 구분할 수 없고, PA처럼 2·3·5단계의 제목·상태 라벨·코드 색이 달라 같은 공정인지도 직관적으로 판단하기 어렵다.
- 확정 정책: 18개 공정 코드는 다음 고정 색상표를 사용한다. 튜브: TR=로열 블루, TA=하늘색, TF=터쿼이즈. 고압: HR=보라, HA=금색, HF=호박색. 진공: VR=남색, VA=청록, VF=옥색. 튜닝: NR=올리브, NA=라임, NF=에메랄드. 조립: AR=코랄, AA=자홍, AF=초록. 출하: PR=보라, PA=주황, PF=파랑. 같은 공정을 뜻하는 2단계 `PA/PF 구성품` 접미사, 3단계 `기존/새 PA/PF` 상태 라벨, 그룹 헤더·포함 개수 배지·코드 문자열은 반드시 같은 색을 사용한다. 품명과 모델·일련번호는 본문 색을 유지한다. 미분류·형식 오류·코드 없음은 중립 회색으로 표시한다.
- 구현 방향: 공정 코드 색상 매핑을 한 곳에서 관리하고, `SummaryCode`, `BomEditor` 제목의 PA/PF 접미사, `ShippingConclusionCard`의 PA/PF 상태 라벨, PA/PF 그룹 헤더·배지, 출하 준비·이력의 같은 MES 코드 표시 경로가 이 매핑을 공통으로 사용하도록 정리한다. 긴 품명은 본문 색을 유지해 공정 강조와 구분한다. 적용 범위와 기존 화면별 예외를 먼저 확인한 뒤, 확정된 색상표 외의 레이아웃·데이터·상태 색상은 바꾸지 않는다.
- 수용 기준:
  - 화면에 함께 표시되는 PR, PA, PF, AR, AA, AF는 각각 보라, 주황, 파랑, 코랄, 자홍, 초록으로 명확히 다른 색상으로 표시된다.
  - 18개 공정 코드에 빠진 항목 없이 고정 색상표가 지정된다.
  - 2단계의 `PA/PF 구성품` 접미사와 3단계의 `기존/새 PA/PF` 상태 라벨이 각 공정의 확정 색상으로 표시된다.
  - 1·2·3·5단계와 출하 준비·이력의 같은 공정 코드, 그리고 PA/PF 그룹 헤더·포함 개수 배지가 항상 같은 색으로 표시된다.
  - 코드의 모델 번호와 일련번호는 본문 색을 유지하고 공정 구간만 색상이 적용된다.
  - 미분류·형식 오류·코드 없음 상태는 공정 코드 색상으로 오인되지 않는 중립 표현을 사용한다.
  - 색약 환경에서도 공정 약어(PR/PA/AF/PF)가 텍스트로 남아 정보를 식별할 수 있다.
- 브라우저 확인 시나리오:
  1. 2단계의 `PA 구성품`·`PF 구성품` 접미사와 3단계의 `기존/새 PA/PF 재사용·생성 예정` 상태 라벨이 PA/PF 표준 색상으로 보이는지 확인한다.
  2. PA/PF/AA/AF 코드가 섞인 BOM 구성과 5단계 `BOM·동반 출하품`에서 코드, 그룹 헤더, 포함 개수 배지가 각 공정별로 같은 색인지 확인한다.
  3. 18개 공정 코드가 섞인 기준 PF 검색 결과와 BOM 구성 목록에서 각 공정 구간의 색상이 모두 표준 색상표와 일치하는지 확인한다.
  4. BOM 매칭, 저장 및 전환, 출하 준비·이력에서 동일 코드가 같은 색으로 보이는지 확인한다.
  5. 미분류·코드 없음 상태를 확인해 색상 규칙 및 기존 레이아웃 회귀가 없는지 확인한다.
- 우선순위: 중간
