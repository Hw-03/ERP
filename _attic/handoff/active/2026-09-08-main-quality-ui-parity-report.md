# DEXCOWIN MES main–품질 개선 브라우저 동등성 검증 보고서

검증·정리: 2026-09-08~2026-09-10 KST

비교 판정: **의도된 차이 안내 후 적용 가능**

main 반영 상태: **미반영 — 사용자 승인 전 정지**

## 직원 관점 한 페이지 결론

고정 main과 품질 개선 버전을 서로 다른 합성 DB·포트·브라우저 세션에서 비교했다. 화면만 본 것이 아니라 로그인, 입력, 저장, 결재 요청·승인·반려·취소, 창고·부서 재고 이동, 생산·분해, 출하 준비·픽업·취소, 불량 처리와 관리자 CRUD를 실제 UI로 수행했다. 재고가 바뀌는 시험은 클릭 전 예상 수량, API 응답, 독립 SQL 전후 값, 화면·이력·원장을 함께 대조했다.

전체 206개 시나리오의 최종 분류는 **동일 177, 의도된 품질 개선 차이 21, 회귀 또는 불편 0, 미검증 8**이다. 확인한 범위에서 품질 버전이 직원의 기존 업무를 깨뜨리는 차이는 남아 있지 않다.

직원이 알 필요가 있는 차이는 다음과 같다.

- 잘못된 PIN은 명확한 오류로 구분하고, 연결 실패·서버 오류를 PIN 오류로 오인시키지 않는다.
- 일반 세션은 마지막 사용자 활동 후 30분에 잠긴다. 29분에 경고와 `로그인 유지`가 표시되며, 자동 조회는 시간을 연장하지 않는다.
- 만료 시 미저장 입력은 가린 뒤 같은 직원이 재인증한 경우에만 현재 탭 메모리에서 복구한다.
- 창고 담당자는 창고 결재, 부서 담당자는 담당 부서 결재, 두 역할 겸임자는 둘 다 처리할 수 있다. 역할 없는 직원과 관리자 인증만 한 직원은 결재 권한을 얻지 않는다.
- 인수인계 작성 화면이 없는 부서의 직접 API 작성도 차단한다.
- 대시보드 전체 카드에 `출하 중간 공정·공정 완료 품목 제외`가 표시되고, 목록 위의 별도 설명 행은 제거됐다.

실제 벽시계 30분 대기는 사용자 결정으로 생략했다. 대신 제어된 서버 시간과 +31분 절전 복귀 조건에서 잠금, 다중 탭 동기화, 활동 연장, 자동 조회 제외를 검증했다. 실물 모바일 센서·카메라·운영체제 인쇄 대화상자와 외부·직원 서버 연동은 보호 조건 또는 장비 부재로 실행하지 않았다. 이 항목들은 검증표에 이유와 함께 남겼다.

품질 후보는 GitHub의 독립 PostgreSQL 16에서 필수 두 연결 잠금·경합 게이트를 통과했다. 고정 main의 동일 PostgreSQL A/B 실행은 하지 않았으므로 해당 비교 행 자체는 미검증으로 유지한다. SQLite 결과를 PostgreSQL 통과로 대신하지 않았다.

후보를 만든 뒤 main이 `8d7fa611…`에서 `aa1a74a3…`으로 두 커밋 이동했다. 사용자는 이 두 커밋을 이번 검토에서 제외하고 나중에 확인하기로 했다. 따라서 이 보고서의 통합 후보 기준은 `8d7fa611…`이며, 현재 main에는 반영하지 않았다. 현재 main과 후보는 분기돼 있어 실제 반영 전에는 제외한 두 커밋을 별도로 검토해야 한다.

## 비교 코드와 실행 환경

| 항목 | 정확한 값 |
|---|---|
| 최초 A/B 고정 main | `27e6c9dba5465d83f837788c12c89efc5bbf07fa` |
| 품질 원본 HEAD | `d4a3bb103b4491d5731537dd611936d417525fc3` |
| 최초 pending merge 완료 | `db58a2e640261e8b0205a8227048a1f0bcad67c1` — 부모에 고정 main 포함 |
| 최종 후보의 main 기준 | `8d7fa6117a0369e0c731cd392c8081105f93af33` |
| 통합 merge | `710270099b…` — `8d7fa611…`과 `db58a2e6…` 병합 |
| 코드 검증 후보 | `ec13a9d16639ac3d97166a2eac040fc399f5bf8d` |
| 원 품질 계보 | 공통 기준 `d2b0dd29…` 이후 품질 70커밋 전부가 후보의 조상 |
| 후보 증가량 | 기준 main `8d7fa611…`보다 77커밋 앞섬 — 품질 70개와 merge·통합 수정 7개 |
| 현재 main | `aa1a74a3b38e50e028ee17ba69d4c6612038633e` — 이번 범위에서 제외한 신규 2커밋 포함 |
| A/B 주소 | 고정 main `127.0.0.1:3301/8021`, 품질 `127.0.0.1:3302/8022` |
| A/B DB | 각 버전 정식 schema/bootstrap으로 만든 별도 SQLite 합성 DB |
| 브라우저 | 실제 Chrome/Playwright, PC 1440×900·모바일 390×844·경계 viewport, `ko-KR`, KST |
| 로컬 런타임 | Node 20.20.2, Python 3.12.13 |
| PostgreSQL | GitHub Actions 격리 PostgreSQL 16.15, Alembic head, 필수 두 연결 검사 |

초기 품질 결과는 HEAD export가 아니라 pending merge의 실제 파일에서 복제했다. 최초 index tree와 소스 해시는 `../../runtime/ui-parity-20260908-155732/evidence/manifest/`에 보존했다. `C:\ERP-dev`와 직원 서버에는 접근하지 않았고, 기존 개발 서버를 시작·중지·재시작하지 않았다. 시험이 소유한 합성 DB와 프로세스만 사용·종료했다.

## 전체 화면·조작 검증표

| 판정 | 수 |
|---|---:|
| 동일 | 177 |
| 의도된 품질 개선 차이 | 21 |
| 회귀 또는 불편 | 0 |
| 미검증 | 8 |
| 합계 | 206 |

- [전체 Markdown 검증표](../../runtime/ui-parity-20260908-155732/parity-matrix.md)
- [Excel 호환 CSV](../../runtime/ui-parity-20260908-155732/parity-matrix.csv)
- 브라우저 증거: `../../runtime/ui-parity-20260908-155732/evidence/browser/`
- SQL snapshot: `../../runtime/ui-parity-20260908-155732/evidence/sql/`
- 통합·CI 로그: `../../runtime/final-integration-20260910/logs/`

실제 확인 영역은 PC 대시보드·입출고·내역·출하·불량·창고지도·주간보고·일일작업일보, 모바일 5탭·더보기·체크리스트·보고서·창고지도, 관리자 모델·품목·직원·부서·BOM·내보내기·정합성·보안이다. 숨긴 탭, 직접 URL, 비관리자, 담당 역할 조합, 빈 목록, 로딩·실패·재시도, 모달·시트·뒤로가기·포커스·키보드 조작도 개별 행으로 남겼다.

미검증 8건은 다음과 같다.

| ID | 항목 | 이유 |
|---|---|---|
| `AUTH-15` | 실제 벽시계 30분 무활동 | 사용자 결정으로 생략; 제어된 +31분 잠금은 통과 |
| `RESP-04` | 실물 모바일 회전·OS 키보드·제스처 | Chromium viewport·touch 에뮬레이션만 사용 |
| `COMMON-11` | 카메라 바코드 스캔 | 격리 호스트에 카메라 없음 |
| `COMMON-12` | 외부·직원 서버 동기화 | 보호 조건에 따라 실행 금지 |
| `HAND-05` | 운영체제 네이티브 인쇄 대화상자 | 브라우저 인쇄 콘텐츠까지만 확인 |
| `ADM-26` | 실제 배포·직원 데이터 동기화 | 직원 서버 보호 조건에 따라 실행 금지 |
| `ENV-06` | 고정 main과 품질의 PostgreSQL A/B 경합 | 품질 후보는 PASS; 고정 main의 동일 실행은 미실행 |
| `ENV-07` | 실제 직원 서버 변경 시험 | 격리 환경으로 대체하고 직원 서버는 보호 |

## 주요 실제 업무 결과

### 로그인·세션

성공·실패·잘못된 PIN, 연결 거절·지연·재시도, 새로고침 복원, 다른 탭 로그인·로그아웃 전파, 세션 만료, 관리자 인증, 로그아웃 응답 유실을 비교했다. 품질 버전은 서버가 확인한 `expires_at`을 기준으로 활동을 갱신하며, 만료·폐기 세션을 늦은 활동 요청으로 되살리지 않는다. 클릭·키보드·스크롤은 활동이고 자동 조회와 단순 탭 활성화는 활동이 아니다. 관련 구현은 `backend/app/services/operator_session.py:18-19,136-190`, `backend/app/routers/operator_sessions.py:418-436`, `frontend/app/mes/_components/login/useOperatorIdleSession.ts:16-196`이다.

### 결재·권한

창고 담당만, 부서 담당만, 겸임, 역할 없음의 네 조합에서 메뉴·건수·목록·상세·승인·반려 API를 대조했다. 직접 URL·API 우회도 같은 권한표로 차단됐다. 관리자 인증만으로 업무 결재 권한이 생기지 않았고, 요청자의 본인 요청 조회·취소는 유지됐다. 관련 구현은 `backend/app/routers/stock_requests.py:227-405,558-571,675-728`이다.

### 입출고·재고

창고/부서 입출고, 부서 이동, 자동 부서 지정, 여러 부서가 섞인 요청, BOM, 생산·분해, 자가승인·일반승인·반려·취소를 UI에서 실행했다. 확장 시나리오에서 양쪽 모두 요청 8건(완료 4·반려 2·취소 2), batch 8건, transaction 8건, 중복 client request ID 0이었고 최종 재고가 초기값으로 돌아왔다.

stale 부족 시험에서는 다른 UI 요청이 마지막 가용 1개를 먼저 예약한 뒤 오래 열린 요청을 제출했다. 양쪽 모두 422로 거절되고 대상 요청·batch·거래·재고 변화가 없었다. 품질의 처리 완료 후 응답 유실 재시도는 원 batch를 조회해 복구하며 수량을 두 번 반영하지 않았다.

### 출하

생성·검색·더보기, 준비·픽업, 각 취소, 구성품 변경, 예약·실재고·상태·이력을 확인했다. 경쟁 출하가 출하 위치 10개를 먼저 예약한 뒤 대상 준비를 누르면 422로 거절되고 대상 allocation·event·operation·거래·재고가 변하지 않았다. 고정된 5단계 카드 높이·2열·내부 스크롤도 유지됐다.

### 불량·보고서·관리자

불량 격리·정상 복귀·다건 처리·폐기·반품·분해, 통계 기간·검색·분류, 실패 후 선택 유지와 재시도를 확인했다. 일일작업일보의 작성자·날짜·탭·상세 전환과 저장·수정·취소, 주간보고·체크리스트, 창고지도와 관리자 CRUD도 실행했다. 삭제·보정·복구는 폐기 가능한 합성 데이터에서만 수행했다.

## 의도된 품질 개선 차이

| 영역 | main | 품질 | 직원 영향 |
|---|---|---|---|
| 인증 오류 | 일부 오류 구분이 약함 | PIN 오류와 연결·서버 오류 구분 | 원인을 더 명확히 알 수 있음 |
| 비활동 세션 | 고정 12시간 | 마지막 활동 30분·1분 전 경고·명시적 연장 | 장시간 자리 비움 뒤 재로그인 필요 |
| 만료 초안 | 별도 보호 없음 | 같은 직원 재인증 때만 탭 메모리 복구 | 입력 유실을 줄이고 다른 직원 노출 차단 |
| 결재 역할 | 메뉴와 일부 API 범위가 어긋남 | 창고·부서 역할을 메뉴와 API 모두 분리 | 담당 업무만 보이고 처리 가능 |
| 인수인계 작성 | UI가 숨겨져도 비작성 부서 직접 POST 201 | 직접 POST 403·문서/재고 불변 | 숨긴 기능 우회 차단 |
| 응답 유실 재시도 | 수량 중복은 없으나 완료 결과 복구가 약함 | 기존 batch를 찾아 완료 결과 복구 | 재시도 뒤 상태를 바로 확인 가능 |
| 대시보드 집계 설명 | 별도 설명 행 | 전체 카드 안에 제외 범위 표시 | 목록 높이를 덜 차지함 |

`HAND-06`은 이전 보고서에서 main 공통 문제로 잘못 분류됐지만, 최종 품질 후보가 직접 POST를 차단하므로 의도된 개선으로 정정했다. 구현은 `backend/app/services/handover.py:51-59`, 라우터 변환은 `backend/app/routers/handover.py:61-73`이다.

## 발견·수정·재검증한 회귀와 통합 문제

| 항목 | 재현·영향 | 조치와 증거 |
|---|---|---|
| 창고지도 편집 PIN 우회 | 잘못된 PIN 뒤 편집 화면이 먼저 열림 | 전용 권한 확인 후 진입하도록 수정; 403·정상 201/204·SQL 원복 확인 |
| 다른 탭 로그아웃 잠금 잔존 | 정상 로그아웃 뒤 재로그인 차단 | 서버 완료 확인과 탭 표식 정리 순서 수정; 두 탭 재로그인 확인 |
| IO 초안 직접 조회 | 인증 없이 작성 중 업무 내용 조회 가능 | 현재 세션 요청자만 허용; 수정 전 200 재현 후 401/403 확인 |
| 요청순 재고 투영 누락 | 통합 후보에서 물리 위치 원장과 요청순 계산이 어긋날 위험 | 실제 재고 투영을 합쳐 관련 보안·재고 계약 재검증 |
| 인수인계 작성 API 불일치 | 화면이 없는 부서가 직접 문서 생성 가능 | 튜브 부서 작성 정책을 서비스 경계에 적용; 관련 28개 검사 PASS |
| 백업 복구 오류 범위 | 내부 백업 게시 복구에서 후속 파일 오류까지 없는 영수증으로 오인할 위험 | `FileNotFoundError` 허용을 복구 확인표 읽기에만 한정; 후속 오류 전파 검사 PASS |
| BOM 상세 비동기 테스트 | CI가 헤더 mount 뒤 데이터 로드 전 즉시 판정 | BOM 배지 자체를 비동기로 대기; 관련 38개 검사와 스마트 게이트 PASS |

여기서 백업 `receipt`는 업무 영수증이나 스캔 기능이 아니라, DB 백업 파일 게시가 어느 단계까지 완료됐는지 기록하는 내부 복구 확인표다. 직원 화면·카메라·업무 문서와 관계없다.

## main에도 남아 있는 공통 문제

- 불량 통계 오류 화면의 초기 포커스가 alert·재시도 버튼으로 이동하지 않는다.
- 불량 완료 뒤 브라우저 뒤로가기가 완료 전 장바구니를 다시 표시한다.
- 관리자 모델 삭제가 커스텀 확인 뒤 native confirm을 한 번 더 요구한다.
- BOM 생산·분해 왕복 뒤 수량 0인 위치 행이 남지만 합계·가용량·거래는 양쪽이 같다.
- 주간보고 품목 상세를 닫은 뒤 포커스가 원래 행으로 돌아오지 않는다.
- 일일작업일보 상세 토글 중 `DailyWorkActivity`가 상위 화면 상태를 render 안에서 갱신한다는 React 경고가 양쪽에 있다.

이들은 품질 버전에서 새로 생긴 차이가 아니며 이번 범위에서 제품 코드를 추가 수정하지 않았다.

## 현재 후보 검증 결과

| 검사 | 결과 |
|---|---|
| 로컬 필수 게이트 | Node 20.20.2에서 strict lint, 앱·테스트·E2E 타입, 프런트 전체, production build·bundle PASS |
| 프런트 GitHub CI | 289 files / 2,807 tests PASS; statements·lines 92.98%, branches 91.42%, functions 90% |
| production build·bundle | PASS; 2,798,388 bytes / 2,977,955.84 bytes 한도 |
| 실제 Playwright E2E | 25 PASS, 조건부 1 skip, 실패·재시도 0; 전용 DB와 프로세스 소유권 정리 확인 |
| PostgreSQL 16 | Alembic head와 필수 두 연결 잠금·경합 게이트 PASS |
| Windows 운영 계약 | PASS |
| 검증 정책 계약 | PASS |
| 백엔드 전체 pytest·OpenAPI | PASS; 전체 pytest exit 0, OpenAPI 정본 exact match |

코드 후보 CI는 [GitHub Actions run 34449901292](https://github.com/Hw-03/ERP/actions/runs/34449901292)다. 프런트·E2E·PostgreSQL·Windows·정책 로그는 `../../runtime/final-integration-20260910/logs/ci-34449901292-*.log`에 보존한다.

읽기 전용 독립 검토에서도 P1/P2 잔존 문제를 발견하지 못했다. 검토 기록은 `../../runtime/final-integration-20260910/reviews/final-artifact-review-20260910.md`에 보존한다.

## main 반영 전 조건

1. 이 보고서와 active TODO를 포함한 최종 문서 SHA의 전체 CI가 성공해야 한다.
2. 현재 main의 제외된 두 커밋 `9312c5bb…`, `aa1a74a3…`을 나중에 검토해야 한다. 현재 main과 후보가 분기돼 있어 그 전에는 `--ff-only` 반영할 수 없다.
3. 실제 벽시계 30분 시험을 출시 조건으로 다시 지정하면 `AUTH-15`를 별도로 실행한다. 현재는 사용자 결정으로 생략했다.
4. 실물 모바일·카메라·네이티브 인쇄와 외부·직원 동기화는 승인된 실제 환경에서만 닫는다.

후보 기준 기능 판정은 **의도된 차이 안내 후 적용 가능**이다. 다만 현재 main 반영은 제외한 신규 2커밋의 후속 검토와 사용자 승인을 기다리며 멈춘다. commit/push는 품질 후보 브랜치에만 수행했고 main 병합·main push·배포·직원 서버 조작은 수행하지 않았다.
