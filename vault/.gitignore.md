---
type: code-note
project: ERP
layer: gitignore
source_path: .gitignore
status: active
updated: 2026-04-27
source_sha: 36a1bef91835
tags:
  - erp
  - gitignore
  - source-file
---

# .gitignore

> [!summary] 역할
> 원본 프로젝트의 `.gitignore` 파일을 Obsidian에서 추적하기 위한 미러 노트다.

## 원본 위치

- Source: `.gitignore`
- Layer: `gitignore`
- Kind: `source-file`
- Size: `843` bytes

## 읽는 포인트

- 실제 수정은 원본 파일에서 한다.
- Vault 노트는 구조 파악과 인수인계를 돕는 설명 레이어다.

## 원본 발췌

````text
# Python
__pycache__/
*.pyc
*.pyo
.env
.venv
venv/

# Test artifacts (Phase 5.4-H)
.pytest_cache/
.coverage
coverage/
htmlcov/
frontend/.vitest/

# Database
*.db
*.db-shm
*.db-wal

# Backend runtime logs (Phase 4)
backend/logs/

# Node
node_modules/
.next/
out/
build/
npm-debug.log*

# TypeScript build
tsconfig.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.claude/

# Dev-only generated assets
.dev/screenshots/

# Playwright MCP regression artifacts (root-level temp output)
.playwright-mcp/
/0[0-9]_*.png
/0[0-9][a-z]_*.png
/api_requests.log
/all_console_errors.log
/bom-snapshot.yml
/verify_*.png
/*-snap.yml
/verify-*.yml

# Local / personal files
workspace.ahk
backend/query_test.py

# Obsidian vault lock file (자동 생성)
vault/.obsidian/workspace.json
````

---

## 정책

- `main` 브랜치는 코드만 유지한다.
- `vault-sync` 브랜치는 같은 코드에 `vault/` 인수인계 문서를 더한다.
- 코드와 노트가 다르면 실제 코드가 우선이다.
