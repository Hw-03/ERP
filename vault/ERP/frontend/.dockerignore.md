---
type: file-explanation
source_path: "frontend/.dockerignore"
importance: normal
layer: frontend
graph: file
updated: 2026-05-22
project: DEXCOWIN MES
---

# .dockerignore — .dockerignore 설명

## 이 파일은 무엇을 책임지나

`.dockerignore`는 Git/도구 설정입니다. 프로젝트 구조 안에서 `frontend/.dockerignore` 위치에 있으며, 필요할 때 역할과 연결 파일을 확인하기 위한 설명을 둡니다.

## 업무 흐름에서의 의미

사용자가 화면에서 보고 누르는 경험과 직접 연결됩니다. 문구, 버튼, 표, 상세 패널 개선은 이 계층에서 확인합니다.

## 언제 보면 좋나

- 이 파일이 맡은 화면/API/데이터 흐름을 확인해야 할 때
- 수정 전에 영향 범위를 빠르게 파악해야 할 때

## 중요한 내용

자동으로 뽑을 수 있는 함수/클래스 목록은 적지만, 파일 위치와 확장자로 볼 때 위 역할을 맡습니다.

## 연결되는 파일

- [[ERP/frontend/📁_frontend]] — 이 파일이 속한 폴더의 안내판입니다.

## 핵심 발췌

```text
# frontend Docker 이미지에 들어가면 안 되는 것 (build context = frontend/)
# Dockerfile: package*.json → npm ci → COPY . . → npm run build.
# 아래는 npm ci / npm run build 가 재생성하므로 로컬본을 이미지에 복사하면 안 됨.

# 의존성·빌드 산출물 (npm ci / next build 가 재생성)
node_modules/
.next/
.next-prod/
out/

# 테스트·커버리지 — 레포엔 남기지만 운영 이미지엔 불필요
**/__tests__/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
vitest.config.mts
vitest.setup.ts
coverage/
.vitest/

# TS 증분 캐시
tsconfig.tsbuildinfo

# 로컬 환경/로그 (운영 env 는 compose 에서 주입)
.env.local
*.log

# 격리소 (혹시 컨텍스트에 포함돼도 차단)
_attic/
```
