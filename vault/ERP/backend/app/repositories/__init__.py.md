# __init__.py

## 이 파일은 뭐예요?
`repositories` 패키지 초기화 파일. 실질적인 내용은 없으며(1줄, 빈 파일), 이 디렉터리를 Python 패키지로 인식시키는 역할만 한다.

## 언제 보나요?
- `repositories` 패키지 구조 자체를 확인할 때
- 패키지 레벨 import가 왜 동작하는지 추적할 때

## 중요한 내용
- 내용 없음. 빈 패키지 마커 파일.

## 연결되는 파일
### 먼저 볼 파일
- [[ERP/backend/app/repositories/inventory_repository.py]] — 실제 repository 로직
- [[ERP/backend/app/repositories/item_repository.py]] — 실제 repository 로직
