# __init__.py

## 이 파일은 뭐예요?
`backend/tests/dependencies/` 폴더를 Python 패키지로 인식시키는 빈 초기화 파일입니다. 실제 코드 내용은 없습니다.

## 언제 보나요?
- pytest가 `tests/dependencies/` 디렉터리를 패키지로 수집할 때 필요한 마커 파일이므로 직접 볼 일은 없습니다.
- 임포트 오류나 테스트 수집 실패가 날 때 이 파일이 있는지 확인합니다.

## 중요한 내용
- 내용 없음 (빈 파일)

## 연결되는 파일
### 먼저 볼 파일
- [[ERP/backend/tests/dependencies/test_admin.py]] — 이 패키지 안의 실제 테스트 파일
