---
type: code-note
project: DEXCOWIN MES
layer: backend
status: stub
created: 2026-05-21
updated: 2026-05-21
source_path: erp/backend/.dockerignore
tags: [vault, code-note, auto-generated, stub]
---

# .dockerignore

> [!info] 자동 생성된 stub 노트
> 이 노트는 자동 보정으로 생성됐다. 원본 위치: [[erp/backend/.dockerignore]]
> 신입이 자주 보게 되면 A/B/C 계층으로 승격 예정.

## 원본 첫 줄

```
# backend Docker 이미지에 들어가면 안 되는 것 (build context = backend/)
# 원인: Dockerfile 이 COPY . . 라서 .dockerignore 없으면 잡동사니 전부 이미지에 구워짐.

# 테스트 — 레포엔 남기지만(verify 게이트용) 운영 이미지엔 불필요
tests/

# Python 캐시
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 로컬 SQLite (운영은 docker-compose 의 Postgres 사용 → 이미지에 DB 불필요)
*.db
*.db-shm
*.db-wal
*.db.bak*
*.db.backup-*
_backup/

# 런타임 로그
logs/

# 시크릿/개인 파일 (.env.example 은 매칭 안 됨 — 유지)
.env
query_test.py

# 격리소 (혹시 컨텍스트에 포함돼도 차단)
_attic/
```
