---
type: guide
project: DEXCOWIN MES
status: active
updated: 2026-05-22
tags:
  - layer/meta
  - topic/glossary
  - audience/all
aliases:
  - 용어 풀이
  - Glossary
  - 사전
---

# 📚 용어사전

> [!summary] 용도
> DEXCOWIN MES 코드/문서/UI 에서 마주칠 전문용어를 한 곳에 모아 풀어 설명. 변경된 식별자(`ItemCode` 등)·새 라우터·새 트랜잭션 타입까지 모두 반영 (2026-05-22 기준).

> [!info] 핵심 변경 (2026-04-28 → 2026-05-22)
> - **ErpCode → ItemCode** (commit f1ff96c) · **UtcDatetime** 전체 응답 확산 (commit 4db421a)
> - 죽은 라우터 제거: queue / alerts / counts / loss / ship_packages
> - 불량 처리 시스템 재설계: 격리 → 처리 2단계, `_defect_hub` 컴포넌트 신규
> - 시스템 공식명은 **DEXCOWIN MES**. ERP / X-Ray라 부르지 않는다.

---

## 🗺 한눈에 보기

| 분류 | 대표 용어 |
|---|---|
| 식별자 | [[#ItemCode]] · [[#SR-yyyymmdd-...]] |
| 도메인 모델 | [[#Item 품목]] · [[#Inventory 재고]] · [[#InventoryLocation 위치 재고]] · [[#BOM Bill of Materials]] |
| 트랜잭션 (11종) | [[#트랜잭션 타입 한 장 표 TransactionTypeEnum 11종]] |
| 결재/요청 | [[#StockRequest 결재 요청]] · [[#warehouse_role]] · [[#department_role]] |
| 프론트 UI | [[#FlowBadge]] · [[#legacy 폴더]] · [[#모바일 wizard]] · [[#불량 처리 허브]] · [[#_defect_hub]] · [[#DefectHub]] |
| 불량 처리 | [[#격리 Quarantine]] · [[#InventoryLocation 위치 재고]] · [[#MARK_DEFECTIVE / SCRAP / SUPPLIER_RETURN / DISASSEMBLE]] |
| 운영 | [[#bootstrap_db.py --all]] · [[#verify_local.ps1]] · [[#vault-sync]] · [[#_attic]] · [[#DEXCOWIN MES]] |

---

## ㄱ

### 가용 수량 (available)

> [!info] 실제로 지금 쓸 수 있는 재고

```
available = warehouse_qty + Σ(InventoryLocation.quantity, status=PRODUCTION) − pending_quantity
```

격리(`DEFECTIVE`) 수량은 가용에서 빠진다. 큐 배치로 50개 예약돼 있으면 실제 가용도 50개 줄어듦.

**사용처**: `backend/app/services/inventory.py`, `backend/app/services/stock_math.py`

**관련 용어**: [[#Inventory 재고]], [[#예약 수량 pending_quantity]], [[#InventoryLocation 위치 재고]]

### 가상 테이블 (ORM 모델)

> [!info] DB 테이블을 파이썬 클래스로 표현한 것 — `backend/app/models.py` 의 `Item` 클래스가 `items` 테이블과 연결

### 격리 (Quarantine)

> [!info] 정상 재고를 DEFECTIVE 상태로 변경하는 즉시 처리 작업

`POST /defects/quarantine` 또는 `POST /api/inventory/mark-defective` 호출. 결재 없이 즉시 반영된다. `InventoryLocation.status`를 `PRODUCTION → DEFECTIVE`로 변경. 총 보유량(`Inventory.quantity`)은 변동 없음.

**사용처**: `backend/app/routers/defects.py`, `backend/app/routers/inventory/defective.py`

**관련 용어**: [[#InventoryLocation 위치 재고]], [[#MARK_DEFECTIVE / SCRAP / SUPPLIER_RETURN / DISASSEMBLE]], [[#불량 처리 허브]]

### 공정 코드 (process_type_code)

> [!info] 품목이 어느 공정 단계인지 나타내는 2자리 코드 (총 18개)

예: `TR`(튜브 Raw), `PA`(완제품 Assembly). 첫 글자 = 공정 종류 (T튜브 / H고압 / V진공 / A조립 / P완제품), 둘째 글자 = 단계 (R 원자재 / A 조립). 품목 분류 단일 기준.

**사용처**: `backend/app/models.py` (`ProcessType`)

**관련 용어**: [[#ItemCode]], [[#기호 product symbol]]

### 기호 (product symbol)

> [!info] 제품 모델을 나타내는 숫자 1자리

`3`=DX3000, `4`=ADX4000W, `6`=ADX6000FB, `7`=COCOON, `8`=SOLO. `product_symbols` 테이블 100개 슬롯. 공용 부품은 `346` 처럼 정렬 연결.

**관련 용어**: [[#ItemCode]], [[#model_symbol]]

---

## ㄴ

### 낱개 IO (manual / adjust_in / adjust_out)

> [!info] BOM 묶음 없이 단일 품목 단위로 처리하는 입출고

**사용처**: `backend/app/services/io.py` (`_submit_immediate`), `backend/app/routers/io.py`

---

## ㄷ

### 대시보드 (dashboard)

> [!info] KPI / 위치별 재고 / 게이지 / 빨간 불량 행을 한 화면에서 보는 진입점 — 빨간 불량 수량 클릭 시 [[#불량 처리 허브]] 로 점프

**사용처**: `frontend/app/legacy/_components/_dashboard_*`

---

## ㄹ

### 라우터 (router)

> [!info] API 엔드포인트를 기능별로 묶은 파일 (FastAPI). `backend/app/routers/` 에 위치

> [!warning] 죽은 라우터 제거됨
> 과거 `queue` / `alerts` / `counts` / `loss` / `ship_packages` 라우터는 모두 삭제됨. 옛 문서에서 이 이름들을 보면 무시.

### legacy 폴더

> [!warning] 이름은 "legacy"지만 실제 활성 UI

`frontend/app/legacy/` 는 데스크탑 입출고탭, 대시보드, 내역 화면 등을 포함한 **현재 운영 중**인 UI 경로. "legacy 라서 만지면 안 되겠지" 라는 가정은 틀림.

**관련 용어**: [[#FlowBadge]], [[#모바일 wizard]], [[#_attic]]

---

## ㅁ

### 마스터 (master data)

> [!info] 잘 바뀌지 않는 기준 데이터

품목 마스터, 직원 마스터, 코드 마스터(공정 코드 / 기호 / 옵션) 등.

### model_symbol

> [!info] 한 품목이 사용되는 제품들의 [[#기호 product symbol]]를 정렬해서 이어 붙인 문자열

예: `[1, 4, 5]` 슬롯을 쓰면 `"346"` 이 된다. `Item.model_symbol` 컬럼에 저장됨.

### 모바일 wizard

> [!info] 모바일 입출고 단계별 흐름 (Step 1~5)

**사용처**: `frontend/app/legacy/_components/mobile/io/MobileIoComposeWizard.tsx`

---

## ㅂ

### 배치 (batch / io_batch)

> [!info] 한 번에 처리할 자재 묶음 (트랜잭션 그룹) — `io_batches` = 헤더, `TransactionLog.operation_batch_id` = FK

### 백플러시 (BACKFLUSH)

> [!info] 완제품 생산 확정 시 BOM 자재를 자동으로 빼는 처리 — "거꾸로 씻어내린다". 수동 자재 출고 불필요

**사용처**: `backend/app/services/production.py`

**관련 용어**: [[#PRODUCE]], [[#BOM Bill of Materials]]

### bootstrap_db.py --all

> [!info] DB 초기화 / 시드 / 마이그레이션 명령

```bash
cd backend
python bootstrap_db.py --all
```

> [!warning] DB 변경 작업
> 서버 시작은 DB를 바꾸지 않는다. DB 가 바뀌는 건 오직 `bootstrap_db.py --all` 실행 시. 작업 전 영향도를 먼저 설명해야 한다 (CLAUDE.md 규칙).

### BOM (Bill of Materials, 자재명세서)

> [!info] 제품 1개 만들려면 어떤 자재가 몇 개 필요한지 기록한 표

예: "ADX6000 1대 = 튜브 1개 + 고압보드 2개 + 조립품 1개". 재귀 전개, 최대 깊이 10 (순환 방지).

**관련 용어**: [[#백플러시 BACKFLUSH]], [[#DISASSEMBLE]]

### 불량 (defective)

> [!info] 쓸 수 없다고 판단된 재고 — `InventoryLocation.status = DEFECTIVE` 행이 부서별 불량 풀

**관련 용어**: [[#InventoryLocation 위치 재고]], [[#MARK_DEFECTIVE / SCRAP / SUPPLIER_RETURN / DISASSEMBLE]], [[#불량 처리 허브]]

### 불량 처리 허브

> [!info] 입출고탭 → 요청 작성 → Step 1 [불량] → 진입하는 별도 화면 (대시보드 패턴)

상단 KPI 카드 4개(격리 중 / 1년 이상 / 결재 대기 / 오늘 처리) + 액션 버튼 3개 + 부서별 접고 펼치는 목록. 대시보드 빨간 불량 수량 클릭으로도 진입.

**사용처**: `frontend/app/legacy/_components/_defect_hub/DefectHub.tsx`

**관련 용어**: [[#MARK_DEFECTIVE / SCRAP / SUPPLIER_RETURN / DISASSEMBLE]], [[#_defect_hub]], [[#DefectHub]]

---

## ㅅ

### 스키마 (schema)

> [!info] 두 가지 뜻 — (1) DB 스키마 = 테이블/컬럼 정의 (`backend/app/models.py`), (2) API 스키마 = Pydantic 클래스 (`backend/app/schemas.py`)

**관련 용어**: [[#Pydantic]], [[#UtcDatetime]]

### 시리얼 (serial_no)

> [!info] ItemCode 의 3번째 파트 — `process_type` 카테고리 내부에서 모델 무관 전역 유일

### SR-yyyymmdd-...

> [!info] StockRequest 의 `request_code` 형식

```
SR-{YYYYMMDD}-{HHMMSS}-{XXXXXXXX}
```

마지막 8자리: hex 랜덤 (32비트 엔트로피).

**관련 용어**: [[#StockRequest 결재 요청]]

### StockRequest (결재 요청)

> [!info] 입출고 결재 요청 — 창고 재고가 움직이는 작업은 승인 후에만 실재고 반영

**상태 흐름**: `draft → submitted → reserved → completed` (또는 `rejected` / `cancelled` / `failed_approval`)

**관련 용어**: [[#SR-yyyymmdd-...]], [[#warehouse_role]], [[#department_role]]

---

## ㅇ

### 예약 수량 (pending_quantity)

> [!info] 큐 배치가 출고 예정으로 찜해둔 창고 수량

**관련 용어**: [[#가용 수량 available]]

### ItemCode

> [!info] 품목 고유 식별자 (구 ErpCode) — DB 컬럼 `items.item_code` (UNIQUE)

**4파트 구조**: `{model_symbol}-{process_type}-{serial:04d}[-{option}]`

| 예시 | 의미 |
|---|---|
| `346-AR-0001` | DX3000/ADX4000W/ADX6000 공용 AR 자재 1번 |
| `3-PA-0012-BG` | DX3000 전용 완제품, 옵션 BG |
| `34678-TR-0023` | 5개 모델 공용 튜브 |

> [!warning] legacy 식별자 보존
> 도메인 코드 안의 `erp_code` 변수/함수명은 일부 보존됨. 함부로 rename 금지.

**관련 용어**: [[#model_symbol]], [[#공정 코드 process_type_code]], [[#시리얼 serial_no]]

### Item (품목)

> [!info] 관리 대상이 되는 자재/반제품/완제품

각 품목은 고유 `item_id` (UUID) + `item_code` ([[#ItemCode]]) 를 갖는다.

### Inventory (재고)

> [!info] 한 품목의 전사 재고 요약

`items` 와 1:1. 컬럼:
- `quantity` — 총 보유 (창고 + 부서 합)
- `warehouse_qty` — 창고 보관량
- `pending_quantity` — 예약분 ([[#예약 수량 pending_quantity]])

### InventoryLocation (위치 재고)

> [!info] 부서 × 상태 단위 재고 분포

`(item_id, department, status)` 당 1행. `status` 는 `PRODUCTION` (정상) 또는 `DEFECTIVE` (격리).

```python
class LocationStatusEnum(str, enum.Enum):
    PRODUCTION = "PRODUCTION"
    DEFECTIVE = "DEFECTIVE"
```

> [!info] 핵심 설계
> 동일 부서 안에서 정상/불량 수량이 같은 행에 합쳐지지 않고, **상태별로 별도 행**으로 쌓인다. 격리는 즉시 / 정상 복귀도 즉시 (결재 X).

**관련 용어**: [[#MARK_DEFECTIVE]], [[#불량 처리 허브]], [[#재고 3계층]]

### import 경로 검증

> [!info] 프론트엔드 작업 전 실제 렌더 경로 먼저 확인 — CLAUDE.md: "Before editing frontend code, verify the real render/import path first"

---

## ㅈ

### 재고 3계층

> [!info] DEXCOWIN MES 의 핵심 설계

| 계층 | 컬럼 | 의미 |
|---|---|---|
| 창고 재고 | `Inventory.warehouse_qty` | 중앙 창고에 있는 수량 |
| 부서 정상 | `InventoryLocation.quantity (status=PRODUCTION)` | 각 부서 정상 풀 |
| 부서 불량 | `InventoryLocation.quantity (status=DEFECTIVE)` | 각 부서 격리 풀 |

`Inventory.quantity = warehouse_qty + Σ(InventoryLocation.quantity)` 항등식이 항상 성립해야 한다.

### 거래 로그 (transaction_logs)

> [!info] 모든 재고 변동을 기록하는 이력 테이블

모든 재고 변동은 `transaction_logs` 에 한 줄씩 영구 기록. 타입 11종 전체 표는 [[#트랜잭션 타입 한 장 표 TransactionTypeEnum 11종]] 참조.

---

## ㅊ

### 차이 (variance)

> [!info] 배치 확정 시 BOM 예정 수량과 실제 반영 수량의 차이 — `variance_logs` 에 기록

---

## ㅌ

### 트랜잭션 (transaction)

> [!info] 두 가지 뜻
> 1. **DB 관점** — 여러 SQL 작업을 한 덩어리로 묶어 "전부 성공 or 전부 실패" 로 처리
> 2. **거래 관점** — 재고 변동 한 건 (`transaction_logs` 의 row 하나)

---

## ㅎ

### 호환성 / 유산 식별자

> [!warning] 코드 안에 의도적으로 보존된 옛 이름들
> - `erp_code` — 도메인 코드 안의 식별자. 도메인 이름은 [[#ItemCode]] 로 바꿨지만 변수/함수 이름은 보존.
> - `xray-erp` — 옛 프로젝트 별칭. 함부로 rename 금지.
> - `frontend/app/legacy/` — 이름은 legacy 지만 실제 활성 UI ([[#legacy 폴더]]).

---

## 🔤 영문 / 약어 — A–Z

### 트랜잭션 타입 한 장 표 (TransactionTypeEnum 11종)

> [!info] 현재 사용 중인 모든 거래 타입

| 타입 | 의미 | 결재 | 비고 |
|---|---|---|---|
| `RECEIVE` | 외부 → 창고 입고 | ○ | `raw_receive` 와 연결 |
| `PRODUCE` | 생산 (완제품 IN) | ○ | 확정 시 [[#백플러시 BACKFLUSH]] 자동 발생 |
| `SHIP` | 창고 → 외부 출고 | ○ | `raw_ship`, `package_out` |
| `ADJUST` | 수량 보정 | ○ | 낱개 IO + 부서 `correction` 매핑 |
| `BACKFLUSH` | BOM 자재 자동 차감 | (자동) | `PRODUCE` 확정 시 따라옴 |
| `DISASSEMBLE` | 분해 (PA/PF → 자재) | ○ | 본인 소멸 + 자식 입고/폐기 |
| `TRANSFER_TO_PROD` | 창고 → 부서 | ○ | `warehouse_to_dept` |
| `TRANSFER_TO_WH` | 부서 → 창고 | ○ | `dept_to_warehouse` |
| `TRANSFER_DEPT` | 부서 → 부서 | ○ | `dept_internal` |
| `MARK_DEFECTIVE` | 정상 ↔ 격리 | × (즉시) | `InventoryLocation.status` 토글 |
| `SUPPLIER_RETURN` | 공급처 반품 | ○ | R 정상/격리 모두 가능 |

> [!warning] 제거된 타입
> `LOSS` / `RETURN` / `RESERVE` / `RESERVE_RELEASE` / `SCRAP`(일부) — commit b792f7a 에서 죽은 거래 타입 5종 제거. 옛 문서에서 보이면 무시.

### _defect_hub

> [!info] 불량 처리 UI 폴더 — 격리 추가 모달, PA/PF 위자드, R 처리 위자드 포함

`frontend/app/legacy/_components/_defect_hub/` 에 위치. 신규 추가된 폴더 (불량 처리 재설계 결과물).

**관련 용어**: [[#불량 처리 허브]], [[#DefectHub]], [[#격리 Quarantine]]

### DefectHub

> [!info] 불량 처리 허브 패널 React 컴포넌트

KPI 카드 4개 + 액션 버튼 3개 + 부서별 접고 펼치는 목록으로 구성.

**사용처**: `frontend/app/legacy/_components/_defect_hub/DefectHub.tsx`

**관련 용어**: [[#_defect_hub]], [[#불량 처리 허브]]

### DEXCOWIN MES

> [!info] 시스템 공식명

> [!quote] CLAUDE.md
> "Official system name: **DEXCOWIN MES**. Do not call it ERP or X-Ray in user-facing text or documents."

사내 코드/식별자(`xray-erp`, `erp_code` 등) 는 보존하되, 화면 문구·문서·발표 자료에서는 항상 DEXCOWIN MES 로 표기.

### FlowBadge

> [!info] 입출고 내역에서 흐름을 표시하는 알약형 뱃지 (프론트)

거래 타입 아이콘 + 라벨 + 톤 컬러를 한 번에 보여주는 React 컴포넌트.

**사용처**: `frontend/app/legacy/_components/_history_sections/historyTableHelpers.tsx`

### MARK_DEFECTIVE / SCRAP / SUPPLIER_RETURN / DISASSEMBLE

> [!info] 불량 처리 4대 액션 트랜잭션

| 타입 | 의미 | 결재 | 격리 경유 |
|---|---|---|---|
| `MARK_DEFECTIVE` | 정상 → 불량 (격리). 역방향 = 정상 복귀 | × | — |
| `SCRAP` | 폐기 (재고 영구 소멸, 로그는 보존) | ○ | 거치거나 R정상에서 바로 |
| `SUPPLIER_RETURN` | 공급처 반품 (R 한정) | ○ | 거치거나 R정상에서 바로 |
| `DISASSEMBLE` | PA/PF 분해. 자식 = 분해 부서 정상 재고로 즉시 입고 | ○ | 격리만 가능 |

> [!info] 격리 = 재작업 대기 풀 통합
> 별도 "재작업중" 상태는 만들지 않음. 격리 풀에 1년씩 묵을 수 있음.

**관련 용어**: [[#InventoryLocation 위치 재고]], [[#불량 처리 허브]]

### UtcDatetime

> [!info] UTC 시각 직렬화 통합 Pydantic Annotated 타입 — naive `datetime` 을 `+00:00` 으로 명시 직렬화

> [!quote] commit 4db421a
> "F4b 완료 — UtcDatetime을 전체 응답 스키마로 확산 (9h 오차 근본 수정)"

응답 스키마(`ItemResponse`, `EmployeeResponse`, `StockRequestResponse` 등) 전체로 확산됨.

**사용처**: `backend/app/schemas.py`

### verify_local.ps1

> [!warning] commit/push 전 반드시 실행 — GitHub CI 실패 방지
> ```powershell
> powershell -ExecutionPolicy Bypass -File .\scripts\dev\verify_local.ps1
> ```

### vault-sync

> [!info] vault/ 폴더가 포함된 유일한 브랜치

`vault/guides/` (이 문서가 있는 곳) 는 `vault-sync` 브랜치에서만 추적됨. main 에는 vault 폴더가 없다.

### warehouse_role

> [!info] 창고 결재 역할

값: `none` / `primary` / `deputy`. 창고 정/부 결재자.

**관련 용어**: [[#department_role]]

### department_role

> [!info] 부서 결재 역할

값: `none` / `primary` / `deputy`. 부서 정/부 결재자.

> [!warning] 결재 라우팅
> 라인(튜브/고압/진공/튜닝/조립/출하) 자체에는 별도 부서장이 없고, 산하 6라인 결재는 생산부장(정/부) 또는 창고장(정/부)이 처리.

**관련 용어**: [[#warehouse_role]], [[#부서 department]]

### _attic

> [!warning] 격리/임시 보관 폴더 — 재구성 / 이동 / rename 금지

`_attic/` 은 archived source material, backups, 옛 작업 노트를 담은 의도적 격리 공간.
예외: `_attic/docs/openapi.json` 같은 게이트 파일은 별도 룰을 따른다.

---

## 🅰 영문 약어 표

| 약어 | 풀이 | | 약어 | 풀이 |
|---|---|---|---|---|
| API | Application Programming Interface | | ORM | Object-Relational Mapping |
| BOM | Bill of Materials (자재명세서) | | PA | 완제품 Assembly (공정 코드) |
| CRUD | Create / Read / Update / Delete | | PF | 완제품 Finished (출하성, 창고 X) |
| DB | Database | | PK | Primary Key |
| FK | Foreign Key | | PR | 완제품 Raw |
| FG | Finished Good (완제품) | | R | Raw Material (원자재) |
| JSON | JavaScript Object Notation | | REST | Representational State Transfer |
| MES | Manufacturing Execution System | | SQL | Structured Query Language |
| MOC | Map of Content | | SR | Stock Request prefix |
| TR/TA | 튜브 Raw / Assembly | | UI / UTC | User Interface / Coordinated UT |

---

## 📌 빠른 인덱스

| 명령 | 용도 |
|---|---|
| `cd backend && python bootstrap_db.py --all` | DB 초기화 / 시드 / 마이그레이션 |
| `cd backend && python -m uvicorn app.main:app --reload` | 백엔드 실행 (DB 안 바꿈) |
| `powershell -ExecutionPolicy Bypass -File .\scripts\dev\verify_local.ps1` | 로컬 검증 (commit/push 전 필수) |

| 영역 | 경로 |
|---|---|
| 백엔드 모델 / API 스키마 | `backend/app/models.py`, `backend/app/schemas.py` |
| 라우터 / 서비스 | `backend/app/routers/`, `backend/app/services/` |
| 활성 UI (데스크탑·모바일) | `frontend/app/legacy/_components/` |
| 불량 처리 허브 | `frontend/app/legacy/_components/_defect_hub/` |
| 입출고 내역 / 입출고탭 | `_history_sections/`, `_warehouse_v2/` |
| 편집 금지 / 격리 보관 | `_attic/` |
| Vault 가이드 (이 파일) | `vault/guides/` (vault-sync 브랜치) |

---

## 🔗 관련 문서

- [[guides/처음_읽는_사람]] · [[guides/전체_컨텍스트]] — 인수인계 진입 가이드
- [[guides/위험지대_지도]] — 어디가 무너지기 쉬운지
- [[guides/볼트_갱신_작업요령]] — vault-sync 갱신 루틴
- `docs/defect-handling-redesign.md` — 불량 처리 허브 설계 합의 원본
- `CLAUDE.md` — 프로젝트 규칙 (공식 명칭, verify 명령 등)

Up: [[ERP/📁_ERP]]
