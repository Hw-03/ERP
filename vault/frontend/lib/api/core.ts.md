---
type: code-note
project: DEXCOWIN MES
layer: frontend
status: stub
created: 2026-05-21
updated: 2026-05-21
source_path: erp/frontend/lib/api/core.ts
tags: [vault, code-note, auto-generated, stub]
---

# core.ts

> [!info] 자동 생성된 stub 노트
> 이 노트는 자동 보정으로 생성됐다. 원본 위치: [[erp/frontend/lib/api/core.ts]]
> 신입이 자주 보게 되면 A/B/C 계층으로 승격 예정.

## 원본 첫 줄

```
/**
 * API core — fetch wrapper / URL 빌더 / 에러 파서.
 *
 * 본 파일은 `frontend/lib/api-core.ts` 의 thin re-export.
 * 외부 호환성 유지를 위해 기존 위치 (`@/lib/api-core`) 도 그대로 사용 가능.
 * 새 코드는 `@/lib/api/core` 사용을 권장 (도메인 분리 후 일관성).
 */
export * from "../api-core";
```
