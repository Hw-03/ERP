# Legacy Duplicate App Route Snapshot

This archive preserves the previous `frontend/app/legacy/*` implementation.

- Purpose: reference-only snapshot of the duplicate app-route implementation
- Status: not used by the live app
- Live route behavior now delegates to `frontend/legacy/page.tsx`
