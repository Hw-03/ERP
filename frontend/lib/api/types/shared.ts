/**
 * 공통 / cross-domain 타입 — `@/lib/api/types/shared`.
 * Round-7 (R7-T) 분리. types.ts 정본에서 re-export.
 */
export type {
  ProcessTypeCode,
  TransactionType,
  LocationStatus,
  Department,
  EmployeeLevel,
  WarehouseRole,
  ProcessTypeSummary,
  InventoryLocationRow,
} from "../types";
