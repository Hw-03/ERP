/**
 * Operations 도메인 타입 — `@/lib/api/types/operations`.
 * (Alerts, PhysicalCounts, Scrap/Loss/Variance)
 */
export type {
  AlertKind,
  StockAlert,
  PhysicalCount,
  ScrapLogRow,
  LossLogRow,
  VarianceLogRow,
} from "../types";
